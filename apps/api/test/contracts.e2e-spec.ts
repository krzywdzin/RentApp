describe('Contracts (e2e)', () => {
  it.todo('POST /contracts creates contract from rental');
  it.todo('POST /contracts/:id/sign accepts signature');
  it.todo('GET /contracts/:id returns contract with signatures');
  it.todo('full flow: create + sign all + PDF + email');
  it.todo('POST /contracts/:id/annex creates annex');
});
