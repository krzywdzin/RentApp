---
phase: 09.1-mobile-and-admin-bug-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/rentals/index.tsx
  - apps/mobile/src/components/ErrorBoundary.tsx
  - apps/mobile/src/providers/index.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "App does not crash on unhandled promise rejections or component errors"
    - "Error boundary catches component crashes and shows recovery UI instead of white screen"
    - "Lists use optimized rendering to reduce lag"
  artifacts:
    - path: "apps/mobile/src/components/ErrorBoundary.tsx"
      provides: "Global error boundary component"
      contains: "componentDidCatch"
    - path: "apps/mobile/src/providers/index.tsx"
      provides: "Provider tree with error boundary wrapping"
      contains: "ErrorBoundary"
  key_links:
    - from: "apps/mobile/src/providers/index.tsx"
      to: "apps/mobile/src/components/ErrorBoundary.tsx"
      via: "wraps app children"
      pattern: "ErrorBoundary"
---

<objective>
Fix BUG-6 (app slowness/lag) and BUG-7 (random crashes).

Purpose: The mobile app has performance issues and random crashes. Add error boundaries to prevent crashes from killing the app, and apply React performance optimizations to reduce lag.
Output: Stable, responsive mobile app with error recovery.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error boundary and crash prevention (BUG-7)</name>
  <files>apps/mobile/src/components/ErrorBoundary.tsx, apps/mobile/src/providers/index.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <read_first>
    - apps/mobile/src/providers/index.tsx
    - apps/mobile/app/(tabs)/_layout.tsx
    - apps/mobile/app/_layout.tsx
  </read_first>
  <action>
1. **Create ErrorBoundary component** at `apps/mobile/src/components/ErrorBoundary.tsx`:
   - Class component with `componentDidCatch(error, errorInfo)` that logs the error
   - `getDerivedStateFromError` sets `hasError: true`
   - Fallback UI shows: "Cos poszlo nie tak" message with a "Sprobuj ponownie" button that resets error state
   - Style: centered, white background, icon (exclamation), error message, retry button using AppButton

2. **Wrap app in ErrorBoundary** in provider tree:
   - In `apps/mobile/src/providers/index.tsx` (or root layout), wrap the children with `<ErrorBoundary>` as the outermost wrapper
   - This catches any component error in the tree and shows fallback instead of crashing

3. **Add global unhandled rejection handler** in root layout (`apps/mobile/app/_layout.tsx`):
   - In a useEffect, register: `const handler = (event: any) => { console.warn('Unhandled rejection:', event?.reason); }; global.addEventListener?.('unhandledrejection', handler);` (if available in React Native context)
   - Or use ErrorUtils: `ErrorUtils.setGlobalHandler((error, isFatal) => { console.error('Global error:', error); });` -- but be careful not to override expo's handler
   - A simpler approach: just add the ErrorBoundary wrapper, which handles the majority of crash scenarios
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBoundary.tsx exists with componentDidCatch and fallback UI
    - Provider tree wraps children in ErrorBoundary
    - Fallback UI has retry button that resets error state
  </acceptance_criteria>
  <done>App catches component errors gracefully and shows recovery UI instead of crashing to white screen</done>
</task>

<task type="auto">
  <name>Task 2: Optimize list rendering and reduce re-renders (BUG-6)</name>
  <files>apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/rentals/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/index.tsx
    - apps/mobile/app/(tabs)/rentals/index.tsx
    - apps/mobile/src/components/AppCard.tsx
  </read_first>
  <action>
Apply standard React Native performance optimizations:

1. **In dashboard (`apps/mobile/app/(tabs)/index.tsx`)**:
   - Wrap expensive computed values in `useMemo` if not already
   - Wrap FlatList renderItem callbacks in `useCallback`
   - Add `removeClippedSubviews={true}` to any FlatList
   - Add `maxToRenderPerBatch={10}` and `windowSize={5}` to FlatList for windowed rendering
   - Add `initialNumToRender={10}` to limit initial render batch

2. **In rental list (`apps/mobile/app/(tabs)/rentals/index.tsx`)**:
   - Same FlatList optimizations: `removeClippedSubviews`, `maxToRenderPerBatch`, `windowSize`, `initialNumToRender`
   - Wrap renderItem in useCallback
   - Ensure `keyExtractor` uses stable keys (item.id)
   - Add `getItemLayout` if items have fixed height for instant scroll-to calculations

3. **General**: Memoize list item components with `React.memo()` if they are defined inline or as separate components that receive props. Check if AppCard is already memoized -- if not, wrap its export with React.memo.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - FlatList components have removeClippedSubviews, maxToRenderPerBatch, windowSize props
    - renderItem callbacks are wrapped in useCallback
    - List items use stable keyExtractor
  </acceptance_criteria>
  <done>Lists render smoothly without visible lag, FlatList uses windowed rendering to reduce memory pressure</done>
</task>

</tasks>

<verification>
- TypeScript compiles for mobile project
- ErrorBoundary component exists and is wired into provider tree
- FlatList components have performance props
</verification>

<success_criteria>
- BUG-7: App shows error recovery screen instead of crashing on component errors
- BUG-6: Lists scroll smoothly with windowed rendering, no unnecessary re-renders
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-mobile-and-admin-bug-fixes/09.1-04-SUMMARY.md`
</output>
