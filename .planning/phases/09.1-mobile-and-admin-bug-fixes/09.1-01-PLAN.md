---
phase: 09.1-mobile-and-admin-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/customers/customers.controller.ts
  - apps/api/src/customers/customers.service.ts
  - apps/api/src/customers/dto/search-customer.dto.ts
  - apps/mobile/src/api/customers.api.ts
  - apps/api/src/rentals/rentals.controller.ts
  - apps/api/src/rentals/rentals.service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Employee can type a customer name in mobile search and see matching results"
    - "Employee can delete a rental that is in DRAFT status"
    - "Deleting a non-DRAFT rental is rejected with 400"
  artifacts:
    - path: "apps/api/src/customers/dto/search-customer.dto.ts"
      provides: "Search DTO with generic query param"
      contains: "query"
    - path: "apps/api/src/customers/customers.service.ts"
      provides: "Search method accepting generic query string"
      contains: "search"
    - path: "apps/mobile/src/api/customers.api.ts"
      provides: "Mobile API client sending lastName param"
      contains: "lastName"
    - path: "apps/api/src/rentals/rentals.controller.ts"
      provides: "DELETE endpoint for draft rentals"
      contains: "Delete"
    - path: "apps/api/src/rentals/rentals.service.ts"
      provides: "Delete method with DRAFT-only guard"
      contains: "delete"
  key_links:
    - from: "apps/mobile/src/api/customers.api.ts"
      to: "apps/api/src/customers/customers.controller.ts"
      via: "GET /customers/search?lastName=..."
      pattern: "lastName"
    - from: "apps/api/src/rentals/rentals.controller.ts"
      to: "apps/api/src/rentals/rentals.service.ts"
      via: "delete method call"
      pattern: "delete"
---

<objective>
Fix BUG-5 (customer search returns no results) and BUG-3 (cannot delete draft rentals).

Purpose: Customer search is broken because the mobile app sends `?query=X` but the API expects `?lastName=X`. Draft rentals cannot be deleted because no DELETE endpoint exists.
Output: Working customer search from mobile, working draft rental deletion endpoint.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix customer search API mismatch (BUG-5)</name>
  <files>apps/mobile/src/api/customers.api.ts, apps/api/src/customers/dto/search-customer.dto.ts, apps/api/src/customers/customers.service.ts</files>
  <read_first>
    - apps/mobile/src/api/customers.api.ts
    - apps/api/src/customers/dto/search-customer.dto.ts
    - apps/api/src/customers/customers.service.ts
    - apps/api/src/customers/customers.controller.ts
  </read_first>
  <action>
The mobile app sends `params: { query }` which results in `?query=kowalski` but the API SearchCustomerDto only accepts `lastName`, `phone`, `pesel`. The API throws "At least one search criterion required" because `query` is not a recognized field.

Fix approach: Change the mobile API client to send the search term as `lastName` since the API already does a case-insensitive contains search on that field. This is the minimal fix that keeps the API contract stable.

In `apps/mobile/src/api/customers.api.ts`:
- Change `searchCustomers` to send `params: { lastName: query }` instead of `params: { query }`

This makes the mobile search send `?lastName=kowalski` which the API already handles with `{ contains: dto.lastName, mode: 'insensitive' }`.

No API changes needed -- the API search endpoint already works correctly with `lastName` param.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/api/customers.api.ts contains `lastName: query` instead of `query`
    - No TypeScript compilation errors in mobile app
  </acceptance_criteria>
  <done>Mobile customer search sends lastName param that API recognizes, search returns matching customers</done>
</task>

<task type="auto">
  <name>Task 2: Add draft rental deletion endpoint (BUG-3)</name>
  <files>apps/api/src/rentals/rentals.service.ts, apps/api/src/rentals/rentals.controller.ts</files>
  <read_first>
    - apps/api/src/rentals/rentals.service.ts
    - apps/api/src/rentals/rentals.controller.ts
  </read_first>
  <action>
No DELETE endpoint exists for rentals. Add one that only allows deleting DRAFT-status rentals.

In `apps/api/src/rentals/rentals.service.ts`, add a `delete` method:
- Find rental by ID, throw NotFoundException if not found
- Check rental.status === 'DRAFT', throw BadRequestException("Only DRAFT rentals can be deleted") if not
- Delete associated contract signatures, contracts, and the rental in a transaction
- Use `tx.contractSignature.deleteMany({ where: { contract: { rentalId: id } } })` then `tx.contract.deleteMany({ where: { rentalId: id } })` then `tx.rental.delete({ where: { id } })`
- Return the deleted rental data

In `apps/api/src/rentals/rentals.controller.ts`:
- Add `import { Delete } from '@nestjs/common'` to existing imports
- Add `@Delete(':id')` endpoint with `@Roles(UserRole.ADMIN, UserRole.EMPLOYEE)`
- Accept `@Param('id', ParseUUIDPipe) id: string` and `@CurrentUser('id') userId: string`
- Call `this.rentalsService.delete(id)`
- Return result with `__audit` metadata: `{ action: 'rental.delete', entityType: 'Rental', entityId: id }`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/api/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/rentals/rentals.controller.ts contains "@Delete" decorator
    - apps/api/src/rentals/rentals.service.ts contains "async delete" method
    - Service method checks rental.status === 'DRAFT' before deleting
    - Transaction deletes contractSignature, contract, and rental
  </acceptance_criteria>
  <done>DELETE /rentals/:id endpoint exists, rejects non-DRAFT deletions with 400, successfully deletes DRAFT rentals in transaction</done>
</task>

</tasks>

<verification>
- TypeScript compiles for both API and mobile projects
- Customer search sends lastName param
- Delete endpoint exists with DRAFT guard
</verification>

<success_criteria>
- BUG-5: Mobile customer search sends ?lastName=X to API, which returns matching customers
- BUG-3: DELETE /rentals/:id endpoint allows deleting DRAFT rentals, rejects non-DRAFT with 400
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-mobile-and-admin-bug-fixes/09.1-01-SUMMARY.md`
</output>
