---
phase: 09.1-mobile-and-admin-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(admin)/uzytkownicy/page.tsx
  - apps/web/src/components/layout/sidebar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin can navigate to user management page from sidebar"
    - "Admin can create a new employee account with email, name, and role"
    - "New user receives setup password email after creation"
  artifacts:
    - path: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      provides: "User management page with create form"
      contains: "CreateUserForm"
    - path: "apps/web/src/components/layout/sidebar.tsx"
      provides: "Sidebar with user management nav item"
      contains: "uzytkownicy"
  key_links:
    - from: "apps/web/src/components/layout/sidebar.tsx"
      to: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      via: "navigation link"
      pattern: "uzytkownicy"
    - from: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      to: "apps/web/src/app/api/[...path]/route.ts"
      via: "POST /api/users BFF proxy"
      pattern: "users"
---

<objective>
Fix BUG-4: Admin cannot create new user accounts.

Purpose: No user management page exists in the admin panel. The API already supports POST /users (admin-only), but there's no UI to invoke it.
Output: A user management page at /uzytkownicy with a create-user form and sidebar navigation.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user management page and sidebar nav (BUG-4)</name>
  <files>apps/web/src/app/(admin)/uzytkownicy/page.tsx, apps/web/src/components/layout/sidebar.tsx</files>
  <read_first>
    - apps/web/src/components/layout/sidebar.tsx
    - apps/web/src/app/(admin)/layout.tsx
    - apps/web/src/app/(admin)/klienci/page.tsx
    - apps/api/src/users/dto/create-user.dto.ts
    - apps/api/src/users/users.controller.ts
    - apps/web/src/app/api/[...path]/route.ts
  </read_first>
  <action>
1. **Add sidebar nav item** in `apps/web/src/components/layout/sidebar.tsx`:
   - Import `UserPlus` from lucide-react (or `UserCog`)
   - Add `{ href: '/uzytkownicy', label: 'Uzytkownicy', icon: UserPlus }` to the `navItems` array, after the Audyt item

2. **Create user management page** at `apps/web/src/app/(admin)/uzytkownicy/page.tsx`:
   - 'use client' page
   - Page title: "Zarzadzanie uzytkownikami"
   - Create user form with fields: email (required), name (required, min 2 chars), role (select: ADMIN, EMPLOYEE)
   - Use shadcn/ui components: Card, Button, Input, Label, Select (matching existing admin panel patterns from klienci page)
   - Form validation with Zod: email (valid email), name (min 2 chars), role (enum ADMIN/EMPLOYEE)
   - Submit via `fetch('/api/users', { method: 'POST', body: JSON.stringify(formData) })` -- the existing BFF catch-all proxy at `/api/[...path]` will forward to the NestJS API
   - On success: show toast "Uzytkownik utworzony. Email z linkiem do ustawienia hasla zostal wyslany.", clear form
   - On error: show error toast with message from API
   - Use `sonner` toast (or whatever toast pattern existing admin pages use -- check klienci/page.tsx)

The API already handles: creating user with setup token, sending setup password email via MailService. The BFF proxy at `apps/web/src/app/api/[...path]/route.ts` already handles POST requests and forwards auth cookies. No API changes needed.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/(admin)/uzytkownicy/page.tsx exists with create user form
    - sidebar.tsx navItems array includes uzytkownicy entry
    - Form sends POST /api/users with email, name, role fields
    - Form has validation for email format, name min length, role enum
  </acceptance_criteria>
  <done>Admin can navigate to /uzytkownicy from sidebar and create new employee accounts via a form</done>
</task>

</tasks>

<verification>
- TypeScript compiles for web project
- /uzytkownicy page exists with create user form
- Sidebar shows "Uzytkownicy" nav item
</verification>

<success_criteria>
- BUG-4: Admin navigates to /uzytkownicy, fills in email + name + role, submits, and new user receives setup email
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-mobile-and-admin-bug-fixes/09.1-03-SUMMARY.md`
</output>
