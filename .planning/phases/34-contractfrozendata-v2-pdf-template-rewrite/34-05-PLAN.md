---
phase: 34-contractfrozendata-v2-pdf-template-rewrite
plan: 05
type: execute
wave: 3
depends_on: ["34-02", "34-03"]
files_modified:
  - apps/mobile/src/stores/rental-draft.store.ts
  - apps/mobile/app/(tabs)/new-rental/contract.tsx
  - apps/mobile/app/(tabs)/new-rental/signatures.tsx
  - apps/mobile/src/components/terms-webview.tsx
  - apps/mobile/src/components/second-driver-form.tsx
autonomous: false
requirements:
  - UMOWA-02
  - UMOWA-03
  - UMOWA-04
  - NAJEM-05
  - NAJEM-06
  - NAJEM-07

must_haves:
  truths:
    - "Worker can view and edit rental terms in mobile using TipTap WebView editor"
    - "Worker can add per-rental terms notes (plain text)"
    - "Customer must check terms acceptance checkbox before proceeding to signatures"
    - "Worker can add a second driver with full personal data in the contract step"
    - "Second driver is verified via CEPiK before contract signing"
    - "Second driver signs the contract (2 additional signature captures)"
    - "Terms and second driver data are sent when creating the contract"
  artifacts:
    - path: "apps/mobile/app/(tabs)/new-rental/contract.tsx"
      provides: "Contract step with terms display, acceptance checkbox, second driver form"
      contains: "termsAccepted"
    - path: "apps/mobile/app/(tabs)/new-rental/signatures.tsx"
      provides: "Signature flow with 4 or 6 signatures"
      contains: "second_customer"
    - path: "apps/mobile/src/components/terms-webview.tsx"
      provides: "WebView component for displaying/editing TipTap HTML terms"
      contains: "WebView"
    - path: "apps/mobile/src/components/second-driver-form.tsx"
      provides: "Form for second driver personal data entry"
      contains: "secondDriver"
    - path: "apps/mobile/src/stores/rental-draft.store.ts"
      provides: "Draft store with secondDriver, rentalTerms, termsNotes, termsAcceptedAt"
      contains: "secondDriver"
  key_links:
    - from: "apps/mobile/app/(tabs)/new-rental/contract.tsx"
      to: "apps/mobile/src/stores/rental-draft.store.ts"
      via: "Zustand store for draft state"
      pattern: "useRentalDraftStore"
    - from: "apps/mobile/app/(tabs)/new-rental/signatures.tsx"
      to: "/api/contracts"
      via: "Contract creation + signature upload"
      pattern: "contracts"
    - from: "apps/mobile/app/(tabs)/new-rental/contract.tsx"
      to: "/api/cepik/verify-driver"
      via: "CEPiK verification for second driver"
      pattern: "verify-driver"
    - from: "apps/mobile/app/(tabs)/new-rental/signatures.tsx"
      to: "/api/rentals/:id/terms"
      via: "PATCH rental terms before contract creation"
      pattern: "rentals.*terms"
---

<objective>
Implement the mobile UI for terms editing (TipTap in WebView), terms acceptance checkbox, second driver form with CEPiK verification, and extend the signature flow to support 6 signatures when a second driver is present.

Purpose: Workers can customize terms per rental, customers accept terms before signing, second drivers are captured and verified in the field.
Output: Updated contract step, signatures flow, new components for terms and second driver.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-contractfrozendata-v2-pdf-template-rewrite/34-CONTEXT.md
@.planning/phases/34-contractfrozendata-v2-pdf-template-rewrite/34-02-SUMMARY.md
@.planning/phases/34-contractfrozendata-v2-pdf-template-rewrite/34-03-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 02 and 03 -->
```
POST /rentals/:rentalId/driver -> CreateRentalDriverDto -> RentalDriverDto
GET /rentals/:rentalId/driver -> RentalDriverDto | 404
DELETE /rentals/:rentalId/driver -> 204
POST /cepik/verify-driver -> { driverId, rentalId } -> CepikVerification
POST /contracts -> CreateContractDto (now includes termsAcceptedAt)
POST /contracts/:id/sign -> SignContractDto (now accepts second_customer_page1/page2)
GET /settings/default_rental_terms -> { key, value: "<html>" }
PATCH /rentals/:id/terms -> UpdateRentalTermsDto { rentalTerms?, termsNotes? } -> Rental
```

<!-- Current mobile draft store (apps/mobile/src/stores/rental-draft.store.ts) -->
<!-- Zustand store with rental draft fields, used across wizard steps -->

<!-- Current contract step (apps/mobile/app/(tabs)/new-rental/contract.tsx -- 200 lines) -->
<!-- Shows contract preview, RODO consent, proceed to signatures -->

<!-- Current signatures step (apps/mobile/app/(tabs)/new-rental/signatures.tsx -- 357 lines) -->
<!-- 4-signature flow: customer page1, employee page1, customer page2, employee page2 -->
<!-- Creates contract at first signature, uploads each signature, triggers PDF at completion -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft store + terms WebView + second driver form components</name>
  <files>
    apps/mobile/src/stores/rental-draft.store.ts
    apps/mobile/src/components/terms-webview.tsx
    apps/mobile/src/components/second-driver-form.tsx
  </files>
  <read_first>
    - apps/mobile/src/stores/rental-draft.store.ts (full file)
    - apps/mobile/app/(tabs)/new-rental/contract.tsx (full file -- understand current flow)
    - apps/mobile/src/components/ (list to understand component patterns)
    - .planning/phases/34-contractfrozendata-v2-pdf-template-rewrite/34-RESEARCH.md (lines 309-312 for WebView approach)
  </read_first>
  <action>
**1. Extend rental draft store** (`rental-draft.store.ts`):
Add fields:
```typescript
rentalTerms: string | null;     // Custom terms HTML (null = use default)
termsNotes: string | null;      // Plain text notes
termsAcceptedAt: string | null; // ISO datetime when customer accepted
secondDriver: {
  firstName: string;
  lastName: string;
  pesel: string;
  idNumber: string;
  licenseNumber: string;
  licenseCategory: string;
  street: string;
  houseNumber: string;
  postalCode: string;
  city: string;
  phone: string;
} | null;
secondDriverId: string | null;       // API-created driver ID
secondDriverCepikStatus: string | null; // PENDING, PASSED, FAILED, etc.
```
Add actions: `setRentalTerms`, `setTermsNotes`, `setTermsAcceptedAt`, `setSecondDriver`, `setSecondDriverId`, `setSecondDriverCepikStatus`, `clearSecondDriver`.
Initialize new fields as null in the reset/initial state.

**2. Create TermsWebView component** (`terms-webview.tsx`):
- Uses `react-native-webview` (already installed per RESEARCH.md)
- Props: `html: string`, `onHtmlChange?: (html: string) => void`, `editable?: boolean`
- When `editable=false` (default): displays terms as read-only HTML for customer review
- When `editable=true`: loads TipTap editor in WebView for worker editing
- For read-only mode: simple HTML wrapper with styles matching PDF appearance
- For edit mode: load a minimal HTML page that imports TipTap from CDN (jsdelivr) with starter-kit. Use `postMessage` to:
  a. Inject initial HTML via `injectedJavaScript`
  b. Receive updated HTML on blur/change via `onMessage`
- Style: match the app's theme (font, colors)
- Height: auto-resize based on content (use `onMessage` to send content height)

**3. Create SecondDriverForm component** (`second-driver-form.tsx`):
- Props: `rentalId: string`, `onDriverCreated: (driverId: string) => void`, `onDriverRemoved: () => void`
- Full form with fields: firstName, lastName, pesel, idNumber, licenseNumber, licenseCategory, street, houseNumber, postalCode, city, phone
- Use same input styling as the customer form in the mobile wizard
- "Zapisz kierowce" button that POSTs to `/rentals/:rentalId/driver`
- After save, show "Sprawdz CEPiK" button that POSTs to `/cepik/verify-driver`
- Display CEPiK status: PASSED (green), FAILED (red), PENDING (yellow), ERROR (orange)
- "Usun kierowce" button to DELETE the driver
- All state managed via Zustand store (secondDriver, secondDriverId, secondDriverCepikStatus)
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "secondDriver" apps/mobile/src/stores/rental-draft.store.ts
    - grep -q "rentalTerms" apps/mobile/src/stores/rental-draft.store.ts
    - grep -q "termsAcceptedAt" apps/mobile/src/stores/rental-draft.store.ts
    - grep -q "termsNotes" apps/mobile/src/stores/rental-draft.store.ts
    - grep -q "WebView" apps/mobile/src/components/terms-webview.tsx
    - grep -q "postMessage\|onMessage" apps/mobile/src/components/terms-webview.tsx
    - test -f apps/mobile/src/components/second-driver-form.tsx
    - grep -q "verify-driver\|cepik" apps/mobile/src/components/second-driver-form.tsx
  </acceptance_criteria>
  <done>Draft store has all new fields. TermsWebView displays/edits HTML terms. SecondDriverForm captures all personal data, saves via API, triggers CEPiK verification.</done>
</task>

<task type="auto">
  <name>Task 2: Contract step -- terms display, acceptance, second driver + Signatures flow -- 6 signatures</name>
  <files>
    apps/mobile/app/(tabs)/new-rental/contract.tsx
    apps/mobile/app/(tabs)/new-rental/signatures.tsx
  </files>
  <read_first>
    - apps/mobile/app/(tabs)/new-rental/contract.tsx (full file -- 200 lines)
    - apps/mobile/app/(tabs)/new-rental/signatures.tsx (full file -- 357 lines)
    - apps/mobile/src/stores/rental-draft.store.ts (updated from task 1)
    - apps/mobile/src/components/terms-webview.tsx (from task 1)
    - apps/mobile/src/components/second-driver-form.tsx (from task 1)
  </read_first>
  <action>
**1. Update contract.tsx:**

Add to the contract step (before the "Dalej" / proceed button):

a. **Terms section:**
- Fetch default terms on mount: `GET /settings/default_rental_terms`
- If `rentalTerms` in draft is null, use the fetched default
- Show TermsWebView in read-only mode by default
- "Edytuj warunki" button opens TermsWebView in editable mode (modal or expanded view)
- Show termsNotes input (TextInput, multiline, placeholder "Uwagi dodatkowe do warunkow...")
- Store changes in draft store

b. **Terms acceptance:**
- After terms display, show checkbox: "Zapoznalem sie z warunkami najmu i akceptuje je"
- When checked, record `termsAcceptedAt` as current ISO datetime in draft store
- When unchecked, clear `termsAcceptedAt`
- The checkbox must be checked to enable the "Dalej" (proceed to signatures) button

c. **Second driver section:**
- "Dodaj drugiego kierowce" button (if no second driver set)
- When tapped, show SecondDriverForm component
- If second driver already added, show summary card with name and CEPiK status
- Allow removing second driver

d. **Flow order on screen:**
1. Contract preview (existing)
2. Terms display (new)
3. Terms notes input (new)
4. Terms acceptance checkbox (new)
5. Second driver section (new)
6. RODO consent (existing)
7. "Dalej do podpisow" button (existing, now gated on terms acceptance)

**2. Update signatures.tsx:**

Current flow: 2 steps (customer signs page1+page2, employee signs page1+page2).
New flow: 2 or 3 steps depending on second driver.

a. **Determine signature steps dynamically:**
```typescript
const hasSecondDriver = draft.secondDriverId !== null;
const steps = hasSecondDriver
  ? ['Podpis Klienta', 'Podpis Drugiego Kierowcy', 'Podpis Pracownika']
  : ['Podpis Klienta', 'Podpis Pracownika'];
```

b. **Second driver signature step:**
- Same UI as customer signature (signature pad canvas)
- Captures one signature image
- Uploads as both `second_customer_page1` and `second_customer_page2` types
- Same pattern as customer: one capture, two uploads

c. **Contract creation (at first signature):**
- Include `termsAcceptedAt` from draft store in CreateContractDto
- Before contract creation, if worker customized terms or added notes, call `PATCH /rentals/:id/terms` with `{ rentalTerms, termsNotes }` to persist them on the rental. The contract service reads these from the rental when building frozen data.

d. **Completion logic:**
- After all signatures uploaded, the API side handles PDF generation (plan 02 made this dynamic based on required signature count)
- No change needed on mobile for completion detection -- just ensure all required signatures are uploaded

IMPORTANT: Before contract creation, if the worker customized terms, call `PATCH /rentals/${rentalId}/terms` to update `rentalTerms` and `termsNotes` fields. The contract service reads these from the rental when building frozen data. This endpoint was created in Plan 03.

IMPORTANT: Make sure the "Dalej do podpisow" button is DISABLED when `termsAcceptedAt` is null. The customer MUST accept terms before signing.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "termsAccepted\|termsAcceptedAt" apps/mobile/app/\(tabs\)/new-rental/contract.tsx
    - grep -q "TermsWebView\|terms-webview" apps/mobile/app/\(tabs\)/new-rental/contract.tsx
    - grep -q "SecondDriverForm\|second-driver-form" apps/mobile/app/\(tabs\)/new-rental/contract.tsx
    - grep -q "Zapoznalem\|zapoznalem\|checkbox\|Checkbox" apps/mobile/app/\(tabs\)/new-rental/contract.tsx
    - grep -q "second_customer\|secondCustomer\|Drugiego Kierowcy" apps/mobile/app/\(tabs\)/new-rental/signatures.tsx
    - grep -q "termsAcceptedAt" apps/mobile/app/\(tabs\)/new-rental/signatures.tsx
    - grep -q "rentals.*terms\|/terms" apps/mobile/app/\(tabs\)/new-rental/signatures.tsx
  </acceptance_criteria>
  <done>Contract step shows terms (read-only or editable), terms notes, acceptance checkbox (blocks signatures until checked), second driver form with CEPiK. Signatures flow supports 4 or 6 signatures. Terms and second driver data included in contract creation. Rental terms PATCHed before contract creation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete contract flow with terms + second driver</name>
  <files>n/a -- human verification checkpoint</files>
  <action>
Human verification of the complete end-to-end contract flow across all Phase 34 plans. All code changes were made in Tasks 1-2 above and Plans 01-04. This checkpoint verifies the integrated result including: editable terms (TipTap WebView), terms acceptance checkbox, second driver form with CEPiK, 6-signature flow, PDF generation with all new sections, web admin TipTap editor, and portal VIN/year hiding.
  </action>
  <verify>
Manual verification steps:
1. Open mobile app, start new rental wizard
2. At contract step, verify terms are displayed (default from settings)
3. Try to proceed to signatures -- should be blocked until checkbox is checked
4. Tap "Edytuj warunki" -- terms editor should open in WebView
5. Add some notes in the "Uwagi dodatkowe" field
6. Check the acceptance checkbox -- "Dalej" should enable
7. Tap "Dodaj drugiego kierowce" -- form should appear
8. Fill second driver data and save
9. Tap "Sprawdz CEPiK" for second driver
10. Proceed to signatures -- should show 3 steps (customer, second driver, employee)
11. Complete all signatures
12. Download generated PDF -- verify: company data shown (if company rental), no VIN/year, dynamic terms, terms notes, second driver section, all 6 signatures
13. Check admin web panel: /ustawienia page loads with TipTap editor, edit and save terms
14. Check customer portal: verify VIN and year are NOT visible
  </verify>
  <done>User confirms all 14 verification steps pass. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
- Mobile TypeScript compiles: `npx tsc --noEmit -p apps/mobile/tsconfig.json`
- Contract step: terms display, acceptance checkbox, second driver form all visible
- Signatures: 3 steps when second driver present, 2 without
- PDF output: company section, no VIN/year, dynamic terms, terms notes, second driver, 6 signatures
- Web admin: /ustawienia page with TipTap editor works
- Portal: VIN/year hidden
</verification>

<success_criteria>
- Worker can edit terms per rental in mobile (TipTap WebView)
- Worker can add terms notes (plain text)
- Customer acceptance checkbox blocks signature flow until checked
- Worker can add second driver with full data
- Second driver verified via CEPiK
- Signature flow: 3 steps with second driver (customer, second driver, employee)
- Contract creation includes termsAcceptedAt, terms, and second driver
- Rental terms PATCHed via /rentals/:id/terms before contract creation
- Human verification confirms end-to-end flow
</success_criteria>

<output>
After completion, create `.planning/phases/34-contractfrozendata-v2-pdf-template-rewrite/34-05-SUMMARY.md`
</output>
