---
phase: 22-api-validation-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/rentals/rentals.controller.ts
  - apps/api/src/rentals/rentals.service.ts
  - apps/api/src/rentals/dto/rentals-query.dto.ts
  - apps/api/src/customers/customers.controller.ts
  - apps/api/src/customers/customers.service.ts
  - apps/api/src/customers/dto/customers-query.dto.ts
  - apps/api/src/contracts/contracts.controller.ts
  - apps/api/src/contracts/contracts.service.ts
  - apps/api/src/contracts/dto/contracts-query.dto.ts
  - apps/api/src/portal/portal.controller.ts
  - apps/api/src/users/users.controller.ts
autonomous: true
requirements: [AVAL-01, AVAL-02]

must_haves:
  truths:
    - "GET /rentals returns paginated response with total, page, limit fields and default page size of 20"
    - "GET /customers returns paginated response with total, page, limit fields"
    - "GET /contracts returns paginated response with total, page, limit fields"
    - "Passing non-UUID string as :id to portal rental, users PATCH, or users reset-password returns 400"
  artifacts:
    - path: "apps/api/src/rentals/dto/rentals-query.dto.ts"
      provides: "Pagination + filter DTO for rentals"
      contains: "page"
    - path: "apps/api/src/customers/dto/customers-query.dto.ts"
      provides: "Pagination DTO for customers"
      contains: "page"
    - path: "apps/api/src/contracts/dto/contracts-query.dto.ts"
      provides: "Pagination DTO for contracts"
      contains: "page"
  key_links:
    - from: "apps/api/src/rentals/rentals.controller.ts"
      to: "apps/api/src/rentals/rentals.service.ts"
      via: "findAll accepts pagination params"
      pattern: "page.*limit\|take.*skip"
    - from: "apps/api/src/portal/portal.controller.ts"
      to: "@nestjs/common"
      via: "ParseUUIDPipe on :id param"
      pattern: "ParseUUIDPipe"
---

<objective>
Add server-side pagination to GET /rentals, GET /customers, and GET /contracts endpoints following the existing notification pagination pattern (page/limit with total count). Add ParseUUIDPipe to 3 endpoints missing UUID validation on :id params.

Purpose: Prevent unbounded query results and ensure invalid UUIDs return 400 instead of 500.
Output: New pagination query DTOs, updated services with take/skip, updated controllers with ParseUUIDPipe.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing pagination pattern to follow (from notifications) -->

From apps/api/src/notifications/dto/notification-query.dto.ts:
```typescript
export class NotificationQueryDto {
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(100)
  limit?: number = 20;
}
```

From apps/api/src/notifications/notifications.service.ts (pagination implementation pattern):
```typescript
const { page = 1, limit = 20 } = query;
const [data, total] = await Promise.all([
  this.prisma.inAppNotification.findMany({
    where,
    take: limit,
    skip: (page - 1) * limit,
    orderBy: { createdAt: 'desc' },
  }),
  this.prisma.inAppNotification.count({ where }),
]);
return { data, total, page, limit };
```

From apps/api/src/rentals/rentals.service.ts:
```typescript
async findAll(status?: RentalStatus, customerId?: string, vehicleId?: string): Promise<RentalWithRelations[]> {
```

From apps/api/src/customers/customers.service.ts:
```typescript
async findAll(includeArchived = false): Promise<CustomerDto[]> {
```

From apps/api/src/contracts/contracts.service.ts:
```typescript
async findAll(): Promise<ContractDto[]> {
```

Endpoints missing ParseUUIDPipe:
```typescript
// portal.controller.ts line 31
@Param('id') id: string  // needs ParseUUIDPipe

// users.controller.ts line 36
@Param('id') id: string  // PATCH update - needs ParseUUIDPipe

// users.controller.ts line 44
@Param('id') id: string  // POST reset-password - needs ParseUUIDPipe
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-side pagination to rentals, customers, and contracts</name>
  <files>
    apps/api/src/rentals/dto/rentals-query.dto.ts,
    apps/api/src/rentals/rentals.controller.ts,
    apps/api/src/rentals/rentals.service.ts,
    apps/api/src/customers/dto/customers-query.dto.ts,
    apps/api/src/customers/customers.controller.ts,
    apps/api/src/customers/customers.service.ts,
    apps/api/src/contracts/dto/contracts-query.dto.ts,
    apps/api/src/contracts/contracts.controller.ts,
    apps/api/src/contracts/contracts.service.ts
  </files>
  <read_first>
    apps/api/src/rentals/rentals.controller.ts,
    apps/api/src/rentals/rentals.service.ts,
    apps/api/src/customers/customers.controller.ts,
    apps/api/src/customers/customers.service.ts,
    apps/api/src/contracts/contracts.controller.ts,
    apps/api/src/contracts/contracts.service.ts,
    apps/api/src/notifications/dto/notification-query.dto.ts,
    apps/api/src/notifications/notifications.service.ts
  </read_first>
  <action>
    Follow the exact pagination pattern from NotificationQueryDto and notifications.service.ts.

    1. **Create RentalsQueryDto** (`apps/api/src/rentals/dto/rentals-query.dto.ts`):
       - `page?: number = 1` with `@IsOptional()`, `@Type(() => Number)`, `@IsInt()`, `@Min(1)`
       - `limit?: number = 20` with `@IsOptional()`, `@Type(() => Number)`, `@IsInt()`, `@Min(1)`, `@Max(100)`
       - `status?: RentalStatus` with `@IsOptional()`, `@IsEnum(RentalStatus)` (move from controller query params)
       - `customerId?: string` with `@IsOptional()`, `@IsUUID()`
       - `vehicleId?: string` with `@IsOptional()`, `@IsUUID()`

    2. **Update rentals.service.ts `findAll`**: Change signature to accept `RentalsQueryDto`. Extract `page` and `limit`, build `where` clause from status/customerId/vehicleId filters. Use `Promise.all([findMany({where, take: limit, skip: (page-1)*limit, ...includes}), count({where})])`. Return `{ data, total, page, limit }`.

    3. **Update rentals.controller.ts**: Change the `@Get()` handler to accept `@Query() query: RentalsQueryDto` instead of individual `@Query()` params. Pass `query` to `findAll`.

    4. **Create CustomersQueryDto** (`apps/api/src/customers/dto/customers-query.dto.ts`):
       - `page?: number = 1`, `limit?: number = 20` (same pattern)
       - `includeArchived?: boolean` with `@IsOptional()`, `@Type(() => Boolean)`

    5. **Update customers.service.ts `findAll`**: Accept `CustomersQueryDto`. Apply pagination with `take`/`skip`. Return `{ data, total, page, limit }`. Keep existing `includeArchived` filter logic.

    6. **Update customers.controller.ts**: Use `@Query() query: CustomersQueryDto`.

    7. **Create ContractsQueryDto** (`apps/api/src/contracts/dto/contracts-query.dto.ts`):
       - `page?: number = 1`, `limit?: number = 20` (same pattern)

    8. **Update contracts.service.ts `findAll`**: Accept `ContractsQueryDto`. Apply pagination. Return `{ data, total, page, limit }`.

    9. **Update contracts.controller.ts**: Use `@Query() query: ContractsQueryDto`.

    **IMPORTANT**: The return type changes from `Promise<SomeDto[]>` to `Promise<{ data: SomeDto[], total: number, page: number, limit: number }>`. Update all callers within the API if any service calls `findAll` internally.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -l "page.*limit\|take.*skip" apps/api/src/rentals/rentals.service.ts apps/api/src/customers/customers.service.ts apps/api/src/contracts/contracts.service.ts && ls apps/api/src/rentals/dto/rentals-query.dto.ts apps/api/src/customers/dto/customers-query.dto.ts apps/api/src/contracts/dto/contracts-query.dto.ts</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/api/src/rentals/dto/rentals-query.dto.ts (pagination DTO exists)
    - test -f apps/api/src/customers/dto/customers-query.dto.ts (pagination DTO exists)
    - test -f apps/api/src/contracts/dto/contracts-query.dto.ts (pagination DTO exists)
    - grep -q "take.*limit\|skip.*(page" apps/api/src/rentals/rentals.service.ts (pagination applied)
    - grep -q "take.*limit\|skip.*(page" apps/api/src/customers/customers.service.ts (pagination applied)
    - grep -q "take.*limit\|skip.*(page" apps/api/src/contracts/contracts.service.ts (pagination applied)
    - grep -q "total.*page.*limit\|data.*total" apps/api/src/rentals/rentals.service.ts (paginated response shape)
  </acceptance_criteria>
  <done>GET /rentals, GET /customers, GET /contracts all accept page/limit query params with defaults (page=1, limit=20) and return { data, total, page, limit } responses</done>
</task>

<task type="auto">
  <name>Task 2: Add ParseUUIDPipe to unvalidated :id params</name>
  <files>
    apps/api/src/portal/portal.controller.ts,
    apps/api/src/users/users.controller.ts
  </files>
  <read_first>
    apps/api/src/portal/portal.controller.ts,
    apps/api/src/users/users.controller.ts
  </read_first>
  <action>
    1. **portal.controller.ts**: On the `getRentalDetail` method, change `@Param('id') id: string` to `@Param('id', ParseUUIDPipe) id: string`. Add `ParseUUIDPipe` to the imports from `@nestjs/common`.

    2. **users.controller.ts**: On both `update` (PATCH :id) and `resetPassword` (POST :id/reset-password) methods, change `@Param('id') id: string` to `@Param('id', ParseUUIDPipe) id: string`. Add `ParseUUIDPipe` to the imports from `@nestjs/common` if not already imported.

    This ensures non-UUID strings are rejected with 400 before hitting the service/database layer.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -n "ParseUUIDPipe" apps/api/src/portal/portal.controller.ts apps/api/src/users/users.controller.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ParseUUIDPipe" apps/api/src/portal/portal.controller.ts (portal rental ID validated)
    - grep -c "ParseUUIDPipe" apps/api/src/users/users.controller.ts returns 2 or more (both PATCH and POST endpoints)
  </acceptance_criteria>
  <done>All 3 endpoints (portal GET rental/:id, users PATCH :id, users POST :id/reset-password) validate :id as UUID via ParseUUIDPipe -- non-UUID strings return 400</done>
</task>

</tasks>

<verification>
- New pagination DTOs compile: `cd apps/api && npx tsc --noEmit 2>&1 | tail -10`
- Existing tests still pass: `cd apps/api && npx jest --passWithNoTests 2>&1 | tail -5`
</verification>

<success_criteria>
- GET /rentals?page=1&limit=10 returns { data: [...], total: N, page: 1, limit: 10 }
- GET /customers?page=2&limit=5 returns paginated response
- GET /contracts returns paginated response with defaults
- GET /portal/rentals/not-a-uuid returns 400
- PATCH /users/not-a-uuid returns 400
- POST /users/not-a-uuid/reset-password returns 400
</success_criteria>

<output>
After completion, create `.planning/phases/22-api-validation-performance/22-02-SUMMARY.md`
</output>
