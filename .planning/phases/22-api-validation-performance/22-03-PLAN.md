---
phase: 22-api-validation-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/vehicles/vehicles.service.ts
  - apps/api/src/photos/photos.service.ts
  - apps/api/src/notifications/notifications.service.ts
autonomous: true
requirements: [APERF-01, APERF-02, APERF-03]

must_haves:
  truths:
    - "importFleet pre-fetches all registrations in one query before the loop"
    - "getComparison generates all presigned URLs in parallel via Promise.all"
    - "enqueueExpiryAlert uses createMany for batch notification inserts"
  artifacts:
    - path: "apps/api/src/vehicles/vehicles.service.ts"
      provides: "Bulk registration pre-fetch in importFleet"
      contains: "findMany"
    - path: "apps/api/src/photos/photos.service.ts"
      provides: "Parallel presigned URL generation"
      contains: "Promise.all"
    - path: "apps/api/src/notifications/notifications.service.ts"
      provides: "Batch notification creation"
      contains: "createMany"
  key_links:
    - from: "apps/api/src/vehicles/vehicles.service.ts"
      to: "prisma.vehicle.findMany"
      via: "bulk pre-fetch before import loop"
      pattern: "findMany.*registration.*in"
---

<objective>
Fix 3 N+1 query patterns identified in the API audit: importFleet per-row DB lookups, getComparison sequential presigned URL calls, and enqueueExpiryAlert per-admin per-vehicle individual inserts.

Purpose: Reduce DB round-trips from O(N) to O(1) for fleet imports, parallelize S3 calls for photo comparison, and batch notification inserts.
Output: Optimized service methods with bulk operations.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From apps/api/src/vehicles/vehicles.service.ts (importFleet N+1 pattern):
```typescript
// Current: per-row DB query inside loop
for (let i = 0; i < rows.length; i++) {
  const registration = String(normalized.registration);
  const existing = await this.prisma.vehicle.findUnique({  // N+1!
    where: { registration },
  });
```

From apps/api/src/photos/photos.service.ts:
```typescript
async getComparison(rentalId: string): Promise<PhotoComparisonPair[]> {
// For each position: 4 sequential presign calls (handover photo, handover thumb, return photo, return thumb)
```

From apps/api/src/notifications/notifications.service.ts:
```typescript
async enqueueExpiryAlert(
// Nested loop: for each vehicle x for each admin => individual notification.create + inAppNotification.create
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix importFleet N+1 with bulk registration pre-fetch</name>
  <files>apps/api/src/vehicles/vehicles.service.ts</files>
  <read_first>apps/api/src/vehicles/vehicles.service.ts</read_first>
  <action>
    In `importFleet` method:

    1. After parsing CSV rows and before the main loop, extract all registration values from the parsed rows into an array.
    2. Execute a single `this.prisma.vehicle.findMany({ where: { registration: { in: registrations } }, select: { registration: true } })` query.
    3. Build a `Set<string>` from the results: `const existingRegistrations = new Set(existing.map(v => v.registration))`.
    4. Inside the loop, replace `await this.prisma.vehicle.findUnique({ where: { registration } })` with `existingRegistrations.has(registration)`.
    5. The rest of the loop logic (skip if exists, create if new) remains the same.

    This changes the query pattern from O(N) individual findUnique calls to 1 findMany + O(1) Set lookups.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -A3 "existingRegistrations\|findMany.*registration.*in\|\.has(registration)" apps/api/src/vehicles/vehicles.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "findMany" apps/api/src/vehicles/vehicles.service.ts (bulk query present)
    - grep -q "Set\|\.has(" apps/api/src/vehicles/vehicles.service.ts (Set-based lookup)
    - ! grep -q "findUnique.*registration" apps/api/src/vehicles/vehicles.service.ts (per-row query removed from import loop)
  </acceptance_criteria>
  <done>importFleet executes 1 findMany query before the loop instead of N findUnique queries inside the loop</done>
</task>

<task type="auto">
  <name>Task 2: Parallelize photo comparison presigned URLs and batch expiry notifications</name>
  <files>
    apps/api/src/photos/photos.service.ts,
    apps/api/src/notifications/notifications.service.ts
  </files>
  <read_first>
    apps/api/src/photos/photos.service.ts,
    apps/api/src/notifications/notifications.service.ts
  </read_first>
  <action>
    1. **photos.service.ts getComparison** (APERF-02):
       - Find the section where presigned URLs are generated for each photo position.
       - Currently, each position generates presigned URLs sequentially (4 calls per position: handover photo, handover thumbnail, return photo, return thumbnail).
       - Collect all presign calls into a single array and execute with `Promise.all()`.
       - Map the results back to the correct positions after resolution.
       - If the current pattern is a `for` loop with `await` per URL, refactor to:
         ```typescript
         const urlPromises = positions.map(pos => ({
           handoverUrl: this.storage.getPresignedUrl(pos.handover?.key),
           // etc.
         }));
         ```
         Then resolve all at once.

    2. **notifications.service.ts enqueueExpiryAlert** (APERF-03):
       - In the `enqueueExpiryAlert` method, find the nested loop that creates individual `notification` and `inAppNotification` records per admin per vehicle.
       - Collect all notification data into arrays, then use `this.prisma.notification.createMany({ data: notificationData })` for the notification batch.
       - For `inAppNotification` records (which need the parent notification ID), use a transaction: create all notifications first via createMany, then create all inAppNotifications via createMany.
       - If createMany cannot return IDs (Prisma limitation), use `this.prisma.$transaction()` with individual creates wrapped in Promise.all for the inAppNotifications that need a reference to the notification.
       - Alternative simpler approach: keep the notification.create calls but wrap them in `Promise.all` to at least parallelize them, and batch the inAppNotification.create calls with createMany where possible.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -c "Promise.all" apps/api/src/photos/photos.service.ts && grep -c "createMany\|Promise.all" apps/api/src/notifications/notifications.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Promise.all" apps/api/src/photos/photos.service.ts (parallel presigned URL generation)
    - grep -q "createMany\|Promise.all" apps/api/src/notifications/notifications.service.ts (batch or parallel notification creation in enqueueExpiryAlert)
  </acceptance_criteria>
  <done>Photo comparison presigned URLs generated in parallel via Promise.all; expiry alert notifications created in batch (createMany or parallelized) instead of sequential individual inserts</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd apps/api && npx tsc --noEmit 2>&1 | tail -10`
- Existing tests pass: `cd apps/api && npx jest --passWithNoTests 2>&1 | tail -5`
</verification>

<success_criteria>
- importFleet with 50 vehicles executes a constant number of DB queries (1 findMany + N creates), not 50+ findUnique + N creates
- getComparison with 10 positions generates all 40 presigned URLs in parallel, not sequentially
- enqueueExpiryAlert with 5 admins and 10 vehicles uses batch operations instead of 100 individual inserts
</success_criteria>

<output>
After completion, create `.planning/phases/22-api-validation-performance/22-03-SUMMARY.md`
</output>
