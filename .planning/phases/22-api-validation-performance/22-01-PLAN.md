---
phase: 22-api-validation-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/rentals/dto/calendar-query.dto.ts
  - apps/api/src/rentals/dto/create-rental.dto.ts
  - apps/api/src/vehicles/dto/create-vehicle.dto.ts
  - apps/api/src/photos/dto/upload-photo.dto.ts
  - apps/api/src/notifications/dto/notification-query.dto.ts
  - apps/api/src/notifications/notifications.controller.ts
autonomous: true
requirements: [AVAL-03, AVAL-04, AVAL-05, AVAL-06, AVAL-07, AVAL-08]

must_haves:
  truths:
    - "CalendarQueryDto rejects from > to and ranges exceeding 6 months"
    - "CreateRentalDto rejects startDate >= endDate"
    - "CreateVehicleDto make and model have @MaxLength(100)"
    - "UploadPhotoDto position has @MaxLength(50)"
    - "NotificationQueryDto.isRead only accepts 'true' or 'false'"
    - "markAllAsRead route is defined before :id/read in the controller"
  artifacts:
    - path: "apps/api/src/rentals/dto/calendar-query.dto.ts"
      provides: "Cross-field from < to validation + 6-month max range"
      contains: "Validate"
    - path: "apps/api/src/rentals/dto/create-rental.dto.ts"
      provides: "Cross-field startDate < endDate validation"
      contains: "Validate"
    - path: "apps/api/src/notifications/notifications.controller.ts"
      provides: "Correct route order for read-all before :id/read"
      contains: "markAllAsRead"
  key_links:
    - from: "apps/api/src/rentals/dto/calendar-query.dto.ts"
      to: "apps/api/src/rentals/rentals.controller.ts"
      via: "NestJS validation pipe"
      pattern: "CalendarQueryDto"
---

<objective>
Close 6 DTO validation gaps identified in the API audit: cross-field date validation on CalendarQueryDto and CreateRentalDto, @MaxLength on CreateVehicleDto fields, @MaxLength on UploadPhotoDto.position, @IsIn on NotificationQueryDto.isRead, and notification route order fix.

Purpose: Prevent invalid data from reaching service logic -- bad date ranges, unbounded strings, and route mismatches.
Output: Updated DTOs with proper constraints and corrected controller route order.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/API-AUDIT.md

<interfaces>
<!-- Existing DTO patterns from the codebase -->

From apps/api/src/notifications/dto/notification-query.dto.ts:
```typescript
import { IsOptional, IsInt, Min, Max } from 'class-validator';
import { Type } from 'class-transformer';

export class NotificationQueryDto {
  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  page?: number = 1;

  @IsOptional()
  @Type(() => Number)
  @IsInt()
  @Min(1)
  @Max(100)
  limit?: number = 20;

  @IsOptional()
  isRead?: string; // 'true' | 'false'
}
```

From apps/api/src/notifications/notifications.controller.ts (current route order):
```typescript
@Patch('in-app/:id/read')   // line 30 - parameterized BEFORE static
async markAsRead(...)

@Patch('in-app/read-all')    // line 39 - static AFTER parameterized (WRONG)
async markAllAsRead(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-field date validations and @MaxLength constraints to DTOs</name>
  <files>
    apps/api/src/rentals/dto/calendar-query.dto.ts,
    apps/api/src/rentals/dto/create-rental.dto.ts,
    apps/api/src/vehicles/dto/create-vehicle.dto.ts,
    apps/api/src/photos/dto/upload-photo.dto.ts
  </files>
  <read_first>
    apps/api/src/rentals/dto/calendar-query.dto.ts,
    apps/api/src/rentals/dto/create-rental.dto.ts,
    apps/api/src/vehicles/dto/create-vehicle.dto.ts,
    apps/api/src/photos/dto/upload-photo.dto.ts
  </read_first>
  <action>
    1. **CalendarQueryDto** (AVAL-03): Add a class-level custom validator using `@ValidatorConstraint` and `@Validate` at the class level (or use class-validator's `@ValidateIf` pattern). Implement:
       - `from` must be before `to` (compare ISO string dates)
       - Max range of 6 months (184 days) between from and to
       - Add a custom validator class `CalendarRangeValidator` that implements `ValidatorConstraintInterface`
       - Register it with `@Validate(CalendarRangeValidator)` on `to` field, using `object.from` for comparison
       - Error messages: "from must be before to" and "Date range must not exceed 6 months"

    2. **CreateRentalDto** (AVAL-04): Add cross-field validation on `endDate`:
       - Add `@Validate(DateAfterValidator, ['startDate'])` custom validator on `endDate`
       - Create a reusable `DateAfterValidator` in `apps/api/src/common/validators/date-after.validator.ts`
       - The validator checks that the decorated property's ISO date is strictly after the related property
       - Error message: "endDate must be after startDate"

    3. **CreateVehicleDto** (AVAL-05): Add `@MaxLength(100)` to `make` and `model` fields. These fields already have `@IsString()`.

    4. **UploadPhotoDto** (AVAL-06): Add `@MaxLength(50)` to `position` field. The field already has `@IsString()` but no length limit.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx ts-node -e "
      const { CalendarQueryDto } = require('./apps/api/src/rentals/dto/calendar-query.dto');
      const { CreateVehicleDto } = require('./apps/api/src/vehicles/dto/create-vehicle.dto');
      console.log('DTOs loaded successfully');
    " 2>&1 || echo "Checking with grep..." && grep -l "MaxLength\|Validate" apps/api/src/rentals/dto/calendar-query.dto.ts apps/api/src/rentals/dto/create-rental.dto.ts apps/api/src/vehicles/dto/create-vehicle.dto.ts apps/api/src/photos/dto/upload-photo.dto.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "6.*month\|184\|MAX_RANGE" apps/api/src/rentals/dto/calendar-query.dto.ts (6-month range check exists)
    - grep -q "Validate\|ValidatorConstraint" apps/api/src/rentals/dto/calendar-query.dto.ts (cross-field validator present)
    - grep -q "Validate\|ValidatorConstraint\|endDate.*after\|startDate" apps/api/src/rentals/dto/create-rental.dto.ts (date ordering validator)
    - grep -q "MaxLength(100)" apps/api/src/vehicles/dto/create-vehicle.dto.ts (make/model length bounded)
    - grep -q "MaxLength(50)" apps/api/src/photos/dto/upload-photo.dto.ts (position length bounded)
  </acceptance_criteria>
  <done>All 4 DTOs have proper validation constraints: CalendarQueryDto rejects from >= to and > 6-month ranges, CreateRentalDto rejects startDate >= endDate, CreateVehicleDto make/model have MaxLength(100), UploadPhotoDto position has MaxLength(50)</done>
</task>

<task type="auto">
  <name>Task 2: Fix NotificationQueryDto.isRead validation and controller route order</name>
  <files>
    apps/api/src/notifications/dto/notification-query.dto.ts,
    apps/api/src/notifications/notifications.controller.ts
  </files>
  <read_first>
    apps/api/src/notifications/dto/notification-query.dto.ts,
    apps/api/src/notifications/notifications.controller.ts
  </read_first>
  <action>
    1. **NotificationQueryDto.isRead** (AVAL-07): Add `@IsIn(['true', 'false'])` decorator to `isRead` field. Keep it as `string` type since the service already compares it as string (`query.isRead === 'true'`). Import `IsIn` from `class-validator`.

    2. **Notification route order** (AVAL-08): In `notifications.controller.ts`, move the `markAllAsRead` method (currently at line 39, `@Patch('in-app/read-all')`) ABOVE the `markAsRead` method (currently at line 30, `@Patch('in-app/:id/read')`). NestJS resolves routes in registration order, so the static `read-all` path must come before the parameterized `:id/read` to prevent `read-all` being matched as an `:id` value. Preserve all decorators, parameter decorators, and method bodies exactly.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -n "IsIn\|isRead" apps/api/src/notifications/dto/notification-query.dto.ts && echo "---" && grep -n "read-all\|:id/read\|markAllAsRead\|markAsRead" apps/api/src/notifications/notifications.controller.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "IsIn.*true.*false" apps/api/src/notifications/dto/notification-query.dto.ts (isRead validates against 'true'|'false')
    - Line number of "read-all" is less than line number of ":id/read" in notifications.controller.ts (static route first)
  </acceptance_criteria>
  <done>NotificationQueryDto.isRead validates with @IsIn(['true', 'false']), and markAllAsRead route is declared before markAsRead in the controller file</done>
</task>

</tasks>

<verification>
- All 6 AVAL requirements (03-08) addressed in DTO files and controller
- No runtime test failures: `cd apps/api && npx jest --passWithNoTests 2>&1 | tail -5`
</verification>

<success_criteria>
- CalendarQueryDto rejects { from: '2025-06-01', to: '2025-01-01' } (reversed dates)
- CalendarQueryDto rejects { from: '2025-01-01', to: '2025-12-01' } (> 6 months)
- CreateRentalDto rejects startDate >= endDate
- CreateVehicleDto make/model capped at 100 chars
- UploadPhotoDto.position capped at 50 chars
- NotificationQueryDto.isRead rejects arbitrary strings
- markAllAsRead is above markAsRead in notifications controller
</success_criteria>

<output>
After completion, create `.planning/phases/22-api-validation-performance/22-01-SUMMARY.md`
</output>
