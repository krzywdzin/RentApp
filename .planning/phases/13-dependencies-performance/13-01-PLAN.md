---
phase: 13-dependencies-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03

must_haves:
  truths:
    - "react-native-webview is an explicit dependency in mobile package.json"
    - "expo-router uses tilde range (~) instead of exact pin"
    - "npx expo install --check reports no critical version mismatches"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "Aligned Expo SDK 54 dependency versions"
      contains: "react-native-webview"
  key_links:
    - from: "apps/mobile/package.json"
      to: "expo SDK 54"
      via: "dependency version ranges"
      pattern: "expo.*~54"
---

<objective>
Fix Expo SDK 54 dependency mismatches and add missing explicit dependencies in the mobile app.

Purpose: Ensure the mobile dependency tree is clean -- no implicit transitive dependencies, no exact pins where tilde/caret is safe, and expo doctor reports compatibility.
Output: Updated apps/mobile/package.json with aligned versions.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dependencies-performance/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing explicit dependency and align version ranges</name>
  <files>apps/mobile/package.json</files>
  <read_first>
    - apps/mobile/package.json
  </read_first>
  <action>
1. Add `react-native-webview` as an explicit dependency in apps/mobile/package.json. It is currently only a transitive dependency via react-native-signature-canvas. Use a version compatible with Expo SDK 54 / RN 0.81 (check `npx expo install react-native-webview --check` output for the correct version).

2. Change `expo-router` from exact pin `6.0.23` to tilde range `~6.0.23` to allow patch updates.

3. Change `react` from exact pin `19.1.0` to tilde range `~19.1.0` and `react-native` from exact `0.81.5` to tilde `~0.81.5`. Similarly for `react-native-safe-area-context` (currently `5.6.2` -- change to `~5.6.2`).

4. Verify `@sentry/react-native` version `^7.2.0` is compatible with SDK 54 / RN 0.81. If not, pin to a known compatible version (check Sentry SDK 54 compatibility docs).

5. Verify `@react-native-community/netinfo` version `^11.4.1` is compatible with SDK 54. Adjust if needed.

6. Run `npx expo install --check` in apps/mobile and fix any remaining version mismatches it identifies.

7. Run `npx expo install --fix` if the check step identifies auto-fixable issues.

Do NOT run npm/yarn install at the workspace root -- only use `npx expo install` commands within apps/mobile.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/mobile && npx expo install --check 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "react-native-webview" apps/mobile/package.json (explicit dependency present)
    - grep -q '"expo-router": "~' apps/mobile/package.json (tilde range for expo-router)
    - grep -q '"react": "~' apps/mobile/package.json (tilde range for react)
    - grep -q '"react-native": "~' apps/mobile/package.json (tilde range for react-native)
    - grep -q '"react-native-safe-area-context": "~' apps/mobile/package.json (tilde range)
  </acceptance_criteria>
  <done>react-native-webview is explicit, expo-router/react/react-native use tilde ranges, expo install --check passes clean</done>
</task>

</tasks>

<verification>
- `grep "react-native-webview" apps/mobile/package.json` shows the dependency
- `grep '"expo-router"' apps/mobile/package.json` shows tilde range
- `npx expo install --check` in apps/mobile exits without critical errors
</verification>

<success_criteria>
- DEPS-01: react-native-webview is an explicit dependency
- DEPS-02: expo-router uses tilde range, Sentry version verified for SDK 54
- DEPS-03: react and react-native use tilde ranges instead of exact pins
</success_criteria>

<output>
After completion, create `.planning/phases/13-dependencies-performance/13-01-SUMMARY.md`
</output>
