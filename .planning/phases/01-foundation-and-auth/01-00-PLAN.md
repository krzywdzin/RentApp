---
phase: 01-foundation-and-auth
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/test/auth.e2e-spec.ts
  - apps/api/test/audit.e2e-spec.ts
  - apps/api/src/audit/audit.service.spec.ts
  - apps/api/src/common/guards/roles.guard.spec.ts
  - apps/api/src/users/users.service.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "All 5 test stub files exist and can be discovered by Jest"
    - "Running jest --listTests returns all 5 spec/e2e-spec files"
    - "Each test file has at least one describe block with a placeholder test that skips or is marked todo"
  artifacts:
    - path: "apps/api/test/auth.e2e-spec.ts"
      provides: "E2E test scaffold for auth endpoints (AUTH-01, AUTH-02, AUTH-03, AUTH-04)"
    - path: "apps/api/test/audit.e2e-spec.ts"
      provides: "E2E test scaffold for audit trail (AUTH-05)"
    - path: "apps/api/src/audit/audit.service.spec.ts"
      provides: "Unit test scaffold for AuditService (AUTH-05)"
    - path: "apps/api/src/common/guards/roles.guard.spec.ts"
      provides: "Unit test scaffold for RolesGuard (AUTH-04)"
    - path: "apps/api/src/users/users.service.spec.ts"
      provides: "Unit test scaffold for UsersService (AUTH-02)"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds: all spec files required by the VALIDATION.md Nyquist contract. These are stub files with describe blocks and placeholder/todo tests that define the behavioral contract. Implementation plans will fill them in.

Purpose: Satisfies the Nyquist rule that every verify command references an existing test file. Without these stubs, verify commands in Plans 01-02 through 01-05 would reference non-existent files.
Output: 5 test files (3 unit specs, 2 e2e specs) ready for implementation plans to populate.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Wave 0 test stub files</name>
  <files>
    apps/api/test/auth.e2e-spec.ts,
    apps/api/test/audit.e2e-spec.ts,
    apps/api/src/audit/audit.service.spec.ts,
    apps/api/src/common/guards/roles.guard.spec.ts,
    apps/api/src/users/users.service.spec.ts
  </files>
  <action>
    Create 5 test stub files. Each file should have a top-level describe block with `.todo` placeholder tests that document the behavioral contract. These stubs will be filled in by subsequent plans as features are implemented.

    1. Create `apps/api/test/auth.e2e-spec.ts`:
       ```typescript
       import { Test, TestingModule } from '@nestjs/testing';
       import { INestApplication } from '@nestjs/common';
       import * as request from 'supertest';

       describe('Auth (e2e)', () => {
         // AUTH-01: Login
         it.todo('POST /auth/login with valid credentials returns access + refresh tokens');
         it.todo('POST /auth/login with invalid credentials returns 401');
         it.todo('POST /auth/login locks account after 5 failed attempts');

         // AUTH-02: Password management
         it.todo('POST /auth/setup-password with valid token sets password');
         it.todo('POST /auth/setup-password with expired token returns 401');
         it.todo('POST /auth/reset-password-request always returns 200 (no email leak)');
         it.todo('POST /auth/reset-password with valid token resets password');

         // AUTH-03: Token refresh
         it.todo('POST /auth/refresh with valid refresh token returns new token pair');
         it.todo('POST /auth/refresh with reused token invalidates all sessions (rotation)');
         it.todo('POST /auth/refresh with expired token returns 401');

         // AUTH-04: Role enforcement
         it.todo('POST /users without ADMIN role returns 403');
         it.todo('POST /users with ADMIN role creates user and returns 201');
         it.todo('GET /health without auth returns 200 (public endpoint)');
         it.todo('GET /users/me without auth returns 401');
       });
       ```

    2. Create `apps/api/test/audit.e2e-spec.ts`:
       ```typescript
       import { Test, TestingModule } from '@nestjs/testing';
       import { INestApplication } from '@nestjs/common';
       import * as request from 'supertest';

       describe('Audit (e2e)', () => {
         // AUTH-05: Audit trail
         it.todo('POST mutation automatically creates audit log entry');
         it.todo('GET request does NOT create audit log entry');
         it.todo('GET /audit as ADMIN returns paginated audit logs');
         it.todo('GET /audit as EMPLOYEE returns 403');
         it.todo('GET /audit filters by entityType');
         it.todo('GET /audit filters by actorId');
         it.todo('Audit log entry contains actorId, action, entityType, entityId, changes');
       });
       ```

    3. Create `apps/api/src/audit/audit.service.spec.ts`:
       ```typescript
       describe('AuditService', () => {
         it.todo('log() creates an audit record with all required fields');
         it.todo('log() stores changes as JSON');
         it.todo('findAll() returns paginated results');
         it.todo('findAll() filters by entityType');
         it.todo('findAll() filters by actorId');
         it.todo('findAll() filters by entityId');
         it.todo('no update or delete methods exist on AuditService');
       });
       ```

    4. Create `apps/api/src/common/guards/roles.guard.spec.ts`:
       ```typescript
       describe('RolesGuard', () => {
         it.todo('allows request when no roles are required');
         it.todo('allows request when user has required role');
         it.todo('throws ForbiddenException when user lacks required role');
         it.todo('returns false when no user is present on request');
       });
       ```

    5. Create `apps/api/src/users/users.service.spec.ts`:
       ```typescript
       describe('UsersService', () => {
         it.todo('createUser() creates user with null passwordHash');
         it.todo('createUser() generates setup token with 72h expiry');
         it.todo('createUser() sends setup password email');
         it.todo('findByEmail() returns user when found');
         it.todo('findByEmail() returns null when not found');
         it.todo('requestPasswordReset() generates reset token with 1h expiry');
         it.todo('requestPasswordReset() does not throw for non-existent email');
       });
       ```

    Note: These files use `it.todo()` which Jest recognizes as pending tests. They will not fail when run, but they will appear in test output as "todo" reminders. Subsequent plans will replace `.todo` with actual implementations.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/api && ls test/auth.e2e-spec.ts test/audit.e2e-spec.ts src/audit/audit.service.spec.ts src/common/guards/roles.guard.spec.ts src/users/users.service.spec.ts</automated>
  </verify>
  <done>All 5 Wave 0 test stub files exist with describe blocks and .todo test cases documenting the behavioral contract for AUTH-01 through AUTH-05</done>
</task>

</tasks>

<verification>
- All 5 test files exist at their expected paths
- Each file has a describe block with at least 3 it.todo() entries
- Files cover AUTH-01 (login), AUTH-02 (password), AUTH-03 (refresh), AUTH-04 (roles), AUTH-05 (audit)
</verification>

<success_criteria>
- Wave 0 test stubs exist for all behavioral contracts defined in VALIDATION.md
- Jest can discover all 5 files without errors
- No test failures (todo tests are skipped by Jest)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-00-SUMMARY.md`
</output>
