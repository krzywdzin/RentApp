---
phase: 11-web-admin-panel-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/users/users.controller.ts
  - apps/api/src/users/users.service.ts
  - apps/web/src/hooks/queries/use-users.ts
  - apps/web/src/app/(admin)/uzytkownicy/page.tsx
  - apps/web/src/app/(admin)/uzytkownicy/columns.tsx
autonomous: true
requirements: [WEBUX-01]

must_haves:
  truths:
    - "Admin can see a list of all system users with name, email, role, and active status"
    - "Admin can edit a user's name and role via an edit dialog"
    - "Admin can toggle user active/inactive status"
    - "Admin can trigger a password reset email for a user"
  artifacts:
    - path: "apps/api/src/users/users.controller.ts"
      provides: "GET /users, PATCH /users/:id, POST /users/:id/reset-password endpoints"
      contains: "findAll"
    - path: "apps/api/src/users/users.service.ts"
      provides: "findAll, updateUser, deactivateUser, resetPassword service methods"
      contains: "findAll"
    - path: "apps/web/src/hooks/queries/use-users.ts"
      provides: "useUsers, useUpdateUser, useDeactivateUser, useResetPassword hooks"
      contains: "useUsers"
    - path: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      provides: "User management page with DataTable, edit dialog, actions"
      contains: "DataTable"
    - path: "apps/web/src/app/(admin)/uzytkownicy/columns.tsx"
      provides: "User table column definitions"
      contains: "userColumns"
  key_links:
    - from: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      to: "apps/web/src/hooks/queries/use-users.ts"
      via: "useUsers hook"
      pattern: "useUsers"
    - from: "apps/web/src/hooks/queries/use-users.ts"
      to: "/api/users"
      via: "apiClient fetch"
      pattern: "apiClient.*users"
---

<objective>
Add a complete user management page to the web admin panel. Admin can list all users, edit name/role, toggle active status, and trigger password reset.

Purpose: WEBUX-01 -- the admin panel currently only has a create-user form with no way to view, edit, or manage existing users.
Output: API endpoints for user CRUD + web page with DataTable, edit dialog, deactivate toggle, password reset action.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing API and UI patterns the executor needs -->

From apps/api/src/users/users.controller.ts (current -- only POST and GET /me):
```typescript
@Controller('users')
export class UsersController {
  @Post() @Roles(UserRole.ADMIN) create(@Body() dto, @CurrentUser() user) {}
  @Get('me') getMe(@CurrentUser() user) {}
}
```

From apps/api/src/users/users.service.ts (current):
```typescript
export class UsersService {
  async createUser(dto, adminId): Promise<{id, email, name, role}> {}
  async findByEmail(email): Promise<User | null> {}
  async findById(id): Promise<User | null> {}
  async requestPasswordReset(email): Promise<void> {}
}
```

Prisma User model fields: id, email, name, role (ADMIN|EMPLOYEE), passwordHash?, setupToken?, setupTokenExpiry?, isActive (Boolean, default true), failedAttempts, lockedUntil?, createdAt, updatedAt

From apps/web/src/components/data-table/data-table.tsx -- DataTable component props pattern:
```typescript
<DataTable columns={columns} data={data} pageCount={N} pagination={state} onPaginationChange={fn} sorting={state} onSortingChange={fn} isLoading={bool} />
```

Mutation pattern from use-rentals.ts:
```typescript
useMutation({ mutationFn: (data) => apiClient(url, { method, body }), onSuccess: () => { queryClient.invalidateQueries(); toast.success('...'); }, onError: () => { toast.error('...'); } })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user management API endpoints</name>
  <files>apps/api/src/users/users.controller.ts, apps/api/src/users/users.service.ts</files>
  <read_first>
    apps/api/src/users/users.controller.ts
    apps/api/src/users/users.service.ts
    apps/api/src/common/decorators/roles.decorator.ts
    apps/api/prisma/schema.prisma
  </read_first>
  <action>
    Add three new endpoints to UsersController (all @Roles(UserRole.ADMIN)):

    1. `@Get()` -- calls usersService.findAll(), returns array of {id, email, name, role, isActive, createdAt}
    2. `@Patch(':id')` -- calls usersService.updateUser(id, dto), accepts {name?: string, role?: UserRole, isActive?: boolean}, returns updated user
    3. `@Post(':id/reset-password')` -- calls usersService.resetPasswordByAdmin(id), triggers setup token + email, returns {success: true}

    Add to UsersService:
    - `findAll()`: prisma.user.findMany({ select: {id, email, name, role, isActive, createdAt}, orderBy: {createdAt: 'desc'} })
    - `updateUser(id, dto)`: prisma.user.update with {name?, role?, isActive?}, return {id, email, name, role, isActive, createdAt}
    - `resetPasswordByAdmin(id)`: find user by id, generate new setupToken (same pattern as createUser), send setup password email, return void

    Import Param from @nestjs/common for :id route params. Use @Roles(UserRole.ADMIN) on all new endpoints.
    Do NOT remove existing endpoints (POST for create, GET /me).
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "findAll" apps/api/src/users/users.service.ts
    - grep -q "updateUser" apps/api/src/users/users.service.ts
    - grep -q "resetPasswordByAdmin" apps/api/src/users/users.service.ts
    - grep -q "@Get()" apps/api/src/users/users.controller.ts
    - grep -q "@Patch" apps/api/src/users/users.controller.ts
    - grep -q "reset-password" apps/api/src/users/users.controller.ts
  </acceptance_criteria>
  <done>GET /users returns user list, PATCH /users/:id updates user, POST /users/:id/reset-password triggers password reset email. All admin-only.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite user management page with DataTable and actions</name>
  <files>apps/web/src/hooks/queries/use-users.ts, apps/web/src/app/(admin)/uzytkownicy/page.tsx, apps/web/src/app/(admin)/uzytkownicy/columns.tsx</files>
  <read_first>
    apps/web/src/app/(admin)/uzytkownicy/page.tsx
    apps/web/src/hooks/queries/use-rentals.ts
    apps/web/src/app/(admin)/pojazdy/columns.tsx
    apps/web/src/app/(admin)/pojazdy/page.tsx
    apps/web/src/components/data-table/data-table.tsx
  </read_first>
  <action>
    1. Create `apps/web/src/hooks/queries/use-users.ts`:
       - Define UserDto interface: {id: string, email: string, name: string, role: string, isActive: boolean, createdAt: string}
       - Export userKeys = { all: ['users'], list: () => [...all, 'list'] }
       - Export useUsers(): useQuery fetching apiClient<UserDto[]>('/users')
       - Export useUpdateUser(): useMutation calling PATCH /users/:id, invalidates userKeys.all, toast.success('Uzytkownik zaktualizowany')
       - Export useDeactivateUser(): useMutation calling PATCH /users/:id with {isActive: bool}, invalidates userKeys.all, toast with appropriate message
       - Export useResetPassword(): useMutation calling POST /users/:id/reset-password, toast.success('Email z linkiem do resetu hasla wyslany')

    2. Create `apps/web/src/app/(admin)/uzytkownicy/columns.tsx`:
       - Export userColumns: ColumnDef<UserDto>[] with columns: Imie i nazwisko (name), Email (email), Rola (role -- show "Administrator"/"Pracownik"), Status (isActive -- Badge "Aktywny" variant="success" or "Nieaktywny" variant="secondary"), Utworzono (createdAt formatted), Akcje (3-dot dropdown with Edytuj, Resetuj haslo, Deaktywuj/Aktywuj)
       - Actions column uses DropdownMenu from shadcn/ui

    3. Rewrite `apps/web/src/app/(admin)/uzytkownicy/page.tsx`:
       - Keep the existing create user form in a collapsible Card at top
       - Below it, add DataTable with userColumns and useUsers() data
       - Add Edit dialog (Dialog component): form with name Input and role Select (ADMIN/EMPLOYEE), save calls useUpdateUser
       - Deactivate action calls useDeactivateUser with toggled isActive
       - Reset password action calls useResetPassword
       - Use same pagination/sorting pattern as pojazdy or wynajmy pages

    Role labels: ADMIN -> "Administrator", EMPLOYEE -> "Pracownik"
    Status badge: isActive true -> Badge variant="success" "Aktywny", false -> Badge variant="secondary" "Nieaktywny"
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useUsers" apps/web/src/hooks/queries/use-users.ts
    - grep -q "useUpdateUser" apps/web/src/hooks/queries/use-users.ts
    - grep -q "useResetPassword" apps/web/src/hooks/queries/use-users.ts
    - grep -q "userColumns" apps/web/src/app/(admin)/uzytkownicy/columns.tsx
    - grep -q "DataTable" apps/web/src/app/(admin)/uzytkownicy/page.tsx
    - grep -q "useUsers" apps/web/src/app/(admin)/uzytkownicy/page.tsx
    - grep -q "Aktywny" apps/web/src/app/(admin)/uzytkownicy/columns.tsx
    - grep -q "Administrator" apps/web/src/app/(admin)/uzytkownicy/columns.tsx
  </acceptance_criteria>
  <done>User management page shows DataTable with all users. Admin can edit name/role via dialog, toggle active status, and trigger password reset. Create user form preserved at top.</done>
</task>

</tasks>

<verification>
- TypeScript compiles for both API and web: `npx tsc --noEmit`
- User list page renders with DataTable component
- All user management actions (edit, deactivate, reset password) wired to API
</verification>

<success_criteria>
Admin can view all users in a DataTable, edit user name/role, toggle active/inactive, and trigger password reset -- all from /uzytkownicy page.
</success_criteria>

<output>
After completion, create `.planning/phases/11-web-admin-panel-polish/11-01-SUMMARY.md`
</output>
