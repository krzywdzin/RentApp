---
phase: 11-web-admin-panel-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(admin)/wynajmy/columns.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx
  - apps/web/src/app/(admin)/klienci/[id]/page.tsx
  - apps/web/src/app/(admin)/pojazdy/[id]/page.tsx
autonomous: true
requirements: [WEBUX-02, WEBUX-03, WEBUX-04, WEBUX-06]

must_haves:
  truths:
    - "Rental list shows vehicle registration plates and customer names instead of truncated UUIDs"
    - "Rental detail Umowa tab shows actual contract data loaded via useContractByRental"
    - "Edit rental form validates input with inline Zod error messages"
    - "Customer and vehicle detail rental tabs show Polish status labels instead of raw enum values"
  artifacts:
    - path: "apps/web/src/app/(admin)/wynajmy/columns.tsx"
      provides: "Rental columns with vehicle.registration and customer name"
      contains: "registrationNumber\\|registration"
    - path: "apps/web/src/app/(admin)/wynajmy/[id]/page.tsx"
      provides: "Contract tab with useContractByRental data display"
      contains: "useContractByRental"
    - path: "apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx"
      provides: "Edit form with zodResolver validation"
      contains: "zodResolver"
    - path: "apps/web/src/app/(admin)/klienci/[id]/page.tsx"
      provides: "Customer detail rental tab with Polish status badges"
      contains: "getRentalStatusBadge\\|statusConfig"
    - path: "apps/web/src/app/(admin)/pojazdy/[id]/page.tsx"
      provides: "Vehicle detail rental tab with Polish status badges"
      contains: "getRentalStatusBadge\\|statusConfig"
  key_links:
    - from: "apps/web/src/app/(admin)/wynajmy/columns.tsx"
      to: "RentalDto with vehicle/customer relations"
      via: "row.original.vehicle.registration"
      pattern: "vehicle.*registration"
    - from: "apps/web/src/app/(admin)/wynajmy/[id]/page.tsx"
      to: "apps/web/src/hooks/queries/use-contracts.ts"
      via: "useContractByRental hook"
      pattern: "useContractByRental"
---

<objective>
Fix rental data display across the admin panel: show real vehicle/customer names in lists, wire the contract tab, add form validation, and use Polish status labels consistently.

Purpose: WEBUX-02/03/04/06 -- rental list shows UUIDs instead of human-readable data, contract tab is hardcoded placeholder, edit form lacks validation, and detail tabs show raw enum values.
Output: Updated rental columns, wired contract tab, validated edit form, Polish labels on all rental status displays.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- The API already includes vehicle and customer relations in rental responses -->

From apps/api/src/rentals/rentals.service.ts:
```typescript
const RENTAL_INCLUDE = { vehicle: true, customer: true };
// findAll() and findOne() both use this include
// So the API response ALREADY has vehicle.registration, customer.firstName, customer.lastName
```

From packages/shared/src/types/rental.types.ts (RentalDto -- does NOT have vehicle/customer typed):
```typescript
export interface RentalDto {
  id: string; vehicleId: string; customerId: string; // ... other fields
  // NOTE: vehicle and customer are returned by API but not typed here
}
```

From apps/web/src/hooks/queries/use-contracts.ts:
```typescript
export function useContractByRental(rentalId: string) {
  return useQuery({ queryKey: contractKeys.byRental(rentalId), queryFn: () => apiClient<ContractDto>(`/contracts/rental/${rentalId}`), enabled: !!rentalId });
}
```

From apps/web/src/app/(admin)/wynajmy/columns.tsx (existing status config):
```typescript
const statusConfig: Record<RentalStatus, { label: string; variant: string }> = {
  [RentalStatus.DRAFT]: { label: 'Szkic', variant: 'secondary' },
  [RentalStatus.ACTIVE]: { label: 'Aktywny', variant: 'success' },
  [RentalStatus.EXTENDED]: { label: 'Przedluzony', variant: 'warning' },
  [RentalStatus.RETURNED]: { label: 'Zwrocony', variant: 'secondary' },
};
export function getRentalStatusBadge(rental: RentalDto) { ... }
```

Edit form currently uses useForm<EditFormValues>() WITHOUT zodResolver -- no validation.
```typescript
const { register, handleSubmit, reset, formState: { isSubmitting } } = useForm<EditFormValues>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rental list columns and wire contract tab</name>
  <files>apps/web/src/app/(admin)/wynajmy/columns.tsx, apps/web/src/app/(admin)/wynajmy/[id]/page.tsx</files>
  <read_first>
    apps/web/src/app/(admin)/wynajmy/columns.tsx
    apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
    apps/web/src/hooks/queries/use-contracts.ts
    packages/shared/src/types/rental.types.ts
  </read_first>
  <action>
    1. In `columns.tsx`, the API already returns vehicle and customer nested objects. Update column cell renderers:
       - vehicleId column: change cell from `row.original.vehicleId.slice(0, 8)` to `(row.original as any).vehicle?.registrationNumber || (row.original as any).vehicle?.registration || row.original.vehicleId.slice(0, 8)`. Check the Prisma Vehicle model for the exact field name (likely `registration`). Use a type assertion since RentalDto doesn't include these yet (TypeScript strictness is Phase 12).
       - customerId column: change cell to show `(row.original as any).customer?.firstName + ' ' + (row.original as any).customer?.lastName` with fallback to customerId.slice(0,8) if customer is null.
       - Update column header title for vehicleId to "Pojazd" (already correct), customerId to "Klient" (already correct).

    2. In `[id]/page.tsx`, replace the hardcoded "Umowa" tab content:
       - Import useContractByRental from '@/hooks/queries/use-contracts'
       - Add `const { data: contract, isLoading: contractLoading, isError: contractError } = useContractByRental(id);` (place BEFORE the early returns to respect hooks rules)
       - Replace the TabsContent value="umowa" Card content:
         - If contractLoading: show Skeleton
         - If contractError or !contract: show "Brak umowy powiazanej z tym wynajmem." (keep existing text)
         - If contract exists: show contract details in a Card with dl/dd layout:
           - Status (contract.status with Polish label)
           - Data utworzenia (formatDateTime(contract.createdAt))
           - Strony (parties info if available)
           - Podpisy (signatures count / status)
           - Link to /umowy/{contract.id} for full details
       - Contract status labels: DRAFT -> "Szkic", PARTIALLY_SIGNED -> "Czesciowo podpisana", SIGNED -> "Podpisana", VOIDED -> "Uniewazniona"

    Also in rental detail `[id]/page.tsx`, update the Szczegoly tab to show vehicle registration and customer name instead of raw IDs:
       - Pojazd: `(rental as any).vehicle?.registration || rental.vehicleId`
       - Klient: `(rental as any).customer ? \`${(rental as any).customer.firstName} ${(rental as any).customer.lastName}\` : rental.customerId`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "vehicle.*registration\|registrationNumber" apps/web/src/app/(admin)/wynajmy/columns.tsx
    - grep -q "customer.*firstName\|customer.*lastName" apps/web/src/app/(admin)/wynajmy/columns.tsx
    - grep -q "useContractByRental" apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
    - grep -q "contractLoading\|isLoading.*contract" apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
  </acceptance_criteria>
  <done>Rental list shows vehicle registration plates and customer names. Rental detail contract tab loads and displays actual contract data. Rental detail shows human-readable vehicle/customer info.</done>
</task>

<task type="auto">
  <name>Task 2: Add Zod validation to edit form and Polish labels to detail tabs</name>
  <files>apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx, apps/web/src/app/(admin)/klienci/[id]/page.tsx, apps/web/src/app/(admin)/pojazdy/[id]/page.tsx</files>
  <read_first>
    apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx
    apps/web/src/app/(admin)/klienci/[id]/page.tsx
    apps/web/src/app/(admin)/pojazdy/[id]/page.tsx
    apps/web/src/app/(admin)/wynajmy/columns.tsx
  </read_first>
  <action>
    1. In `edytuj/page.tsx` (edit rental form):
       - Import zodResolver from '@hookform/resolvers/zod' and z from 'zod'
       - Define editRentalSchema with z.object:
         - startDate: z.string().min(1, 'Data poczatkowa jest wymagana')
         - endDate: z.string().min(1, 'Data koncowa jest wymagana')
         - dailyRateNet: z.number({ invalid_type_error: 'Podaj stawke' }).positive('Stawka musi byc wieksza od 0')
         - vatRate: z.number({ invalid_type_error: 'Podaj stawke VAT' }).min(0).max(100, 'VAT musi byc miedzy 0 a 100')
         - notes: z.string().optional().default('')
       - Add .refine() to validate endDate > startDate: `.refine(data => !data.startDate || !data.endDate || new Date(data.endDate) > new Date(data.startDate), { message: 'Data koncowa musi byc pozniejsza niz poczatkowa', path: ['endDate'] })`
       - Update useForm call: `useForm<EditFormValues>({ resolver: zodResolver(editRentalSchema) })`
       - Destructure `formState: { isSubmitting, errors }` from useForm
       - Add error messages below each Input: `{errors.fieldName && <p className="text-sm text-destructive">{errors.fieldName.message}</p>}`

    2. In `klienci/[id]/page.tsx` (customer detail -- Wynajmy tab):
       - Import getRentalStatusBadge from '../../../wynajmy/columns' (or from the wynajmy columns path)
       - Replace `<Badge variant="secondary">{rental.status}</Badge>` with `getRentalStatusBadge(rental)`
       - The rental list items already exist. Just swap the Badge rendering.
       - Add loading state: wrap the rentals section. Currently useRentals() result is not checked for loading. Add `const { data: rentals, isLoading: rentalsLoading } = useRentals();` and show Skeleton inside the Wynajmy tab while rentalsLoading.

    3. In `pojazdy/[id]/page.tsx` (vehicle detail -- Wynajmy tab):
       - Same pattern: import getRentalStatusBadge, replace raw status Badge with Polish status badges
       - Add loading state for rentals in Wynajmy tab: show Skeleton while loading
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "zodResolver" apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx
    - grep -q "editRentalSchema\|z\.object" apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx
    - grep -q "errors\." apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx
    - grep -q "getRentalStatusBadge" apps/web/src/app/(admin)/klienci/[id]/page.tsx
    - grep -q "getRentalStatusBadge" apps/web/src/app/(admin)/pojazdy/[id]/page.tsx
    - grep -q "rentalsLoading\|isLoading.*rental" apps/web/src/app/(admin)/klienci/[id]/page.tsx
  </acceptance_criteria>
  <done>Edit rental form shows inline Zod validation errors. Customer and vehicle detail rental tabs show Polish status labels with loading states.</done>
</task>

</tasks>

<verification>
- TypeScript compiles for web app
- Rental list columns render vehicle registration and customer name
- Contract tab shows real contract data or appropriate empty state
- Edit rental form shows inline errors for invalid input
- Customer and vehicle detail pages use Polish status badges
</verification>

<success_criteria>
Rental list shows human-readable vehicle and customer info. Contract tab is wired and displays data. Edit form validates with inline errors. All rental status displays use Polish labels consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/11-web-admin-panel-polish/11-02-SUMMARY.md`
</output>
