---
phase: 23-mobile-quality-ux
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/rentals/index.tsx
  - apps/mobile/src/components/SearchBar.tsx
  - apps/mobile/src/components/AppInput.tsx
  - apps/mobile/app/(tabs)/index.tsx
autonomous: true
requirements:
  - MA11Y-01
  - MA11Y-02
  - MA11Y-03
  - MA11Y-04

must_haves:
  truths:
    - "Filter chips have accessibilityRole='radio' and accessibilityState={{ selected: isActive }}"
    - "SearchBar TextInput has accessibilityLabel describing its purpose"
    - "AppInput label Text is linked to TextInput via nativeID/accessibilityLabelledBy"
    - "Dashboard stat cards have combined accessibilityLabel (e.g. 'Aktywne wynajmy: 5')"
  artifacts:
    - path: "apps/mobile/app/(tabs)/rentals/index.tsx"
      provides: "Accessible filter chips"
      contains: "accessibilityRole"
    - path: "apps/mobile/src/components/SearchBar.tsx"
      provides: "Accessible search input"
      contains: "accessibilityLabel"
    - path: "apps/mobile/src/components/AppInput.tsx"
      provides: "Label-to-input association"
      contains: "accessibilityLabelledBy"
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Accessible stat cards"
      contains: "accessibilityLabel"
  key_links:
    - from: "apps/mobile/src/components/AppInput.tsx"
      to: "TextInput"
      via: "nativeID on Text and accessibilityLabelledBy on TextInput"
      pattern: "accessibilityLabelledBy"
---

<objective>
Add accessibility attributes to interactive mobile elements so screen readers (VoiceOver/TalkBack) can announce filter state, search purpose, input labels, and dashboard statistics.

Purpose: Filter chips have no role/state for screen readers, SearchBar has no label, AppInput label is not programmatically linked to its TextInput, and dashboard stat cards read as disconnected text fragments.

Output: All four accessibility gaps closed with proper React Native accessibility props.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/mobile/app/(tabs)/rentals/index.tsx
@apps/mobile/src/components/SearchBar.tsx
@apps/mobile/src/components/AppInput.tsx
@apps/mobile/app/(tabs)/index.tsx

<interfaces>
React Native accessibility props:
```typescript
// Pressable / View
accessibilityRole: "button" | "radio" | "tab" | "search" | ...
accessibilityState: { selected?: boolean, disabled?: boolean, checked?: boolean }
accessibilityLabel: string  // read by screen reader

// TextInput
accessibilityLabel: string
accessibilityLabelledBy: string  // matches nativeID on another element (Android)

// Text (label element)
nativeID: string  // used by accessibilityLabelledBy
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessibility to filter chips and SearchBar</name>
  <read_first>
    - apps/mobile/app/(tabs)/rentals/index.tsx
    - apps/mobile/src/components/SearchBar.tsx
  </read_first>
  <files>
    apps/mobile/app/(tabs)/rentals/index.tsx,
    apps/mobile/src/components/SearchBar.tsx
  </files>
  <action>
    1. In rentals/index.tsx (MA11Y-01):
       - Find the filter chip Pressable elements (the status filter buttons like "Wszystkie", "Aktywne", etc.)
       - Add to each filter chip Pressable:
         ```typescript
         accessibilityRole="radio"
         accessibilityState={{ selected: isActive }}
         accessibilityLabel={`Filtr: ${label}`}
         ```
       - Where `isActive` is the boolean for whether that filter is currently selected, and `label` is the chip text
       - If chips are rendered in a .map(), the props go on the Pressable inside the map callback

    2. In SearchBar.tsx (MA11Y-02):
       - Add `accessibilityLabel` prop to the SearchBarProps interface: `accessibilityLabel?: string`
       - Pass it to the TextInput: `accessibilityLabel={accessibilityLabel ?? placeholder ?? 'Szukaj'}`
       - Add `accessibilityRole="search"` to the outer View container
       - Add `accessibilityLabel="Wyczysc wyszukiwanie"` to the clear button Pressable
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'accessibilityRole="radio"' apps/mobile/app/\(tabs\)/rentals/index.tsx (filter chips have radio role)
    - grep -q "accessibilityState" apps/mobile/app/\(tabs\)/rentals/index.tsx (filter chips have selected state)
    - grep -q "accessibilityLabel" apps/mobile/src/components/SearchBar.tsx (search input has label)
    - grep -q 'accessibilityRole="search"' apps/mobile/src/components/SearchBar.tsx (search container has role)
  </acceptance_criteria>
  <done>
    - Filter chips announce as radio buttons with selected state to screen readers
    - SearchBar TextInput has descriptive accessibilityLabel
    - SearchBar container has search role
    - Clear button has accessibility label
  </done>
</task>

<task type="auto">
  <name>Task 2: Add accessibility to AppInput and dashboard stat cards</name>
  <read_first>
    - apps/mobile/src/components/AppInput.tsx
    - apps/mobile/app/(tabs)/index.tsx
  </read_first>
  <files>
    apps/mobile/src/components/AppInput.tsx,
    apps/mobile/app/(tabs)/index.tsx
  </files>
  <action>
    1. In AppInput.tsx (MA11Y-03):
       - Generate a stable nativeID from the label: `const inputId = label.toLowerCase().replace(/\s+/g, '-') + '-input'`
       - Add `nativeID={inputId + '-label'}` to the label Text element
       - Add `accessibilityLabelledBy={inputId + '-label'}` to the TextInput
       - Also add `accessibilityLabel={label}` to the TextInput as a fallback for iOS VoiceOver (which doesn't use accessibilityLabelledBy)
       - If error is present, add `accessibilityHint={error}` to the TextInput so screen readers announce the error

    2. In index.tsx - Dashboard (MA11Y-04):
       - Find the stat card Views (the horizontal ScrollView with "Aktywne wynajmy", "Dzisiaj zwroty", etc.)
       - Each stat card should have a combined accessibilityLabel that merges the title and value:
         ```typescript
         accessibilityLabel={`${title}: ${value}`}
         ```
       - For example: `accessibilityLabel={`Aktywne wynajmy: ${activeCount}`}`
       - Add `accessible={true}` to the stat card View so it is treated as a single accessible element (children are not individually focusable)
       - Add `accessibilityRole="text"` since these are informational displays
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "nativeID" apps/mobile/src/components/AppInput.tsx (label has nativeID)
    - grep -q "accessibilityLabelledBy" apps/mobile/src/components/AppInput.tsx (input linked to label)
    - grep -q "accessibilityLabel" apps/mobile/src/components/AppInput.tsx (fallback label on input)
    - grep -q "accessible={true}" apps/mobile/app/\(tabs\)/index.tsx (stat cards are single accessible elements)
    - grep -q "accessibilityLabel" apps/mobile/app/\(tabs\)/index.tsx (stat cards have combined labels)
  </acceptance_criteria>
  <done>
    - AppInput label is programmatically linked to TextInput via nativeID/accessibilityLabelledBy
    - AppInput announces error state via accessibilityHint
    - Dashboard stat cards read as single elements with combined label+value
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `npx tsc --noEmit --project apps/mobile/tsconfig.json`
- Filter chips have accessibilityRole="radio" and accessibilityState
- SearchBar has accessibilityLabel and search role
- AppInput has nativeID/accessibilityLabelledBy linkage
- Dashboard stat cards have combined accessibilityLabel
</verification>

<success_criteria>
- All four accessibility requirements (MA11Y-01 through MA11Y-04) implemented
- Screen reader can announce filter selection state
- Screen reader can identify search input purpose
- Screen reader can associate input labels with their fields
- Screen reader reads stat cards as single "Title: Value" announcements
</success_criteria>

<output>
After completion, create `.planning/phases/23-mobile-quality-ux/23-04-SUMMARY.md`
</output>
