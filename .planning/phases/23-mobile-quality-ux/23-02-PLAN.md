---
phase: 23-mobile-quality-ux
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - apps/mobile/app/(tabs)/new-rental/dates.tsx
  - apps/mobile/app/return/mileage.tsx
  - apps/mobile/src/stores/auth.store.ts
  - apps/mobile/src/hooks/use-rentals.ts
  - apps/mobile/app/(tabs)/new-rental/signatures.tsx
autonomous: true
requirements:
  - MVAL-01
  - MVAL-02
  - MVAL-03
  - MVAL-04
  - MVAL-05

must_haves:
  truths:
    - "Entering non-numeric text like '.' or '1.2.3' in the daily rate field shows an inline validation error"
    - "Entering a return mileage that exceeds handover mileage by more than 10000 shows a validation warning"
    - "Opening the app with valid token but no network does not log the user out"
    - "Return submission retries up to 2 times on transient network failure"
    - "Individual photo upload failures are tracked and allow per-photo retry"
  artifacts:
    - path: "apps/mobile/app/(tabs)/new-rental/dates.tsx"
      provides: "Zod regex validation on dailyRateNet field"
      contains: "regex"
    - path: "apps/mobile/app/return/mileage.tsx"
      provides: "Upper bound check on return mileage"
      contains: "10000"
    - path: "apps/mobile/src/stores/auth.store.ts"
      provides: "Network error vs 401 distinction in initialize()"
      contains: "isAxiosError"
    - path: "apps/mobile/src/hooks/use-rentals.ts"
      provides: "retry: 2 on useReturnRental mutation"
      contains: "retry"
  key_links:
    - from: "apps/mobile/app/(tabs)/new-rental/dates.tsx"
      to: "react-hook-form"
      via: "Zod schema resolver validates dailyRateNet format"
      pattern: "zodResolver.*DatesSchema"
    - from: "apps/mobile/src/stores/auth.store.ts"
      to: "axios"
      via: "isAxiosError check before clearing tokens"
      pattern: "isAxiosError"
---

<objective>
Add input validation to mobile forms and improve error handling so invalid data is caught before submission and transient failures don't destroy user state.

Purpose: Daily rate accepts garbage like "1.2.3", return mileage has no upper bound, a network blip logs the user out, return submission has no retry, and photo upload failures are swallowed with no recovery path.

Output: Validated form inputs with inline errors, resilient auth initialization, retry on return submission, partial photo failure tracking.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-mobile-quality-ux/23-01-SUMMARY.md

@apps/mobile/app/(tabs)/new-rental/dates.tsx
@apps/mobile/app/return/mileage.tsx
@apps/mobile/src/stores/auth.store.ts
@apps/mobile/src/hooks/use-rentals.ts
@apps/mobile/app/(tabs)/new-rental/signatures.tsx

<interfaces>
From apps/mobile/src/hooks/use-rentals.ts:
```typescript
export function useReturnRental() {
  // useMutation with no retry config currently
  return useMutation({
    mutationFn: ({ id, data }) => rentalsApi.returnRental(id, data),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: rentalKeys.all }),
  });
}
```

From apps/mobile/src/stores/auth.store.ts:
```typescript
initialize: async () => {
  // Currently catches ALL errors and clears tokens -- needs to distinguish 401 from network
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add form validation for daily rate and return mileage</name>
  <read_first>
    - apps/mobile/app/(tabs)/new-rental/dates.tsx
    - apps/mobile/app/return/mileage.tsx
  </read_first>
  <files>
    apps/mobile/app/(tabs)/new-rental/dates.tsx,
    apps/mobile/app/return/mileage.tsx
  </files>
  <action>
    1. In dates.tsx (MVAL-01 - daily rate Zod validation):
       - Import `z` from 'zod' and `zodResolver` from '@hookform/resolvers/zod'
       - Create a Zod schema for the dates form:
         ```typescript
         const DatesSchema = z.object({
           startDate: z.date(),
           endDate: z.date(),
           dailyRateNet: z.string()
             .min(1, 'Stawka dzienna jest wymagana')
             .regex(/^\d+([.,]\d{1,2})?$/, 'Nieprawidlowy format stawki (np. 150 lub 150.00)'),
         });
         ```
       - Pass `resolver: zodResolver(DatesSchema)` to `useForm`
       - In the dailyRateNet Controller render, pass `error={errors.dailyRateNet?.message}` to the AppInput
       - Remove or update the manual handleSubmit validation for dailyRateNet (the Zod schema handles it now)
       - Replace `parseFloat(dailyRateStr)` in the pricing memo with a version that normalizes comma to dot: `parseFloat(dailyRateStr.replace(',', '.')) || 0`

    2. In mileage.tsx (MVAL-02 - return mileage upper bound):
       - After the existing lower bound check (`returnMileage >= handoverMileage`), add an upper bound check
       - If `returnMileage - handoverMileage > 10000`, show a validation error: "Przebieg wydaje sie zbyt wysoki (max 10 000 km od wydania)"
       - Use the existing error display pattern (set an error state string, show below the input field)
       - The check should be a soft warning that prevents submission but can be acknowledged -- use a state flag `mileageWarningAcknowledged` that gets set to true if the user explicitly confirms the high mileage via a small "Potwierdz" link below the warning text
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "regex" apps/mobile/app/\(tabs\)/new-rental/dates.tsx (Zod regex validation present)
    - grep -q "zodResolver" apps/mobile/app/\(tabs\)/new-rental/dates.tsx (Zod resolver used)
    - grep -q "10000" apps/mobile/app/return/mileage.tsx (upper bound check present)
    - grep -q "Przebieg wydaje" apps/mobile/app/return/mileage.tsx (Polish error message for high mileage)
  </acceptance_criteria>
  <done>
    - Daily rate field shows inline error for non-numeric input ("1.2.3", ".", letters)
    - Return mileage shows warning for implausible values (>10000 km difference)
    - Both validations happen before form submission
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix auth error handling, add return retry, and track photo failures</name>
  <read_first>
    - apps/mobile/src/stores/auth.store.ts
    - apps/mobile/src/hooks/use-rentals.ts
    - apps/mobile/app/(tabs)/new-rental/signatures.tsx (latest version from plan 01)
  </read_first>
  <files>
    apps/mobile/src/stores/auth.store.ts,
    apps/mobile/src/hooks/use-rentals.ts,
    apps/mobile/app/(tabs)/new-rental/signatures.tsx
  </files>
  <action>
    1. In auth.store.ts (MVAL-03 - distinguish network vs 401):
       - Import `isAxiosError` from 'axios'
       - In the `initialize()` catch block, check: `if (isAxiosError(error) && error.response?.status === 401)` then clear tokens and set isAuthenticated: false (existing behavior)
       - For ALL other errors (network timeout, no connection, 500), just set `isLoading: false` and keep the existing tokens/user state. The user stays authenticated with stale data rather than being logged out. Set a console.warn for debugging: `console.warn('Auth initialize failed (non-401), keeping session:', error)`
       - Change `catch {` to `catch (error) {` to capture the error variable

    2. In use-rentals.ts (MVAL-04 - return submit retry):
       - Add `retry: 2` to the `useReturnRental` mutation options
       - This gives the mutation 2 automatic retries on failure (3 total attempts), matching the pattern used for signature uploads

    3. In signatures.tsx (MVAL-05 - track partial photo failures):
       - After plan 01 changes, modify the photo upload section to track per-photo success/failure
       - Create a `failedPhotos` state: `const [failedPhotos, setFailedPhotos] = useState<Record<string, string>>({})` (key=position, value=uri)
       - In the photo upload loop, wrap each individual photo in try/catch. On failure, add to failedPhotos instead of breaking the loop
       - After all photos are attempted, if failedPhotos has entries, show a toast: `"${Object.keys(failedPhotos).length} zdjec nie zostalo wyslanych. Mozesz dodac je pozniej."` (info toast, not error -- don't block success)
       - Store failedPhotos in the draft store is NOT needed -- the photos were already taken and the rental is created. The info toast is sufficient since the user can add photos later from the rental detail screen
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "isAxiosError" apps/mobile/src/stores/auth.store.ts (axios error type check present)
    - grep -q "401" apps/mobile/src/stores/auth.store.ts (401 status check present)
    - grep -q "retry: 2" apps/mobile/src/hooks/use-rentals.ts (retry config on return mutation)
    - grep -q "failedPhotos" apps/mobile/app/\(tabs\)/new-rental/signatures.tsx (per-photo failure tracking)
  </acceptance_criteria>
  <done>
    - Network errors during auth init keep user logged in (only 401 logs out)
    - Return submission retries twice on transient failure
    - Photo upload tracks individual failures and reports count to user
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `npx tsc --noEmit --project apps/mobile/tsconfig.json`
- Daily rate field rejects "1.2.3" and "." with inline error
- Return mileage shows warning for >10000km difference
- Auth store only clears tokens on 401, not network errors
- useReturnRental has retry: 2
- Photo upload failure tracking with per-photo error handling
</verification>

<success_criteria>
- Invalid daily rate format shows Zod validation error
- Implausible mileage (>10000 over handover) shows warning
- App startup with valid token + no network keeps user logged in
- Return submit retries up to 2 times
- Photo upload failures tracked individually
</success_criteria>

<output>
After completion, create `.planning/phases/23-mobile-quality-ux/23-02-SUMMARY.md`
</output>
