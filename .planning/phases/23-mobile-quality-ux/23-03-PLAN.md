---
phase: 23-mobile-quality-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/lib/constants.ts
  - apps/mobile/src/components/SignatureScreen.tsx
  - apps/mobile/src/components/OfflineBanner.tsx
  - apps/mobile/src/i18n/pl.json
  - apps/mobile/app/(tabs)/new-rental/dates.tsx
  - apps/mobile/app/(tabs)/new-rental/vehicle.tsx
  - apps/mobile/app/(tabs)/new-rental/contract.tsx
  - apps/mobile/app/(tabs)/new-rental/photos.tsx
  - apps/mobile/app/return/mileage.tsx
  - apps/mobile/app/return/checklist.tsx
  - apps/mobile/app/return/notes.tsx
  - apps/mobile/app/return/confirm.tsx
  - apps/mobile/app/return/[rentalId].tsx
  - apps/mobile/app/(tabs)/rentals/[id].tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/rentals/index.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
autonomous: true
requirements:
  - MUX-01
  - MUX-02
  - MUX-03
  - MUX-04
  - MUX-05
  - MUX-06
  - MUX-07

must_haves:
  truths:
    - "Bottom bar buttons render correctly on notched devices -- no overlap with home indicator"
    - "Tapping confirm on empty signature canvas shows a toast, not silence"
    - "Checklist labels display correct Polish diacritics (Wnetrze -> Wnętrze)"
    - "WIZARD_LABELS is defined once in constants and imported, not duplicated per step file"
    - "VAT rate 23 is a named constant DEFAULT_VAT_RATE, not hardcoded in 4 places"
    - "Magic number 86400000 is replaced with ONE_DAY_MS constant"
    - "OfflineBanner does not overlap the status bar on Android"
  artifacts:
    - path: "apps/mobile/src/lib/constants.ts"
      provides: "RENTAL_WIZARD_LABELS, DEFAULT_VAT_RATE, ONE_DAY_MS, UPCOMING_RETURN_THRESHOLD_DAYS constants"
      contains: "DEFAULT_VAT_RATE"
    - path: "apps/mobile/src/components/SignatureScreen.tsx"
      provides: "Toast on empty canvas confirm"
      contains: "Toast.show"
    - path: "apps/mobile/src/components/OfflineBanner.tsx"
      provides: "SafeAreaView wrapping for status bar"
      contains: "useSafeAreaInsets"
  key_links:
    - from: "all bottom-bar screens"
      to: "react-native-safe-area-context"
      via: "useSafeAreaInsets().bottom for paddingBottom"
      pattern: "useSafeAreaInsets"
    - from: "all wizard step files"
      to: "apps/mobile/src/lib/constants.ts"
      via: "import RENTAL_WIZARD_LABELS"
      pattern: "RENTAL_WIZARD_LABELS"
---

<objective>
Fix UX polish issues: safe area insets on bottom bars, empty signature feedback, Polish diacritics, and extract duplicated constants to a single source of truth.

Purpose: Bottom bars are clipped by the iOS home indicator (hardcoded paddingBottom: 32 instead of safe area insets). Empty signature confirmation does nothing (confusing). Checklist labels have missing diacritics. WIZARD_LABELS and VAT rate are copy-pasted across files.

Output: All bottom bars use safe area insets, SignatureScreen shows toast on empty, Polish text corrected, constants extracted.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/mobile/src/lib/constants.ts
@apps/mobile/src/components/SignatureScreen.tsx
@apps/mobile/src/components/OfflineBanner.tsx

<interfaces>
From react-native-safe-area-context:
```typescript
import { useSafeAreaInsets } from 'react-native-safe-area-context';
// const insets = useSafeAreaInsets();
// insets.bottom -- number of points for bottom safe area
```

Files with hardcoded paddingBottom: 32 (all need safe area fix):
- app/(tabs)/new-rental/dates.tsx
- app/(tabs)/new-rental/vehicle.tsx
- app/(tabs)/new-rental/contract.tsx
- app/(tabs)/new-rental/photos.tsx
- app/return/mileage.tsx
- app/return/checklist.tsx
- app/return/notes.tsx
- app/return/confirm.tsx
- app/return/[rentalId].tsx
- app/(tabs)/rentals/[id].tsx
- app/(tabs)/index.tsx
- app/(tabs)/rentals/index.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract constants and fix Polish diacritics</name>
  <read_first>
    - apps/mobile/src/lib/constants.ts
    - apps/mobile/src/i18n/pl.json
    - apps/mobile/app/(tabs)/new-rental/index.tsx (WIZARD_LABELS line 22)
    - apps/mobile/app/(tabs)/new-rental/dates.tsx (WIZARD_LABELS line 18, VAT 1.23 line 61, 86400000 line 36/59)
    - apps/mobile/app/(tabs)/new-rental/contract.tsx (WIZARD_LABELS, VAT)
    - apps/mobile/app/(tabs)/new-rental/vehicle.tsx (WIZARD_LABELS)
    - apps/mobile/app/(tabs)/new-rental/photos.tsx (WIZARD_LABELS)
    - apps/mobile/app/(tabs)/new-rental/signatures.tsx (vatRate: 23)
    - apps/mobile/src/lib/format.ts (vatRate = 23 default)
    - apps/mobile/app/(tabs)/index.tsx (magic number 3 for upcoming returns threshold)
  </read_first>
  <files>
    apps/mobile/src/lib/constants.ts,
    apps/mobile/src/i18n/pl.json,
    apps/mobile/app/(tabs)/new-rental/index.tsx,
    apps/mobile/app/(tabs)/new-rental/dates.tsx,
    apps/mobile/app/(tabs)/new-rental/contract.tsx,
    apps/mobile/app/(tabs)/new-rental/vehicle.tsx,
    apps/mobile/app/(tabs)/new-rental/photos.tsx,
    apps/mobile/app/(tabs)/new-rental/signatures.tsx,
    apps/mobile/src/lib/format.ts,
    apps/mobile/app/(tabs)/index.tsx
  </files>
  <action>
    1. In constants.ts, add these new exports:
       ```typescript
       export const RENTAL_WIZARD_LABELS = ['Klient', 'Pojazd', 'Daty', 'Umowa', 'Zdjęcia', 'Podpisy'] as const;
       export const DEFAULT_VAT_RATE = 23;
       export const VAT_MULTIPLIER = 1 + DEFAULT_VAT_RATE / 100; // 1.23
       export const ONE_DAY_MS = 86_400_000;
       export const UPCOMING_RETURN_THRESHOLD_DAYS = 3;
       ```

    2. Fix Polish diacritics in CHECKLIST_ITEMS in constants.ts:
       - 'Wnetrze' -> 'Wnętrze'
       - 'Oswietlenie' -> 'Oświetlenie'
       - 'Czystosc' -> 'Czystość'

    3. In pl.json, scan for missing diacritics and fix the most visible ones:
       - "Brak polaczenia" -> "Brak połączenia"
       - "Szukaj po nazwisku, telefonie lub PESEL..." -- this is correct as-is for placeholder text
       - Fix any other obvious diacritics issues found (ą, ę, ó, ś, ć, ź, ż, ł, ń)

    4. In each wizard step file (new-rental/index.tsx, vehicle.tsx, dates.tsx, contract.tsx, photos.tsx):
       - Remove the local `const WIZARD_LABELS = [...]` definition
       - Add `import { RENTAL_WIZARD_LABELS } from '@/lib/constants'`
       - Replace `labels={WIZARD_LABELS}` with `labels={RENTAL_WIZARD_LABELS}`

    5. Replace VAT hardcoding:
       - In dates.tsx: Replace `1.23` with `VAT_MULTIPLIER` and `86400000` with `ONE_DAY_MS` (import from constants)
       - In contract.tsx: Replace `1.23` with `VAT_MULTIPLIER` (import from constants)
       - In signatures.tsx: Replace `vatRate: 23` with `vatRate: DEFAULT_VAT_RATE` (import from constants)
       - In format.ts: Replace `vatRate = 23` default with `vatRate = DEFAULT_VAT_RATE` (import from constants)

    6. In index.tsx (dashboard): Replace the magic number `3` for upcoming returns threshold with `UPCOMING_RETURN_THRESHOLD_DAYS` (import from constants)
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RENTAL_WIZARD_LABELS" apps/mobile/src/lib/constants.ts (wizard labels constant exists)
    - grep -q "DEFAULT_VAT_RATE" apps/mobile/src/lib/constants.ts (VAT constant exists)
    - grep -q "ONE_DAY_MS" apps/mobile/src/lib/constants.ts (time constant exists)
    - grep -q "Wnętrze" apps/mobile/src/lib/constants.ts (diacritics fixed)
    - grep -q "Oświetlenie" apps/mobile/src/lib/constants.ts (diacritics fixed)
    - grep -q "Czystość" apps/mobile/src/lib/constants.ts (diacritics fixed)
    - grep -q "RENTAL_WIZARD_LABELS" apps/mobile/app/\(tabs\)/new-rental/dates.tsx (imports shared constant)
    - grep -q "DEFAULT_VAT_RATE" apps/mobile/app/\(tabs\)/new-rental/signatures.tsx (uses VAT constant)
    - grep -q "UPCOMING_RETURN_THRESHOLD_DAYS" apps/mobile/app/\(tabs\)/index.tsx (dashboard uses constant)
  </acceptance_criteria>
  <done>
    - RENTAL_WIZARD_LABELS defined once in constants.ts, imported in all 5 wizard step files
    - DEFAULT_VAT_RATE = 23, VAT_MULTIPLIER = 1.23 used everywhere instead of hardcoded values
    - ONE_DAY_MS replaces 86400000 in dates.tsx
    - UPCOMING_RETURN_THRESHOLD_DAYS replaces magic 3 in dashboard
    - Polish diacritics corrected in checklist labels and i18n strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix safe area insets on bottom bars, empty signature toast, and OfflineBanner</name>
  <read_first>
    - apps/mobile/src/components/SignatureScreen.tsx
    - apps/mobile/src/components/OfflineBanner.tsx
    - apps/mobile/app/(tabs)/_layout.tsx
    - apps/mobile/app/(tabs)/new-rental/dates.tsx (bottomBar style example)
    - apps/mobile/app/return/confirm.tsx (bottomBar style example)
    - apps/mobile/app/(tabs)/rentals/[id].tsx (bottomBar style example)
  </read_first>
  <files>
    apps/mobile/src/components/SignatureScreen.tsx,
    apps/mobile/src/components/OfflineBanner.tsx,
    apps/mobile/app/(tabs)/_layout.tsx,
    apps/mobile/app/(tabs)/new-rental/dates.tsx,
    apps/mobile/app/(tabs)/new-rental/vehicle.tsx,
    apps/mobile/app/(tabs)/new-rental/contract.tsx,
    apps/mobile/app/(tabs)/new-rental/photos.tsx,
    apps/mobile/app/return/mileage.tsx,
    apps/mobile/app/return/checklist.tsx,
    apps/mobile/app/return/notes.tsx,
    apps/mobile/app/return/confirm.tsx,
    apps/mobile/app/return/[rentalId].tsx,
    apps/mobile/app/(tabs)/rentals/[id].tsx,
    apps/mobile/app/(tabs)/index.tsx,
    apps/mobile/app/(tabs)/rentals/index.tsx
  </files>
  <action>
    1. In SignatureScreen.tsx (MUX-02):
       - Import `Toast from 'react-native-toast-message'` and `{ useTranslation } from 'react-i18next'`
       - In `handleEmpty`, add: `Toast.show({ type: 'info', text1: 'Podpis jest pusty', text2: 'Narysuj podpis przed zatwierdzeniem' })`
       - Replace hardcoded button labels: "Wyczysc podpis" -> `t('signatures.clear')` and "Zatwierdz podpis" -> `t('signatures.confirm')` (keys already exist in pl.json)
       - Add `const { t } = useTranslation()` to the component

    2. In OfflineBanner.tsx (MUX-07):
       - Import `useSafeAreaInsets` from 'react-native-safe-area-context'
       - Use `const insets = useSafeAreaInsets()` inside the component
       - Add `paddingTop: insets.top` to the banner container style so it does not overlap the status bar on Android/notched devices

    3. In (tabs)/_layout.tsx (MUX-07 related):
       - Check if OfflineBanner is rendered here and ensure it is placed inside the SafeAreaProvider context (it should be already, since _layout.tsx is a child of the root layout)

    4. For ALL 12 files with `paddingBottom: 32` in their bottomBar style (MUX-01):
       Each file needs the same pattern change:
       - Import `useSafeAreaInsets` from 'react-native-safe-area-context'
       - Add `const insets = useSafeAreaInsets()` in the component
       - Remove `paddingBottom: 32` from the static StyleSheet bottomBar style
       - Apply paddingBottom dynamically on the View: `style={[s.bottomBar, { paddingBottom: Math.max(insets.bottom, 16) }]}`
       - The `Math.max(insets.bottom, 16)` ensures at least 16pt padding on devices without a home indicator

       Files to update:
       - app/(tabs)/new-rental/dates.tsx
       - app/(tabs)/new-rental/vehicle.tsx
       - app/(tabs)/new-rental/contract.tsx
       - app/(tabs)/new-rental/photos.tsx
       - app/return/mileage.tsx
       - app/return/checklist.tsx
       - app/return/notes.tsx
       - app/return/confirm.tsx
       - app/return/[rentalId].tsx
       - app/(tabs)/rentals/[id].tsx
       - app/(tabs)/index.tsx
       - app/(tabs)/rentals/index.tsx
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Toast.show" apps/mobile/src/components/SignatureScreen.tsx (toast on empty signature)
    - grep -q "useSafeAreaInsets" apps/mobile/src/components/OfflineBanner.tsx (safe area on offline banner)
    - grep -q "useSafeAreaInsets" apps/mobile/app/\(tabs\)/new-rental/dates.tsx (safe area on bottom bar)
    - grep -q "useSafeAreaInsets" apps/mobile/app/return/confirm.tsx (safe area on bottom bar)
    - grep -q "useSafeAreaInsets" apps/mobile/app/\(tabs\)/rentals/\[id\].tsx (safe area on bottom bar)
    - grep -q "insets.bottom" apps/mobile/app/\(tabs\)/index.tsx (dynamic bottom padding on dashboard)
    - grep -c "paddingBottom: 32" apps/mobile/app returns 0 (no more hardcoded paddingBottom: 32)
  </acceptance_criteria>
  <done>
    - All 12 bottom bar screens use useSafeAreaInsets().bottom instead of hardcoded paddingBottom: 32
    - SignatureScreen shows toast feedback when user confirms empty canvas
    - SignatureScreen button labels use i18n keys instead of hardcoded Polish
    - OfflineBanner uses safe area top inset to avoid status bar overlap
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `npx tsc --noEmit --project apps/mobile/tsconfig.json`
- `grep -r "paddingBottom: 32" apps/mobile/app/` returns no matches
- All wizard step files import RENTAL_WIZARD_LABELS from constants
- No hardcoded `1.23` or `86400000` in app/ source files
- Checklist labels have correct Polish diacritics
</verification>

<success_criteria>
- Zero hardcoded paddingBottom: 32 in app source (all use safe area insets)
- Empty signature shows toast (not silent)
- Polish diacritics correct in checklist items
- Constants extracted: RENTAL_WIZARD_LABELS, DEFAULT_VAT_RATE, ONE_DAY_MS, UPCOMING_RETURN_THRESHOLD_DAYS
- OfflineBanner respects status bar on Android
</success_criteria>

<output>
After completion, create `.planning/phases/23-mobile-quality-ux/23-03-SUMMARY.md`
</output>
