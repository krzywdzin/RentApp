---
phase: 27-android-apk-build-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/metro.config.js
  - .gitignore
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - ASSET-01
  - ASSET-02
  - ASSET-03

must_haves:
  truths:
    - "react-native-worklets is installed as a dependency in apps/mobile/package.json"
    - "metro.config.js preserves Expo default watchFolders while adding monorepo root"
    - ".expo/ directory is gitignored and untracked"
    - "ios/ directory is removed from git tracking"
    - "app.config.ts has correct Android package and adaptive icon config"
    - "eas.json preview profile produces APK"
    - "All required assets exist at correct dimensions"
  artifacts:
    - path: "apps/mobile/package.json"
      provides: "react-native-worklets dependency"
      contains: "react-native-worklets"
    - path: "apps/mobile/metro.config.js"
      provides: "Fixed monorepo Metro config"
      contains: "watchFolders"
    - path: ".gitignore"
      provides: "Ignores .expo/ and ios/ from tracking"
      contains: ".expo/"
  key_links:
    - from: "apps/mobile/package.json"
      to: "react-native-reanimated"
      via: "peer dependency react-native-worklets"
      pattern: "react-native-worklets"
    - from: "apps/mobile/metro.config.js"
      to: "expo/metro-config"
      via: "getDefaultConfig preserving defaults"
      pattern: "getDefaultConfig"
---

<objective>
Fix all EAS Build configuration issues preventing Android APK generation.

Purpose: The EAS Build fails with `EAS_BUILD_UNKNOWN_GRADLE_ERROR`. expo-doctor identified: missing `react-native-worklets` peer dependency (likely root cause), `.expo/` tracked in git, and metro.config.js overriding Expo default watchFolders. This plan fixes all issues.
Output: Clean build configuration ready for EAS Build trigger.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-android-apk-build-fix/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install missing dependency and fix Metro config</name>
  <files>apps/mobile/package.json, apps/mobile/metro.config.js</files>
  <read_first>
    - apps/mobile/package.json
    - apps/mobile/metro.config.js
    - apps/mobile/app.config.ts
    - apps/mobile/eas.json
  </read_first>
  <action>
1. Install the missing peer dependency required by react-native-reanimated:
   ```
   cd apps/mobile && npx expo install react-native-worklets
   ```
   This MUST use `npx expo install` (not npm/pnpm) to get the version compatible with Expo SDK 54.

2. Fix metro.config.js to merge monorepo watchFolders with Expo defaults instead of overriding them. Replace the current config with:
   ```javascript
   const { getDefaultConfig } = require('expo/metro-config');
   const path = require('path');

   const projectRoot = __dirname;
   const monorepoRoot = path.resolve(projectRoot, '../..');

   const config = getDefaultConfig(projectRoot);

   // Monorepo support: add monorepo root to default watchFolders (instead of overriding)
   config.watchFolders = [...(config.watchFolders || []), monorepoRoot];

   // Resolve modules from both local and root node_modules
   config.resolver.nodeModulesPaths = [
     path.resolve(projectRoot, 'node_modules'),
     path.resolve(monorepoRoot, 'node_modules'),
   ];

   module.exports = config;
   ```
   The key change: spread existing `config.watchFolders` before adding `monorepoRoot`, preserving Expo defaults.

3. Verify app.config.ts already has correct config (no changes expected):
   - android.package = 'pl.kitek.rental'
   - android.adaptiveIcon with foregroundImage and backgroundColor
   - splash config with image, resizeMode, backgroundColor
   - icon pointing to ./assets/icon.png

4. Verify eas.json preview profile already has `buildType: "apk"` (no changes expected).

5. Verify assets exist at correct dimensions (no changes expected):
   - assets/icon.png (1024x1024)
   - assets/adaptive-icon.png (1024x1024)
   - assets/splash.png (1284x2778)
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/mobile && grep -q "react-native-worklets" package.json && echo "PASS: worklets dep" || echo "FAIL: worklets dep"; grep -q 'config.watchFolders || \[\]' metro.config.js && echo "PASS: metro spread" || echo "FAIL: metro spread"; grep -q "pl.kitek.rental" app.config.ts && echo "PASS: package name"; grep -q '"buildType": "apk"' eas.json && echo "PASS: apk build type"; ls assets/icon.png assets/adaptive-icon.png assets/splash.png && echo "PASS: assets exist"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "react-native-worklets" apps/mobile/package.json` returns a match
    - `grep "config.watchFolders || " apps/mobile/metro.config.js` returns a match (spread syntax preserving defaults)
    - `grep "pl.kitek.rental" apps/mobile/app.config.ts` returns matches for both ios and android
    - `grep "buildType.*apk" apps/mobile/eas.json` returns a match
    - `ls apps/mobile/assets/{icon,adaptive-icon,splash}.png` succeeds (all 3 exist)
  </acceptance_criteria>
  <done>react-native-worklets installed, metro.config.js preserves default watchFolders, app.config.ts and eas.json verified correct, all assets verified present.</done>
</task>

<task type="auto">
  <name>Task 2: Clean git tracking of generated directories</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore
  </read_first>
  <action>
1. Add `.expo/` and `apps/mobile/ios/` entries to the ROOT `.gitignore` (there is no apps/mobile/.gitignore):
   ```
   # Expo generated
   .expo/

   # iOS prebuild (non-CNG, regenerated)
   apps/mobile/ios/
   ```
   Add these entries at the end of the existing .gitignore file.

2. Remove the tracked `.expo/` files from git index (keep on disk):
   ```
   git rm -r --cached apps/mobile/.expo/
   ```

3. Remove the tracked `ios/` files from git index (keep on disk):
   ```
   git rm -r --cached apps/mobile/ios/
   ```

4. Verify no .expo/ or ios/ files appear in `git ls-files`:
   ```
   git ls-files apps/mobile/.expo/ apps/mobile/ios/
   ```
   Should return empty output.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -q ".expo/" .gitignore && echo "PASS: .expo in gitignore" || echo "FAIL"; grep -q "apps/mobile/ios/" .gitignore && echo "PASS: ios in gitignore" || echo "FAIL"; test -z "$(git ls-files apps/mobile/.expo/)" && echo "PASS: .expo untracked" || echo "FAIL: .expo still tracked"; test -z "$(git ls-files apps/mobile/ios/)" && echo "PASS: ios untracked" || echo "FAIL: ios still tracked"</automated>
  </verify>
  <acceptance_criteria>
    - `grep ".expo/" .gitignore` returns a match
    - `grep "apps/mobile/ios/" .gitignore` returns a match
    - `git ls-files apps/mobile/.expo/` returns empty
    - `git ls-files apps/mobile/ios/` returns empty
  </acceptance_criteria>
  <done>.expo/ and ios/ directories gitignored and removed from git tracking. Files preserved on disk.</done>
</task>

</tasks>

<verification>
1. `react-native-worklets` appears in apps/mobile/package.json dependencies
2. metro.config.js spreads default watchFolders instead of overriding
3. No .expo/ or ios/ files tracked in git
4. .gitignore contains both new entries
5. app.config.ts, eas.json, and assets all verified correct
</verification>

<success_criteria>
All expo-doctor findings addressed (except non-CNG warning which is resolved by removing ios/ from tracking). Build configuration ready for EAS Build trigger.
</success_criteria>

<output>
After completion, create `.planning/phases/27-android-apk-build-fix/27-01-SUMMARY.md`
</output>
