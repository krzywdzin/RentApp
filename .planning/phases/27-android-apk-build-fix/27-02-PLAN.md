---
phase: 27-android-apk-build-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified: []
autonomous: false
requirements:
  - VERIFY-01
  - VERIFY-02

must_haves:
  truths:
    - "EAS Build completes without Gradle errors"
    - "Built APK is downloadable from EAS"
    - "APK installs and launches on Android device"
  artifacts: []
  key_links:
    - from: "eas build command"
      to: "EAS Build service"
      via: "CLI trigger"
      pattern: "eas build --platform android --profile preview"
---

<objective>
Trigger EAS Build for Android and verify the APK builds successfully.

Purpose: After fixing all configuration issues in Plan 01, this plan triggers the actual build on EAS servers and verifies the resulting APK works.
Output: Working Android APK accessible from EAS Build.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-android-apk-build-fix/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run expo-doctor and trigger EAS Build</name>
  <files></files>
  <read_first>
    - apps/mobile/package.json
    - apps/mobile/app.config.ts
    - apps/mobile/eas.json
    - apps/mobile/metro.config.js
  </read_first>
  <action>
1. Run expo-doctor to verify all issues from Plan 01 are resolved:
   ```
   cd apps/mobile && npx expo-doctor
   ```
   Expected: No failures related to react-native-worklets, .expo/, or watchFolders. The non-CNG warning for ios/ may still appear if the directory exists on disk but is fine since it is gitignored.

2. Commit all changes from Plan 01 if not already committed (dependency install, metro fix, gitignore updates, removed tracked files).

3. Trigger the EAS Build:
   ```
   cd apps/mobile && eas build --platform android --profile preview --non-interactive
   ```
   Use `--non-interactive` to avoid prompts. This will submit the build to EAS servers.

4. Monitor build progress. The build URL will be printed — note it for the checkpoint task. If the build fails, read the error logs and diagnose. Common issues:
   - If Gradle still fails: check if react-native-worklets installed correctly (version compatibility with reanimated 4.1.7)
   - If JS bundle fails: check postinstall hook runs shared build
   - If asset error: verify asset paths in app.config.ts match actual file locations
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/mobile && npx expo-doctor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx expo-doctor` shows no critical failures (worklets, watchFolders, .expo issues resolved)
    - EAS Build command exits with success or provides build URL
    - Build status on EAS dashboard shows "finished" (not "errored")
  </acceptance_criteria>
  <done>EAS Build triggered and completed successfully. Build URL available for APK download.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify APK installs on Android device</name>
  <files></files>
  <action>
Human verification checkpoint. Download and install the APK produced by Task 1 on an Android device. Verify app launches, login screen appears, and adaptive icon displays correctly.
  </action>
  <verify>User confirms APK installs and app launches correctly.</verify>
  <done>APK verified working on physical Android device.</done>
  <what-built>Android APK built via EAS Build with all configuration fixes applied (react-native-worklets dependency, metro config fix, gitignore cleanup).</what-built>
  <how-to-verify>
    1. Download the APK from the EAS Build URL (printed in Task 1 output, also available at https://expo.dev/accounts/kitek/projects/kitek-rental/builds)
    2. Transfer APK to an Android device (USB, email, or direct download)
    3. Install the APK (may need to enable "Install from unknown sources")
    4. Launch the app — verify it opens without crashing
    5. Verify the login screen appears (confirms JS bundle loaded correctly)
    6. Verify the app icon shows correctly (adaptive icon on supported launchers)
  </how-to-verify>
  <resume-signal>Type "approved" if APK installs and launches correctly, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
1. expo-doctor passes with no critical issues
2. `eas build --platform android --profile preview` completes with status "finished"
3. APK downloads and installs on Android device
4. App launches without crashes and shows login screen
</verification>

<success_criteria>
Working Android APK that field employees can install on their Android devices. Build pipeline is fixed and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/27-android-apk-build-fix/27-02-SUMMARY.md`
</output>
