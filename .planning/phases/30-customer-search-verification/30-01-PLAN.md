---
phase: 30-customer-search-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/api/customers.api.ts
  - apps/api/src/customers/customers.service.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03

must_haves:
  truths:
    - "Phone search (9+ digits) finds the correct customer via API"
    - "PESEL search (exactly 11 digits) finds the correct customer via HMAC lookup"
    - "Last name search (text) finds matching customers case-insensitively"
    - "Mobile detectSearchParam correctly routes all three query types"
  artifacts:
    - path: "apps/api/src/customers/customers.service.ts"
      provides: "Search method handling phone, pesel, lastName"
      contains: "async search"
    - path: "apps/mobile/src/api/customers.api.ts"
      provides: "Query type detection and API call"
      contains: "detectSearchParam"
    - path: "apps/api/test/customers.e2e-spec.ts"
      provides: "E2E tests covering all three search types"
      contains: "search?pesel"
  key_links:
    - from: "apps/mobile/src/api/customers.api.ts"
      to: "/customers/search"
      via: "apiClient.get with params from detectSearchParam"
      pattern: "apiClient\\.get.*customers/search"
    - from: "apps/api/src/customers/customers.controller.ts"
      to: "apps/api/src/customers/customers.service.ts"
      via: "search method delegation"
      pattern: "customersService\\.search"
---

<objective>
Verify that customer search works end-to-end across all three query types (phone, PESEL, lastName) in the mobile app and API.

Purpose: This feature was already implemented. This plan runs existing e2e tests, verifies mobile query detection logic, and fixes any issues found (phone search uses exact match -- verify this is correct or add contains/startsWith if needed).
Output: Passing tests confirming all three search types work, plus any fixes applied.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files to understand the existing implementation:
@apps/mobile/src/api/customers.api.ts
@apps/api/src/customers/customers.service.ts
@apps/api/src/customers/customers.controller.ts
@apps/api/src/customers/dto/search-customer.dto.ts
@apps/api/test/customers.e2e-spec.ts

<interfaces>
<!-- Key types and contracts already in place -->

From packages/shared/src/types/customer.types.ts:
```typescript
export interface CustomerSearchResultDto {
  id: string;
  firstName: string;
  lastName: string;
  phone: string;
  email: string | null;
}
```

From apps/api/src/customers/dto/search-customer.dto.ts:
```typescript
export class SearchCustomerDto {
  @IsOptional() @IsString() lastName?: string;
  @IsOptional() @IsString() phone?: string;
  @IsOptional() @IsString() pesel?: string;
}
```

From apps/mobile/src/api/customers.api.ts:
```typescript
function detectSearchParam(query: string): { lastName?: string; phone?: string; pesel?: string }
// - 11 digits -> pesel
// - starts with + or 9+ digits -> phone
// - otherwise -> lastName
```

From apps/api/src/customers/customers.service.ts search method:
```typescript
// pesel: where.peselHmac = hmacIndex(dto.pesel)
// lastName: where.lastName = { contains: dto.lastName, mode: 'insensitive' }
// phone: where.phone = dto.phone  (exact match)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run e2e tests and verify all three search types pass</name>
  <files>apps/api/test/customers.e2e-spec.ts</files>
  <read_first>
    - apps/api/test/customers.e2e-spec.ts (search-related test cases at lines 310-404)
    - apps/api/src/customers/customers.service.ts (search method at line 183)
    - apps/mobile/src/api/customers.api.ts (detectSearchParam function)
  </read_first>
  <behavior>
    - E2E: GET /customers/search?pesel=VALID_PESEL returns matching customer (SRCH-02)
    - E2E: GET /customers/search?lastName=kowalski returns matching customer case-insensitively (SRCH-03)
    - E2E: GET /customers/search?phone=%2B48123456789 returns matching customer (SRCH-01)
    - E2E: GET /customers/search with no params returns 400
    - E2E: search results do NOT expose sensitive fields (pesel, idNumber, licenseNumber)
    - Mobile: detectSearchParam("44051401359") returns { pesel: "44051401359" } (11 digits)
    - Mobile: detectSearchParam("+48123456789") returns { phone: "+48123456789" } (starts with +)
    - Mobile: detectSearchParam("123456789") returns { phone: "123456789" } (9+ digits, not 11)
    - Mobile: detectSearchParam("Kowalski") returns { lastName: "Kowalski" } (text)
  </behavior>
  <action>
    1. Run the existing e2e test suite for customer search:
       `cd apps/api && npx jest --config test/jest-e2e.json --testPathPattern customers.e2e-spec --no-coverage`
       All 6 search-related tests should pass (lines 310-404 in customers.e2e-spec.ts).

    2. Verify mobile detectSearchParam logic covers edge cases. Write a small unit test file `apps/mobile/__tests__/detect-search-param.test.ts` that imports detectSearchParam (will need to export it) and tests:
       - "44051401359" (11 digits) -> { pesel: "44051401359" }
       - "+48 123 456 789" (with spaces, starts with +) -> { phone: "+48 123 456 789" }
       - "123456789" (9 digits, not 11) -> { phone: "123456789" }
       - "12345678" (8 digits, too short for phone) -> { lastName: "12345678" }
       - "Kowalski" (text) -> { lastName: "Kowalski" }
       - "44051401359" with spaces "440 514 013 59" -> { pesel: "44051401359" }

    3. Export detectSearchParam from customers.api.ts (add `export` keyword) so the test can import it.

    4. If any test fails, fix the underlying code. Known consideration: phone search in API uses exact match (`where.phone = dto.phone`). This is correct for the use case -- users search with the full stored phone number. If the e2e test passes with exact match, keep it as-is. If users need partial phone search, add `contains` -- but only if tests indicate a problem.

    5. Run both test suites to confirm everything passes.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/api && npx jest --config test/jest-e2e.json --testPathPattern customers.e2e-spec --no-coverage 2>&1 | tail -20</automated>
    <automated>cd /Users/antonio/Documents/RentApp && npx jest --project apps/mobile --testPathPattern detect-search-param --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All e2e search tests pass (phone, PESEL, lastName, no-params-400, no-sensitive-fields, employee-search)
    - Unit tests for detectSearchParam pass covering all edge cases (11 digits -> pesel, 9+ digits -> phone, text -> lastName, formatted input with spaces/dashes)
    - detectSearchParam is exported from customers.api.ts for testability
  </acceptance_criteria>
  <done>All search types verified working end-to-end with passing tests. SRCH-01 (phone), SRCH-02 (PESEL), SRCH-03 (lastName) confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify phone search handles format variations</name>
  <files>apps/api/src/customers/customers.service.ts, apps/api/test/customers.e2e-spec.ts</files>
  <read_first>
    - apps/api/src/customers/customers.service.ts (search method, line 183-212)
    - apps/api/test/customers.e2e-spec.ts (phone search test at line 343)
  </read_first>
  <action>
    1. Review the phone search logic: currently uses exact match `where.phone = dto.phone`. This means searching "+48123456789" only matches if the stored phone is exactly "+48123456789".

    2. Add a phone search normalization: strip spaces and dashes from the search input before matching, and use `contains` for the phone field so that searching "123456789" finds "+48123456789". Update the service:
       ```typescript
       if (dto.phone) {
         const normalized = dto.phone.replace(/[\s\-()]/g, '');
         where.phone = { contains: normalized };
       }
       ```

    3. Add an e2e test case to customers.e2e-spec.ts:
       - Search with partial phone "123456789" (without +48 prefix) should find the customer whose phone is "+48123456789"
       - Search with phone "48123456789" should also find it

    4. Run e2e tests to confirm the new behavior works alongside existing tests.

    5. If the `contains` approach causes issues with Prisma phone field type, fall back to `startsWith` on the normalized value. The key requirement is that searching "123456789" finds "+48123456789".
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/api && npx jest --config test/jest-e2e.json --testPathPattern customers.e2e-spec --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Phone search with partial number (e.g., "123456789") finds customer stored as "+48123456789"
    - Existing exact phone search test still passes
    - All other search tests remain passing
  </acceptance_criteria>
  <done>Phone search handles format variations (with/without prefix, with spaces). All e2e tests pass.</done>
</task>

</tasks>

<verification>
Run the full customer e2e test suite:
```bash
cd apps/api && npx jest --config test/jest-e2e.json --testPathPattern customers.e2e-spec --no-coverage
```
All tests must pass including the new phone format variation test.

Run the mobile unit test for detectSearchParam:
```bash
cd apps/mobile && npx jest --testPathPattern detect-search-param --no-coverage
```
All detection edge cases must pass.
</verification>

<success_criteria>
1. E2E tests confirm phone search works end-to-end (SRCH-01)
2. E2E tests confirm PESEL search works end-to-end (SRCH-02)
3. E2E tests confirm lastName search works end-to-end (SRCH-03)
4. Mobile detectSearchParam correctly routes all query types
5. Phone search handles format variations (partial numbers, spaces)
6. No regressions in existing customer tests
</success_criteria>

<output>
After completion, create `.planning/phases/30-customer-search-verification/30-01-SUMMARY.md`
</output>
