---
phase: 25-infrastructure-cicd
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/web/package.json
  - apps/mobile/package.json
  - apps/mobile/jest.config.js
  - packages/shared/package.json
  - .env.example
  - apps/api/.env.example
  - apps/web/.env.example
  - docker-compose.yml
  - turbo.json
autonomous: true
requirements: [ICONF-01, ICONF-02, ICONF-03, ICONF-04, ICONF-05, ICONF-06, ICONF-07, ICONF-09]

must_haves:
  truths:
    - "bullmq is not in API dependencies"
    - "@nestjs/cli is not in web devDependencies"
    - "@gorhom/bottom-sheet is not in mobile dependencies"
    - "tailwindcss is not in mobile devDependencies"
    - "zod and typescript versions are aligned across all packages"
    - "Root .env.example is the single source of truth with CORS_ORIGINS and APP_URL documented"
    - "apps/web has .env.example documenting API_URL"
    - "Docker images for MinIO and Mailpit are pinned to specific versions"
    - "MinIO has a health check in docker-compose.yml"
    - "turbo.json has outputs for lint/test and a typecheck task"
  artifacts:
    - path: "apps/api/package.json"
      provides: "API deps without bullmq"
    - path: "apps/web/package.json"
      provides: "Web deps without @nestjs/cli, aligned versions"
    - path: "apps/mobile/package.json"
      provides: "Mobile deps without bottom-sheet/tailwind, aligned versions"
    - path: ".env.example"
      provides: "Consolidated env documentation with CORS_ORIGINS"
      contains: "CORS_ORIGINS"
    - path: "apps/web/.env.example"
      provides: "Web env documentation"
      contains: "API_URL"
    - path: "docker-compose.yml"
      provides: "Pinned images and MinIO health check"
      contains: "healthcheck"
    - path: "turbo.json"
      provides: "Turbo config with outputs and typecheck task"
      contains: "typecheck"
  key_links:
    - from: "turbo.json"
      to: "typecheck task"
      via: "tasks"
      pattern: "typecheck"
    - from: "docker-compose.yml"
      to: "MinIO health"
      via: "healthcheck"
      pattern: "minio/health/live"
---

<objective>
Clean up dependencies, align versions, consolidate env documentation, fix Docker compose, and update turbo config.

Purpose: Removes dead dependencies (bullmq, @nestjs/cli in web, @gorhom/bottom-sheet, tailwindcss in mobile), aligns zod/typescript versions, creates web .env.example, consolidates root/API env examples, pins Docker images, adds MinIO health check, and configures turbo properly.
Output: Cleaned package.json files, consolidated env docs, improved docker-compose, proper turbo.json.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/INFRA-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused dependencies and align versions across packages</name>
  <files>apps/api/package.json, apps/web/package.json, apps/mobile/package.json, apps/mobile/jest.config.js, packages/shared/package.json</files>
  <read_first>
    - apps/api/package.json
    - apps/web/package.json
    - apps/mobile/package.json
    - apps/mobile/jest.config.js
    - packages/shared/package.json
  </read_first>
  <action>
1. **apps/api/package.json**: Remove `"bullmq": "^5.0.0"` from dependencies. It is never imported -- only `bull` and `@nestjs/bull` are used.

2. **apps/web/package.json**:
   - Remove `"@nestjs/cli": "^11.0.0"` from devDependencies (copy-paste error from API).
   - Change `"typescript": "^5.8.0"` to `"typescript": "^5.9.0"` to align with rest of monorepo.

3. **apps/mobile/package.json**:
   - Remove `"@gorhom/bottom-sheet": "^5.2.8"` from dependencies (not imported in source code).
   - Remove `"tailwindcss": "^3.4.0"` from devDependencies (NativeWind disabled for SDK 54).
   - Change `"zod": "^3.24.0"` to `"zod": "^3.25.76"` to align with web.

4. **packages/shared/package.json**: Change `"zod": "^3.24.0"` to `"zod": "^3.25.76"` to align with web.

5. **apps/mobile/jest.config.js**: Remove `@gorhom/bottom-sheet` from the `transformIgnorePatterns` regex since the package is being removed. The regex entry `@gorhom/bottom-sheet|` should be removed (including the trailing pipe).

6. Run `pnpm install` after all package.json changes to update the lockfile.

Do NOT run pnpm install if not on CI -- just update the files. The lockfile will be updated when pnpm install runs next.
  </action>
  <verify>
    <automated>! grep -q 'bullmq' apps/api/package.json && ! grep -q '@nestjs/cli' apps/web/package.json && ! grep -q '@gorhom/bottom-sheet' apps/mobile/package.json && ! grep -q 'tailwindcss' apps/mobile/package.json && grep -q '"zod": "\^3.25.76"' apps/mobile/package.json && grep -q '"typescript": "\^5.9.0"' apps/web/package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'bullmq' apps/api/package.json returns NO match
    - grep '@nestjs/cli' apps/web/package.json returns NO match
    - grep '@gorhom/bottom-sheet' apps/mobile/package.json returns NO match
    - grep 'tailwindcss' apps/mobile/package.json returns NO match
    - grep '"zod": "\^3.25.76"' apps/mobile/package.json returns a match
    - grep '"zod": "\^3.25.76"' packages/shared/package.json returns a match
    - grep '"typescript": "\^5.9.0"' apps/web/package.json returns a match
    - grep '@gorhom/bottom-sheet' apps/mobile/jest.config.js returns NO match
  </acceptance_criteria>
  <done>Unused deps removed (bullmq, @nestjs/cli in web, @gorhom/bottom-sheet, tailwindcss in mobile), zod aligned to ^3.25.76, typescript aligned to ^5.9.0</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate env examples, create web .env.example, fix Docker and turbo</name>
  <files>.env.example, apps/api/.env.example, apps/web/.env.example, docker-compose.yml, turbo.json</files>
  <read_first>
    - .env.example
    - apps/api/.env.example
    - docker-compose.yml
    - turbo.json
  </read_first>
  <action>
1. **Consolidate .env.example files (ICONF-05, ICONF-03)**:
   - The root `.env.example` becomes the single source of truth for API env vars.
   - Update root `.env.example` to ensure it includes:
     - `CORS_ORIGINS=http://localhost:3001` with comment `# Comma-separated origins (production: https://app.kitek.pl)`
     - `APP_URL=http://localhost:3000` (already present)
     - `COMPANY_PHONE` (already present)
     - `S3_REGION=auto` (use the API version, not `us-east-1`)
   - Replace `apps/api/.env.example` content with a single-line reference:
```
# See root .env.example for all API environment variables
# Copy root .env.example to apps/api/.env for local development
```
   This eliminates the drift between root and API .env.example files.

2. **Create `apps/web/.env.example` (ICONF-04)**:
```
# Backend API URL (required)
API_URL=http://localhost:3000
```

3. **Pin Docker images in `docker-compose.yml` (ICONF-06)** and **add MinIO health check (ICONF-07)**:
   - Change `minio/minio:latest` to `minio/minio:RELEASE.2025-01-20T14-49-07Z`
   - Change `axllent/mailpit:latest` to `axllent/mailpit:v1.21`
   - Add healthcheck to minio service:
```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  interval: 10s
  timeout: 5s
  retries: 5
```

4. **Update `turbo.json` (ICONF-09)** -- add outputs for lint/test and a typecheck task:
```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**", ".next/**"] },
    "dev": { "cache": false, "persistent": true },
    "lint": { "dependsOn": ["^build"], "outputs": [] },
    "test": { "dependsOn": ["^build"], "outputs": ["coverage/**"] },
    "typecheck": { "dependsOn": ["^build"], "outputs": [] }
  }
}
```
Key changes: `lint` gets `"outputs": []`, `test` gets `"outputs": ["coverage/**"]`, new `typecheck` task with `"outputs": []`, `build` adds `.next/**` to outputs.
  </action>
  <verify>
    <automated>grep -q 'CORS_ORIGINS' .env.example && test -f apps/web/.env.example && grep -q 'API_URL' apps/web/.env.example && grep -q 'RELEASE' docker-compose.yml && grep -q 'minio/health/live' docker-compose.yml && grep -q 'typecheck' turbo.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'CORS_ORIGINS' .env.example returns a match
    - grep 'S3_REGION=auto' .env.example returns a match
    - apps/web/.env.example exists with API_URL
    - apps/api/.env.example contains reference to root (not full duplication)
    - grep 'RELEASE' docker-compose.yml returns a match (pinned MinIO)
    - grep 'mailpit:v' docker-compose.yml returns a match (pinned Mailpit)
    - grep 'minio/health/live' docker-compose.yml returns a match
    - grep 'typecheck' turbo.json returns a match
    - grep '"outputs": \[\]' turbo.json returns matches for lint and typecheck
    - grep 'coverage' turbo.json returns a match for test outputs
  </acceptance_criteria>
  <done>Env examples consolidated (root is source of truth, API references root, web has own .env.example). Docker images pinned, MinIO has health check. Turbo.json has outputs and typecheck task.</done>
</task>

</tasks>

<verification>
- `grep 'bullmq' apps/api/package.json` returns nothing
- `grep '@nestjs/cli' apps/web/package.json` returns nothing
- `grep '@gorhom/bottom-sheet' apps/mobile/package.json` returns nothing
- `grep 'tailwindcss' apps/mobile/package.json` returns nothing
- `grep 'CORS_ORIGINS' .env.example` shows CORS config documented
- `test -f apps/web/.env.example` confirms web env example exists
- `grep 'minio/health/live' docker-compose.yml` shows MinIO health check
- `grep 'typecheck' turbo.json` shows typecheck task
</verification>

<success_criteria>
All unused dependencies removed, versions aligned, env documentation consolidated with no drift, Docker images pinned with MinIO health check, turbo.json properly configured with outputs and typecheck task.
</success_criteria>

<output>
After completion, create `.planning/phases/25-infrastructure-cicd/25-03-SUMMARY.md`
</output>
