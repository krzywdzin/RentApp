---
phase: 06-mobile-app
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/mobile/app/login.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/src/components/AppButton.tsx
  - apps/mobile/src/components/AppInput.tsx
  - apps/mobile/src/components/AppCard.tsx
  - apps/mobile/src/components/StatusBadge.tsx
  - apps/mobile/src/components/WizardStepper.tsx
  - apps/mobile/src/components/SearchBar.tsx
  - apps/mobile/src/components/EmptyState.tsx
  - apps/mobile/src/components/OfflineBanner.tsx
  - apps/mobile/src/components/LoadingSkeleton.tsx
  - apps/mobile/src/components/ConfirmationDialog.tsx
autonomous: true
requirements:
  - MOB-01
must_haves:
  truths:
    - "Employee can log in with email and password"
    - "Employee sees 4 bottom tabs after login"
    - "Biometric re-login works when enabled"
    - "Profile tab shows user info with logout"
    - "Shared components render correctly with NativeWind styles"
  artifacts:
    - path: "apps/mobile/app/login.tsx"
      provides: "Login screen with email/password form"
      contains: "useLogin"
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Bottom tab navigation with 4 tabs"
      contains: "Tabs.Screen"
    - path: "apps/mobile/src/components/AppButton.tsx"
      provides: "Primary/Secondary/Destructive button component"
      contains: "className"
  key_links:
    - from: "apps/mobile/app/login.tsx"
      to: "apps/mobile/src/hooks/use-auth.ts"
      via: "useLogin mutation hook"
      pattern: "useLogin"
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "lucide-react-native"
      via: "Tab bar icons"
      pattern: "lucide-react-native"
---

<objective>
Build the login screen, tab navigation shell, profile tab, and all shared UI components.

Purpose: Deliver the authentication flow and reusable component library so that Plans 03 and 04 can build feature screens without creating any base components.

Output: Working login -> tabs flow, profile tab with logout, and complete set of shared NativeWind components.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mobile-app/06-CONTEXT.md
@.planning/phases/06-mobile-app/06-UI-SPEC.md
@.planning/phases/06-mobile-app/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared UI component library</name>
  <files>
    apps/mobile/src/components/AppButton.tsx
    apps/mobile/src/components/AppInput.tsx
    apps/mobile/src/components/AppCard.tsx
    apps/mobile/src/components/StatusBadge.tsx
    apps/mobile/src/components/WizardStepper.tsx
    apps/mobile/src/components/SearchBar.tsx
    apps/mobile/src/components/EmptyState.tsx
    apps/mobile/src/components/OfflineBanner.tsx
    apps/mobile/src/components/LoadingSkeleton.tsx
    apps/mobile/src/components/ConfirmationDialog.tsx
  </files>
  <read_first>
    .planning/phases/06-mobile-app/06-UI-SPEC.md
    apps/mobile/src/i18n/pl.json
    apps/mobile/tailwind.config.js
    apps/mobile/src/lib/network.ts
  </read_first>
  <action>
    Create all shared components per UI-SPEC component inventory. All use NativeWind className prop for styling.

    1. **AppButton.tsx**: Props: { title, onPress, variant: "primary" | "secondary" | "destructive", disabled?, loading?, fullWidth? }
       - primary: bg-blue-600 text-white (accent)
       - secondary: border border-zinc-200 bg-white text-zinc-900
       - destructive: bg-red-600 text-white
       - disabled: bg-zinc-100 text-zinc-400
       - loading: ActivityIndicator replaces title text, button disabled
       - Min height 48px (touch target). rounded-xl. font-semibold text-base.

    2. **AppInput.tsx**: Props: { label, error?, leftIcon?, placeholder?, secureTextEntry?, ...TextInputProps }
       - Label: text-[13px] text-zinc-500 mb-1
       - Input: h-12 (48px) border border-zinc-200 rounded-xl px-4 text-base text-zinc-900
       - Focus: border-blue-600 ring
       - Error: border-red-600, error text below in text-red-600 text-[13px]
       - Left icon: positioned inside input left side

    3. **AppCard.tsx**: Props: { children, className?, onPress? }
       - bg-white border border-zinc-200 rounded-xl p-4
       - Optional Pressable wrapper when onPress provided
       - Shadow: shadow-sm on iOS, elevation-1 on Android

    4. **StatusBadge.tsx**: Props: { status: string }
       - Map rental statuses to colors: ACTIVE/RENTED -> bg-green-100 text-green-700, DRAFT -> bg-zinc-100 text-zinc-600, EXTENDED -> bg-amber-100 text-amber-700, RETURNED -> bg-zinc-200 text-zinc-600, OVERDUE -> bg-red-100 text-red-700
       - Pill shape: rounded-full px-3 py-1 text-[13px] font-medium
       - Polish labels: ACTIVE -> "Aktywny", DRAFT -> "Szkic", EXTENDED -> "Przedluzony", RETURNED -> "Zwrocony", OVERDUE -> "Przeterminowany"

    5. **WizardStepper.tsx**: Props: { currentStep: number, totalSteps: number, labels?: string[] }
       - Horizontal bar at top: h-1 bg-zinc-200 rounded-full, filled portion bg-blue-600
       - Step dots below: circles with numbers. Active = bg-blue-600 text-white, completed = bg-green-600 text-white (checkmark), pending = bg-zinc-200 text-zinc-500
       - 8px vertical margin above and below

    6. **SearchBar.tsx**: Props: { value, onChangeText, placeholder }
       - Full width with mx-4. h-12 rounded-xl bg-zinc-100 px-4
       - Search icon (lucide Search) on left in zinc-400
       - Clear button (X icon) on right when value is non-empty
       - Debounced onChange: 300ms debounce built into the component using useRef timer

    7. **EmptyState.tsx**: Props: { icon?: ReactNode, heading: string, body: string, action?: { label: string, onPress: () => void } }
       - Centered layout. Icon in zinc-300 at 48px. Heading text-lg font-semibold text-zinc-900. Body text-base text-zinc-500. Optional AppButton below.

    8. **OfflineBanner.tsx**: Uses useIsOffline hook from network.ts.
       - When offline: persistent banner below status bar. bg-amber-500 text-white px-4 py-2 text-sm.
       - Text from i18n: errors.offline
       - AnimatePresence or simple conditional render

    9. **LoadingSkeleton.tsx**: Props: { variant: "card" | "list-item" | "stat" | "text", count?: number }
       - Pulsing rectangles using Animated.View with opacity animation
       - card: rounded-xl h-24 w-full bg-zinc-200
       - list-item: h-16 w-full bg-zinc-200 rounded-lg
       - stat: h-20 w-28 bg-zinc-200 rounded-xl

    10. **ConfirmationDialog.tsx**: Props: { visible, title, body, confirmLabel, cancelLabel, onConfirm, onCancel, variant?: "destructive" | "default" }
        - React Native Modal with centered content
        - White card, rounded-2xl, p-6
        - Title: text-lg font-semibold. Body: text-base text-zinc-500
        - Two buttons at bottom: Cancel (secondary), Confirm (destructive or primary)
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "variant" apps/mobile/src/components/AppButton.tsx
    - grep -q "className" apps/mobile/src/components/AppButton.tsx
    - grep -q "error" apps/mobile/src/components/AppInput.tsx
    - grep -q "StatusBadge" apps/mobile/src/components/StatusBadge.tsx
    - grep -q "currentStep" apps/mobile/src/components/WizardStepper.tsx
    - grep -q "debounce\|setTimeout\|useRef" apps/mobile/src/components/SearchBar.tsx
    - grep -q "useIsOffline\|netinfo\|NetInfo" apps/mobile/src/components/OfflineBanner.tsx
    - test -f apps/mobile/src/components/EmptyState.tsx
    - test -f apps/mobile/src/components/LoadingSkeleton.tsx
    - test -f apps/mobile/src/components/ConfirmationDialog.tsx
  </acceptance_criteria>
  <done>All 10 shared components created with NativeWind styling matching UI-SPEC specifications. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build login screen, tab navigation shell, dashboard placeholder, and profile tab</name>
  <files>
    apps/mobile/app/login.tsx
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/app/(tabs)/profile.tsx
    apps/mobile/app/(tabs)/new-rental/_layout.tsx
    apps/mobile/app/(tabs)/rentals/_layout.tsx
  </files>
  <read_first>
    apps/mobile/src/hooks/use-auth.ts
    apps/mobile/src/stores/auth.store.ts
    apps/mobile/src/providers/AuthProvider.tsx
    apps/mobile/src/components/AppButton.tsx
    apps/mobile/src/components/AppInput.tsx
    apps/mobile/src/i18n/pl.json
    .planning/phases/06-mobile-app/06-UI-SPEC.md
  </read_first>
  <action>
    1. **app/login.tsx**: Login screen with:
       - KITEK branding at top (text logo or placeholder image)
       - Email input (AppInput, keyboard type "email-address", autoCapitalize "none")
       - Password input (AppInput, secureTextEntry)
       - "Zaloguj" button (AppButton primary, full width)
       - Form validation with react-hook-form + Zod LoginSchema from @rentapp/shared
       - On submit: call useLogin mutation. On success, AuthProvider handles navigation to tabs.
       - Error display: Toast with errors.loginFailed or errors.accountLocked per error type
       - Loading state: button shows spinner during login request
       - KeyboardAvoidingView for input positioning

    2. **app/(tabs)/_layout.tsx**: Bottom tab navigator per UI-SPEC and RESEARCH.md:
       - 4 Tabs.Screen entries:
         - "index" (Home/Pulpit): Home icon from lucide-react-native
         - "new-rental" (Nowy wynajem): PlusCircle icon
         - "rentals" (Wynajmy): List icon
         - "profile" (Profil): User icon
       - Tab bar styling: height 64, paddingBottom 8, activeTintColor #2563EB (blue-600), inactiveTintColor #71717A (zinc-500)
       - headerShown: false (screens manage own headers)
       - Tab labels from i18n translations (nav.*)
       - OfflineBanner rendered above tab content

    3. **app/(tabs)/index.tsx**: Dashboard placeholder screen:
       - Header: greeting from i18n "Czesc, {firstName}" using auth store user
       - Current date display
       - Placeholder text: "Dashboard coming in Plan 03"
       - This is a temporary placeholder -- full dashboard built in Plan 03

    4. **app/(tabs)/profile.tsx**: Complete profile/settings screen per UI-SPEC:
       - User info section: name (firstName + lastName), email, role badge (StatusBadge-like)
       - Biometric toggle: Switch component, reads/writes biometricEnabled from auth store. On enable, checks expo-local-authentication.hasHardwareAsync() and isEnrolledAsync(). If not available, show Toast error.
       - App version: from Constants.expoConfig.version
       - Logout button: AppButton destructive, triggers ConfirmationDialog with i18n confirm.logoutTitle/Body, on confirm calls useLogout mutation
       - Styling: sections with AppCard containers, gaps between sections

    5. **app/(tabs)/new-rental/_layout.tsx**: Stack layout placeholder for rental wizard (screens built in Plan 04):
       - Stack navigator with headerShown: false
       - Export default Stack component

    6. **app/(tabs)/rentals/_layout.tsx**: Stack layout for rentals tab (screens built in Plan 03):
       - Stack navigator with headerShown: false for index, headerShown: true for [id] detail
       - Export default Stack component
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useLogin\|LoginSchema" apps/mobile/app/login.tsx
    - grep -q "Tabs.Screen" apps/mobile/app/\(tabs\)/_layout.tsx
    - grep -q "Home.*PlusCircle\|lucide" apps/mobile/app/\(tabs\)/_layout.tsx
    - grep -q "biometric\|setBiometricEnabled" apps/mobile/app/\(tabs\)/profile.tsx
    - grep -q "useLogout\|logout" apps/mobile/app/\(tabs\)/profile.tsx
    - grep -q "ConfirmationDialog" apps/mobile/app/\(tabs\)/profile.tsx
    - test -f apps/mobile/app/\(tabs\)/new-rental/_layout.tsx
    - test -f apps/mobile/app/\(tabs\)/rentals/_layout.tsx
  </acceptance_criteria>
  <done>Login screen authenticates via API and stores tokens. Tab navigation shows 4 tabs with correct icons and Polish labels. Profile tab displays user info, biometric toggle, and logout with confirmation. Dashboard has temporary placeholder. Stack layouts ready for Plans 03 and 04.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` passes
- Login screen uses RHF + Zod validation + useLogin hook
- Tab bar has 4 tabs with lucide icons and Polish labels
- Profile tab has biometric toggle and logout with confirmation dialog
- All 10 shared components exist and export properly
</verification>

<success_criteria>
- Employee can see login form with email/password inputs and submit button
- Login form validates with Zod and calls API via TanStack Query mutation
- After login, 4-tab navigation is visible (Pulpit, Nowy wynajem, Wynajmy, Profil)
- Profile tab shows user info, biometric toggle, version, and logout
- All shared components (AppButton, AppInput, AppCard, StatusBadge, WizardStepper, SearchBar, EmptyState, OfflineBanner, LoadingSkeleton, ConfirmationDialog) exist and compile
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-app/06-02-SUMMARY.md`
</output>
