---
phase: 29-auth-overhaul-user-management
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - apps/mobile/app/login.tsx
  - apps/mobile/src/api/auth.api.ts
  - apps/mobile/src/hooks/use-auth.ts
autonomous: true
requirements: [AUTH-05]

must_haves:
  truths:
    - "Worker logs in to mobile app with username field labeled 'Nazwa uzytkownika'"
    - "Mobile app sends { login: username, password, deviceId, context: 'mobile' } to API"
    - "No email-related UI or validation in mobile login"
  artifacts:
    - path: "apps/mobile/app/login.tsx"
      provides: "Username-based mobile login form"
      contains: "Nazwa uzytkownika"
    - path: "apps/mobile/src/api/auth.api.ts"
      provides: "Auth API sending login field with mobile context"
      contains: "context"
  key_links:
    - from: "apps/mobile/app/login.tsx"
      to: "useLogin hook"
      via: "form submit with username"
      pattern: "username.*password"
    - from: "apps/mobile/src/api/auth.api.ts"
      to: "API /auth/login"
      via: "POST body with login + context"
      pattern: "login.*context.*mobile"
    - from: "apps/mobile/src/hooks/use-auth.ts"
      to: "authApi.login"
      via: "mutationFn call"
      pattern: "authApi\\.login"
---

<objective>
Mobile app login overhaul from email to username

Purpose: Switch mobile app login from email-based to username-based, sending the correct `login` field with `context: 'mobile'` so the API generates tokens signed with JWT_MOBILE_SECRET.

Output: Updated mobile login screen with username field and auth API calls using correct field names.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-auth-overhaul-user-management/29-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, the API accepts these -->
From apps/api/src/auth/dto/login.dto.ts (after Plan 01):
```typescript
export class LoginDto {
  @IsString() @MinLength(1) login!: string;
  @IsString() @MinLength(8) password!: string;
  @IsUUID() deviceId!: string;
  @IsOptional() @IsIn(['admin', 'mobile']) context?: 'admin' | 'mobile';
}
```

From apps/mobile/src/api/auth.api.ts (current):
```typescript
login: async (email: string, password: string, deviceId: string): Promise<TokenPairDto> => {
  const { data } = await apiClient.post<TokenPairDto>('/auth/login', { email, password, deviceId });
  return data;
}
```

From apps/mobile/src/hooks/use-auth.ts (current):
```typescript
export function useLogin() {
  return useMutation({
    mutationFn: async ({ email, password }: { email: string; password: string }) => {
      const deviceId = await getOrCreateDeviceId();
      const tokens = await authApi.login(email, password, deviceId);
      // ...
    },
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mobile auth API and hook to use login field with mobile context</name>
  <read_first>
    - apps/mobile/src/api/auth.api.ts
    - apps/mobile/src/hooks/use-auth.ts
  </read_first>
  <files>
    apps/mobile/src/api/auth.api.ts
    apps/mobile/src/hooks/use-auth.ts
  </files>
  <action>
    1. In `apps/mobile/src/api/auth.api.ts`:
       - Rename `login` method first param from `email: string` to `login: string`
       - Change POST body from `{ email, password, deviceId }` to `{ login, password, deviceId, context: 'mobile' }`

    2. In `apps/mobile/src/hooks/use-auth.ts`:
       - Update `useLogin` mutationFn parameter type from `{ email: string; password: string }` to `{ username: string; password: string }`
       - Change the call from `authApi.login(email, password, deviceId)` to `authApi.login(username, password, deviceId)`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "login.*string.*password.*string.*deviceId" apps/mobile/src/api/auth.api.ts (renamed param)
    - grep -q "context.*mobile" apps/mobile/src/api/auth.api.ts (sends mobile context)
    - grep -qv "email" apps/mobile/src/api/auth.api.ts (no email references in login method)
    - grep -q "username.*string" apps/mobile/src/hooks/use-auth.ts (accepts username)
    - grep -qv "email.*string.*password" apps/mobile/src/hooks/use-auth.ts (no email in useLogin type)
  </acceptance_criteria>
  <done>
    Mobile auth API sends `{ login, password, deviceId, context: 'mobile' }` to API. useLogin hook accepts `{ username, password }` instead of `{ email, password }`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update mobile login screen from email to username UI</name>
  <read_first>
    - apps/mobile/app/login.tsx
  </read_first>
  <files>
    apps/mobile/app/login.tsx
  </files>
  <action>
    1. Update the zod `LoginFormSchema`:
       - Replace `email: z.string().email('Nieprawidlowy adres email')` with `username: z.string().min(3, 'Nazwa uzytkownika musi miec co najmniej 3 znaki')`
       - Keep `password: z.string().min(8, 'Haslo musi miec min. 8 znakow')`

    2. Update `LoginFormValues` type (auto-derived from schema)

    3. Update `useForm` defaultValues from `{ email: '' }` to `{ username: '' }`

    4. Update `onSubmit` function:
       - Change from `{ email: data.email, password: data.password }` to `{ username: data.username, password: data.password }`

    5. Update the email Controller to username Controller:
       - Change `name="email"` to `name="username"`
       - Change label from "Email" to "Nazwa uzytkownika"
       - Change placeholder from "jan@firma.pl" to "jkowalski"
       - Remove `keyboardType="email-address"` (use default keyboard)
       - Keep `autoCapitalize="none"` and `autoCorrect={false}`
       - Change error reference from `errors.email?.message` to `errors.username?.message`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit -p apps/mobile/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Nazwa uzytkownika" apps/mobile/app/login.tsx
    - grep -q "username.*z.string" apps/mobile/app/login.tsx (zod schema uses username)
    - grep -qv "email.*z.string.*email" apps/mobile/app/login.tsx (no email validation)
    - grep -qv "keyboardType.*email" apps/mobile/app/login.tsx (no email keyboard)
    - grep -q "jkowalski" apps/mobile/app/login.tsx (placeholder)
    - grep -q "username.*data.username" apps/mobile/app/login.tsx (submits username)
  </acceptance_criteria>
  <done>
    Mobile login screen shows "Nazwa uzytkownika" text field with placeholder "jkowalski". No email validation or email keyboard. Form submits username to useLogin hook. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes for mobile app: `npx tsc --noEmit -p apps/mobile/tsconfig.json`
- Login screen shows username field, not email
- Auth API sends login field (not email) with context=mobile
- No email references remain in mobile login flow
</verification>

<success_criteria>
- Worker sees "Nazwa uzytkownika" field on mobile login screen
- Mobile app sends `{ login: username, password, deviceId, context: 'mobile' }` to API
- API generates mobile token signed with JWT_MOBILE_SECRET with aud=mobile
- No email field, email validation, or email keyboard in mobile login
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-auth-overhaul-user-management/29-03-SUMMARY.md`
</output>
