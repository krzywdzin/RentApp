---
phase: 29-auth-overhaul-user-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/auth/dto/login.dto.ts
  - apps/api/src/auth/auth.service.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/auth/strategies/jwt-mobile.strategy.ts
  - apps/api/src/auth/auth.module.ts
  - apps/api/src/users/users.service.ts
  - apps/api/src/users/dto/create-user.dto.ts
  - packages/shared/src/types/user.types.ts
  - .env.example
autonomous: true
requirements: [AUTH-06, USER-02, USER-03, USER-04]

must_haves:
  truths:
    - "API login endpoint accepts context field to distinguish admin vs mobile logins"
    - "Admin login tokens are signed with JWT_ACCESS_SECRET and have aud=admin"
    - "Mobile login tokens are signed with JWT_MOBILE_SECRET and have aud=mobile"
    - "MobileJwtStrategy validates tokens signed with JWT_MOBILE_SECRET"
    - "Admin JwtStrategy rejects tokens with aud=mobile"
    - "Worker creation with password sets passwordHash immediately (no setup token flow)"
    - "Email is optional for worker creation"
  artifacts:
    - path: "apps/api/src/auth/strategies/jwt-mobile.strategy.ts"
      provides: "MobileJwtStrategy for mobile app token validation"
      exports: ["MobileJwtStrategy"]
    - path: "apps/api/src/auth/dto/login.dto.ts"
      provides: "LoginDto with optional context field"
      contains: "context"
    - path: "packages/shared/src/types/user.types.ts"
      provides: "JwtPayload with aud field"
      contains: "aud"
  key_links:
    - from: "apps/api/src/auth/auth.service.ts"
      to: "JWT_MOBILE_SECRET config"
      via: "context parameter in login method"
      pattern: "JWT_MOBILE_SECRET"
    - from: "apps/api/src/auth/strategies/jwt-mobile.strategy.ts"
      to: "JWT_MOBILE_SECRET"
      via: "PassportStrategy secretOrKey"
      pattern: "JWT_MOBILE_SECRET"
    - from: "apps/api/src/users/users.service.ts"
      to: "argon2.hash"
      via: "password field in createUser"
      pattern: "argon2\\.hash.*dto\\.password"
---

<objective>
API auth context separation and worker creation with immediate password

Purpose: Establish the backend foundation for separate admin/mobile auth contexts with distinct JWT secrets, and enable worker account creation with immediate password hashing so workers can log in without email setup flow.

Output: MobileJwtStrategy, LoginDto with context field, auth service generating context-aware tokens, CreateUserDto accepting optional password and making email optional.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-bug-fixes-auth-foundation/28-02-SUMMARY.md

<interfaces>
<!-- Current auth service login method signature -->
From apps/api/src/auth/auth.service.ts:
```typescript
async login(userId: string, deviceId: string) {
  const payload = { sub: userId, role: user.role };
  const accessToken = this.jwtService.sign(payload, {
    secret: this.config.get<string>('JWT_ACCESS_SECRET'),
    expiresIn: '30m',
  });
  // ... refresh token generation
}
```

From apps/api/src/auth/dto/login.dto.ts:
```typescript
export class LoginDto {
  @IsString() @MinLength(1) login!: string;
  @IsString() @MinLength(8) password!: string;
  @IsUUID() deviceId!: string;
}
```

From packages/shared/src/types/user.types.ts:
```typescript
export interface JwtPayload {
  sub: string;
  role: UserRole;
  iat?: number;
  exp?: number;
}
```

From apps/api/src/auth/strategies/jwt.strategy.ts:
```typescript
@Injectable()
export class JwtStrategy extends PassportStrategy(Strategy) {
  constructor(config: ConfigService, private prisma: PrismaService) {
    super({
      jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),
      ignoreExpiration: false,
      secretOrKey: config.get<string>('JWT_ACCESS_SECRET')!,
    });
  }
  async validate(payload: JwtPayload) { ... }
}
```

From apps/api/src/users/dto/create-user.dto.ts:
```typescript
export class CreateUserDto {
  @IsEmail() email!: string;
  @IsString() @MinLength(3) @IsOptional() username?: string;
  @IsString() @MinLength(2) name!: string;
  @IsEnum(UserRole) role!: UserRole;
}
```

From apps/api/src/users/users.service.ts:
```typescript
async createUser(dto: CreateUserDto, adminId: string) {
  // Currently: generates setup token, sends email, passwordHash = null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth context to JWT system and create MobileJwtStrategy</name>
  <read_first>
    - packages/shared/src/types/user.types.ts
    - apps/api/src/auth/dto/login.dto.ts
    - apps/api/src/auth/auth.service.ts
    - apps/api/src/auth/auth.controller.ts
    - apps/api/src/auth/strategies/jwt.strategy.ts
    - apps/api/src/auth/auth.module.ts
    - .env.example
  </read_first>
  <files>
    packages/shared/src/types/user.types.ts
    apps/api/src/auth/dto/login.dto.ts
    apps/api/src/auth/auth.service.ts
    apps/api/src/auth/auth.controller.ts
    apps/api/src/auth/strategies/jwt.strategy.ts
    apps/api/src/auth/strategies/jwt-mobile.strategy.ts
    apps/api/src/auth/auth.module.ts
    .env.example
  </files>
  <action>
    1. In `packages/shared/src/types/user.types.ts`:
       - Add `aud?: 'admin' | 'mobile'` field to JwtPayload interface

    2. In `apps/api/src/auth/dto/login.dto.ts`:
       - Add optional `context` field: `@IsOptional() @IsIn(['admin', 'mobile']) context?: 'admin' | 'mobile'`
       - Import `IsOptional, IsIn` from class-validator

    3. In `apps/api/src/auth/auth.service.ts`:
       - Modify `login(userId, deviceId)` to accept third parameter `context: 'admin' | 'mobile' = 'admin'`
       - Add `aud: context` to the JWT payload object: `{ sub: userId, role: user.role, aud: context }`
       - When `context === 'mobile'`, sign with `this.config.get<string>('JWT_MOBILE_SECRET')` instead of `JWT_ACCESS_SECRET`
       - When `context === 'admin'` (default), keep using `JWT_ACCESS_SECRET`
       - Update refresh method to also accept context param, pass through to `this.login(userId, deviceId, context)`
       - In the refresh method, extract context from the stored/decoded token if possible, or default to 'admin'

    4. In `apps/api/src/auth/auth.controller.ts`:
       - Pass `dto.context ?? 'admin'` as third argument to `this.authService.login(user.id, dto.deviceId, dto.context ?? 'admin')`
       - In refresh endpoint, decode the expired token to get `aud` field and pass it to refresh

    5. Create `apps/api/src/auth/strategies/jwt-mobile.strategy.ts`:
       - Clone jwt.strategy.ts structure
       - Use `PassportStrategy(Strategy, 'jwt-mobile')` -- the second argument is the strategy name
       - Use `config.get<string>('JWT_MOBILE_SECRET')` as secretOrKey
       - Same validate logic as JwtStrategy (lookup user by sub, check isActive)
       - Check that `payload.aud === 'mobile'`, throw UnauthorizedException if not

    6. In `apps/api/src/auth/strategies/jwt.strategy.ts`:
       - In validate method, add check: if `payload.aud === 'mobile'` throw UnauthorizedException('Invalid token audience') -- admin strategy rejects mobile tokens

    7. In `apps/api/src/auth/auth.module.ts`:
       - Import MobileJwtStrategy from './strategies/jwt-mobile.strategy'
       - Add MobileJwtStrategy to providers array

    8. In `.env.example`:
       - Add `JWT_MOBILE_SECRET=change-me-mobile-secret-min-32-chars` after the existing JWT lines

    9. In local `.env` file (if exists), add `JWT_MOBILE_SECRET` with a generated value. Use the same pattern as other secrets.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "aud.*admin.*mobile" packages/shared/src/types/user.types.ts
    - grep -q "context.*admin.*mobile" apps/api/src/auth/dto/login.dto.ts
    - grep -q "JWT_MOBILE_SECRET" apps/api/src/auth/strategies/jwt-mobile.strategy.ts
    - grep -q "jwt-mobile" apps/api/src/auth/strategies/jwt-mobile.strategy.ts
    - grep -q "MobileJwtStrategy" apps/api/src/auth/auth.module.ts
    - grep -q "aud.*context" apps/api/src/auth/auth.service.ts
    - grep -q "JWT_MOBILE_SECRET" .env.example
    - grep -q "payload.aud.*mobile" apps/api/src/auth/strategies/jwt.strategy.ts
  </acceptance_criteria>
  <done>
    JWT system generates context-aware tokens: admin tokens use JWT_ACCESS_SECRET with aud=admin, mobile tokens use JWT_MOBILE_SECRET with aud=mobile. MobileJwtStrategy validates mobile tokens. Admin JwtStrategy rejects mobile tokens. LoginDto accepts optional context field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update worker creation to accept password directly and make email optional</name>
  <read_first>
    - apps/api/src/users/dto/create-user.dto.ts
    - apps/api/src/users/users.service.ts
    - apps/api/src/users/users.controller.ts
  </read_first>
  <files>
    apps/api/src/users/dto/create-user.dto.ts
    apps/api/src/users/users.service.ts
  </files>
  <action>
    1. In `apps/api/src/users/dto/create-user.dto.ts`:
       - Make email optional: Change `@IsEmail() email!: string;` to `@IsOptional() @IsEmail() email?: string;`
       - Add optional password field: `@IsOptional() @IsString() @MinLength(8) password?: string;`
       - Keep existing username field (already optional with @MinLength(3))
       - Import `IsOptional` if not already imported

    2. In `apps/api/src/users/users.service.ts`:
       - Modify `createUser(dto, adminId)` to check if `dto.password` is provided
       - When `dto.password` IS provided (worker fast-create path):
         - Hash password with argon2: `const passwordHash = await argon2.hash(dto.password, { memoryCost: 32768, timeCost: 3, parallelism: 1 });`
         - Create user with `passwordHash` set, `setupToken: null`, `setupTokenExpiry: null`
         - Do NOT send setup email (skip mailService call)
         - Include `username: dto.username` in the create data
       - When `dto.password` is NOT provided (existing email flow):
         - Keep existing behavior: generate setup token, send email
         - This preserves backward compatibility
       - When email is not provided, skip the mailService call entirely (even in the non-password path)
       - Return the same shape: `{ id, email, username, name, role }`
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "@IsOptional" apps/api/src/users/dto/create-user.dto.ts (email is optional)
    - grep -q "password" apps/api/src/users/dto/create-user.dto.ts (password field exists)
    - grep -q "dto.password" apps/api/src/users/users.service.ts (conditional password handling)
    - grep -q "argon2.hash.*dto.password" apps/api/src/users/users.service.ts (hashes provided password)
    - grep -q "username.*dto.username" apps/api/src/users/users.service.ts (stores username)
  </acceptance_criteria>
  <done>
    CreateUserDto accepts optional password and optional email. When password provided, user is created with passwordHash set immediately (no email flow). When password not provided, existing setup-token email flow is preserved. Worker can log in immediately after creation with password.
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes for API: `npx tsc --noEmit -p apps/api/tsconfig.json`
- MobileJwtStrategy file exists and exports the strategy
- LoginDto accepts optional context field
- JwtPayload has aud field in shared types
- CreateUserDto makes email optional and accepts password
- Auth service generates tokens with correct secret based on context
</verification>

<success_criteria>
- API login endpoint accepts `context: "admin" | "mobile"` and generates tokens with appropriate JWT secret and audience claim
- MobileJwtStrategy validates tokens signed with JWT_MOBILE_SECRET
- Admin JwtStrategy rejects tokens with aud=mobile
- Worker creation with password provided: passwordHash set immediately, no email sent
- Worker creation without password: existing email setup flow preserved
- Email is optional for user creation
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/29-auth-overhaul-user-management/29-01-SUMMARY.md`
</output>
