---
phase: 24-web-quality-accessibility
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(admin)/pojazdy/[id]/page.tsx
  - apps/web/src/app/(admin)/klienci/[id]/page.tsx
  - apps/web/src/app/(admin)/uzytkownicy/page.tsx
  - apps/web/src/app/(admin)/audyt/filter-bar.tsx
  - apps/web/src/app/(admin)/wynajmy/calendar-view.tsx
  - apps/web/src/components/layout/top-bar.tsx
  - apps/web/src/app/(admin)/wynajmy/nowy/page.tsx
  - apps/web/src/app/(admin)/pojazdy/filter-bar.tsx
  - apps/web/src/components/photos/damage-comparison.tsx
autonomous: true
requirements:
  - WA11Y-01
  - WA11Y-02
  - WA11Y-03
  - WA11Y-04
  - WA11Y-05
  - WA11Y-06
  - WA11Y-07
  - WA11Y-08

must_haves:
  truths:
    - "Interactive divs on detail pages have role=button, tabIndex=0, and onKeyDown for Enter/Space"
    - "Collapsible card header on users page has role=button, aria-expanded, tabIndex, onKeyDown"
    - "Filter bar labels are properly associated with Select components"
    - "Calendar rental blocks are keyboard accessible"
    - "Logout button has aria-label"
    - "Customer search dropdown uses combobox pattern with aria attributes"
    - "Vehicle search input has aria-label"
    - "Damage SVG pins are keyboard-focusable"
  artifacts:
    - path: "apps/web/src/app/(admin)/pojazdy/[id]/page.tsx"
      provides: "Keyboard-accessible rental history items"
      contains: "role.*button"
    - path: "apps/web/src/app/(admin)/wynajmy/calendar-view.tsx"
      provides: "Keyboard-accessible calendar blocks"
      contains: "tabIndex"
    - path: "apps/web/src/components/layout/top-bar.tsx"
      provides: "Accessible logout button"
      contains: "aria-label"
  key_links:
    - from: "apps/web/src/app/(admin)/pojazdy/[id]/page.tsx"
      to: "keyboard navigation"
      via: "onKeyDown handler on interactive divs"
      pattern: "onKeyDown"
    - from: "apps/web/src/app/(admin)/wynajmy/nowy/page.tsx"
      to: "combobox pattern"
      via: "role=combobox, aria-expanded, aria-haspopup on customer search"
      pattern: "combobox"
---

<objective>
Add keyboard navigation and ARIA attributes to all interactive elements: clickable divs on detail pages, collapsible cards, filter selects, calendar blocks, logout button, customer search dropdown, vehicle search input, and damage SVG pins.

Purpose: Screen reader users and keyboard-only users can navigate and interact with all web admin features.
Output: 9 files updated with proper accessibility attributes.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/WEB-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interactive divs, collapsible cards, filter labels, and simple ARIA fixes</name>
  <files>
    apps/web/src/app/(admin)/pojazdy/[id]/page.tsx,
    apps/web/src/app/(admin)/klienci/[id]/page.tsx,
    apps/web/src/app/(admin)/uzytkownicy/page.tsx,
    apps/web/src/app/(admin)/audyt/filter-bar.tsx,
    apps/web/src/components/layout/top-bar.tsx,
    apps/web/src/app/(admin)/pojazdy/filter-bar.tsx
  </files>
  <read_first>
    apps/web/src/app/(admin)/pojazdy/[id]/page.tsx,
    apps/web/src/app/(admin)/klienci/[id]/page.tsx,
    apps/web/src/app/(admin)/uzytkownicy/page.tsx,
    apps/web/src/app/(admin)/audyt/filter-bar.tsx,
    apps/web/src/components/layout/top-bar.tsx,
    apps/web/src/app/(admin)/pojazdy/filter-bar.tsx
  </read_first>
  <action>
    1. **Interactive divs on vehicle detail** (pojazdy/[id]/page.tsx):
       - Find rental history items rendered as `<div onClick={() => router.push(...)}>`.
       - Add `role="button"`, `tabIndex={0}`, and `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); router.push(...); } }}`.
       - Keep existing onClick and cursor-pointer styling.

    2. **Interactive divs on customer detail** (klienci/[id]/page.tsx):
       - Same pattern as above for rental history items.

    3. **Collapsible card header on users page** (uzytkownicy/page.tsx):
       - Find `<CardHeader className="cursor-pointer" onClick={() => setFormOpen(!formOpen)}>`.
       - Add `role="button"`, `tabIndex={0}`, `aria-expanded={formOpen}`.
       - Add `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); setFormOpen(!formOpen); } }}`.

    4. **Filter bar labels** (audyt/filter-bar.tsx):
       - Find `<label>` elements without `htmlFor` wrapping Select components.
       - Add `aria-label` to each `<SelectTrigger>` with descriptive text (e.g., `aria-label="Filtruj po akcji"`, `aria-label="Filtruj po uzytkowniku"`).
       - Alternatively, add `id` to SelectTrigger and `htmlFor` on label.

    5. **Logout button** (top-bar.tsx):
       - Find the `<Button variant="ghost" size="icon" onClick={logout}>` with LogOut icon.
       - Add `aria-label="Wyloguj"`.

    6. **Vehicle search input** (pojazdy/filter-bar.tsx):
       - Find `<Input placeholder="Szukaj po rejestracji lub VIN...">`.
       - Add `aria-label="Szukaj po rejestracji lub VIN"`.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx turbo run build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "role.*button" apps/web/src/app/\(admin\)/pojazdy/\[id\]/page.tsx returns a match
    - grep "tabIndex" apps/web/src/app/\(admin\)/pojazdy/\[id\]/page.tsx returns a match
    - grep "onKeyDown" apps/web/src/app/\(admin\)/klienci/\[id\]/page.tsx returns a match
    - grep "aria-expanded" apps/web/src/app/\(admin\)/uzytkownicy/page.tsx returns a match
    - grep "aria-label" apps/web/src/app/\(admin\)/audyt/filter-bar.tsx returns a match
    - grep 'aria-label.*Wyloguj' apps/web/src/components/layout/top-bar.tsx returns a match
    - grep "aria-label" apps/web/src/app/\(admin\)/pojazdy/filter-bar.tsx returns a match
  </acceptance_criteria>
  <done>All interactive divs have role=button, tabIndex=0, and keyboard handlers. Collapsible card has aria-expanded. Filter labels are associated. Logout and search inputs have aria-labels.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar blocks, customer search combobox, and damage pin accessibility</name>
  <files>
    apps/web/src/app/(admin)/wynajmy/calendar-view.tsx,
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx,
    apps/web/src/components/photos/damage-comparison.tsx
  </files>
  <read_first>
    apps/web/src/app/(admin)/wynajmy/calendar-view.tsx,
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx,
    apps/web/src/components/photos/damage-comparison.tsx
  </read_first>
  <action>
    1. **Calendar rental blocks** (wynajmy/calendar-view.tsx):
       - Find rental block divs with `onClick` (around lines 236-249).
       - Add `role="button"`, `tabIndex={0}`, `aria-label` with rental info (e.g., vehicle registration + customer name + status).
       - Add `onKeyDown` handler for Enter/Space to trigger the same action as onClick.

    2. **Customer search dropdown combobox** (wynajmy/nowy/page.tsx):
       - Find the custom customer search input and dropdown (around lines 166-189).
       - On the search input: add `role="combobox"`, `aria-expanded={showDropdown}`, `aria-haspopup="listbox"`, `aria-autocomplete="list"`, `aria-controls="customer-search-results"`.
       - On the dropdown container div: add `role="listbox"`, `id="customer-search-results"`.
       - On each result button: add `role="option"`.
       - Add basic arrow key navigation: on ArrowDown/ArrowUp in the input, move focus to the first/last result button. On Escape, close the dropdown.

    3. **Damage SVG pins** (damage-comparison.tsx):
       - Find SVG `<g>` elements inside `<Tooltip>` (around lines 228-258).
       - Make pins focusable by wrapping each pin in a `<g tabIndex={0} role="button" aria-label={pin description}>` or using a `<circle>` with `tabIndex={0}` and `role="button"`.
       - Add `onKeyDown` to open tooltip on Enter/Space if the tooltip library supports programmatic open. If not, add `onFocus`/`onBlur` handlers to show/hide tooltip content.
       - For SVG focusability, add `focusable="true"` attribute on the `<g>` element.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx turbo run build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "tabIndex" apps/web/src/app/\(admin\)/wynajmy/calendar-view.tsx returns a match
    - grep "role.*button" apps/web/src/app/\(admin\)/wynajmy/calendar-view.tsx returns a match
    - grep "combobox" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns a match
    - grep "aria-expanded" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns a match
    - grep "listbox" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns a match
    - grep "tabIndex\|focusable" apps/web/src/components/photos/damage-comparison.tsx returns a match
  </acceptance_criteria>
  <done>Calendar blocks are keyboard-accessible with role and aria-label. Customer search uses combobox pattern with listbox results and arrow key navigation. Damage SVG pins are keyboard-focusable for tooltip access.</done>
</task>

</tasks>

<verification>
- `npx turbo run build --filter=web` completes without errors
- All interactive divs have role, tabIndex, onKeyDown
- Calendar blocks have keyboard support
- Customer search follows combobox ARIA pattern
- Damage pins are focusable
- Logout button has aria-label
</verification>

<success_criteria>
- Tab key can reach all interactive elements: rental history items, collapsible card, calendar blocks, customer search results, damage pins
- Enter/Space activates clickable elements
- Screen reader announces interactive elements with proper roles and labels
- Customer search dropdown can be navigated with arrow keys
</success_criteria>

<output>
After completion, create `.planning/phases/24-web-quality-accessibility/24-04-SUMMARY.md`
</output>
