---
phase: 24-web-quality-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - apps/web/src/app/(admin)/pojazdy/[id]/page.tsx
  - apps/web/src/app/(admin)/klienci/[id]/page.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx
  - apps/web/src/app/(admin)/umowy/[id]/page.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/dokumentacja/page.tsx
  - apps/web/src/app/(admin)/wynajmy/nowy/page.tsx
  - apps/web/src/hooks/use-portal-auth.ts
  - apps/web/src/app/api/[...path]/route.ts
autonomous: true
requirements:
  - WERR-01
  - WERR-02
  - WERR-03
  - WERR-04
  - WERR-05

must_haves:
  truths:
    - "Vehicle detail page shows ErrorState with retry on API failure"
    - "Customer detail page shows ErrorState with retry on API failure"
    - "Rental edit page shows ErrorState with retry on API failure"
    - "Contract detail page shows ErrorState with retry on API failure"
    - "Photo documentation page shows ErrorState for photo and damage query failures"
    - "createRental submit handler has try/catch wrapping mutateAsync"
    - "Portal auth catch blocks log errors via console.error"
    - "Proxy route returns meaningful error body for non-JSON responses"
  artifacts:
    - path: "apps/web/src/app/(admin)/pojazdy/[id]/page.tsx"
      provides: "Vehicle detail with error handling"
      contains: "isError"
    - path: "apps/web/src/app/(admin)/umowy/[id]/page.tsx"
      provides: "Contract detail with error handling"
      contains: "isError"
  key_links:
    - from: "apps/web/src/app/(admin)/pojazdy/[id]/page.tsx"
      to: "apps/web/src/components/ui/error-state.tsx"
      via: "import ErrorState"
      pattern: "import.*ErrorState.*error-state"
---

<objective>
Add error handling to all detail pages (vehicle, customer, rental, contract, photo documentation), wrap createRental submit in try/catch, fix portal auth silent error swallowing, and handle non-JSON proxy responses.

Purpose: Users see actionable error messages with retry instead of blank screens or confusing fallbacks on API failures.
Output: 8 files updated with proper error handling using the shared ErrorState component from plan-01.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-web-quality-accessibility/24-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isError handling to all detail pages</name>
  <files>
    apps/web/src/app/(admin)/pojazdy/[id]/page.tsx,
    apps/web/src/app/(admin)/klienci/[id]/page.tsx,
    apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx,
    apps/web/src/app/(admin)/umowy/[id]/page.tsx,
    apps/web/src/app/(admin)/wynajmy/[id]/dokumentacja/page.tsx
  </files>
  <read_first>
    apps/web/src/app/(admin)/pojazdy/[id]/page.tsx,
    apps/web/src/app/(admin)/klienci/[id]/page.tsx,
    apps/web/src/app/(admin)/wynajmy/[id]/edytuj/page.tsx,
    apps/web/src/app/(admin)/umowy/[id]/page.tsx,
    apps/web/src/app/(admin)/wynajmy/[id]/dokumentacja/page.tsx,
    apps/web/src/components/ui/error-state.tsx
  </read_first>
  <action>
    For each detail page, apply the same pattern:

    1. **Vehicle detail** (pojazdy/[id]/page.tsx):
       - Destructure `isError, refetch` from `useVehicle(id)` alongside existing `data, isLoading`.
       - After the isLoading check, add: `if (isError) return <ErrorState onRetry={refetch} />;`
       - Import `ErrorState` from `@/components/ui/error-state`.

    2. **Customer detail** (klienci/[id]/page.tsx):
       - Same pattern: destructure `isError, refetch` from `useCustomer(id)`.
       - Add ErrorState return after isLoading check.

    3. **Rental edit** (wynajmy/[id]/edytuj/page.tsx):
       - Destructure `isError, refetch` from `useRental(id)`.
       - Add ErrorState return after isLoading check.

    4. **Contract detail** (umowy/[id]/page.tsx):
       - Destructure `isError, refetch` from `useContract(id)`.
       - Add ErrorState return after isLoading check.

    5. **Photo documentation** (wynajmy/[id]/dokumentacja/page.tsx):
       - Destructure `isError` from both `photoQuery` and `damageQuery`.
       - Add combined error check: `if (photoQuery.isError || damageQuery.isError) return <ErrorState onRetry={() => { photoQuery.refetch(); damageQuery.refetch(); }} />;`
       - Place after loading check.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx turbo run build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "isError" apps/web/src/app/\(admin\)/pojazdy/\[id\]/page.tsx returns a match
    - grep "isError" apps/web/src/app/\(admin\)/klienci/\[id\]/page.tsx returns a match
    - grep "isError" apps/web/src/app/\(admin\)/wynajmy/\[id\]/edytuj/page.tsx returns a match
    - grep "isError" apps/web/src/app/\(admin\)/umowy/\[id\]/page.tsx returns a match
    - grep "isError" apps/web/src/app/\(admin\)/wynajmy/\[id\]/dokumentacja/page.tsx returns a match
    - grep "ErrorState" apps/web/src/app/\(admin\)/pojazdy/\[id\]/page.tsx returns a match
  </acceptance_criteria>
  <done>All 5 detail pages destructure isError from their query hooks and render ErrorState with retry on failure. No blank screens or misleading "not found" messages on API errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rental submit, portal auth, and proxy error handling</name>
  <files>
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx,
    apps/web/src/hooks/use-portal-auth.ts,
    apps/web/src/app/api/[...path]/route.ts
  </files>
  <read_first>
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx,
    apps/web/src/hooks/use-portal-auth.ts,
    apps/web/src/app/api/[...path]/route.ts
  </read_first>
  <action>
    1. **Rental submit try/catch** (wynajmy/nowy/page.tsx):
       - Find the `onSubmit` handler that calls `createRental.mutateAsync(...)`.
       - Wrap the mutateAsync call in try/catch. The catch block should be empty (error toast is already handled by the mutation's onError callback) but the try/catch prevents unhandled promise rejection.
       - Pattern: `try { await createRental.mutateAsync(payload); router.push(...); } catch { /* handled by mutation onError */ }`

    2. **Portal auth error logging** (use-portal-auth.ts):
       - Find empty `catch {}` blocks in `checkAuth` (around lines 35-37) and `exchangeToken` (around line 71).
       - Replace empty catch bodies with `console.error('Portal auth check failed:', error)` and `console.error('Token exchange failed:', error)` respectively.
       - Ensure the error parameter is captured: `catch (error) { ... }`.

    3. **Proxy non-JSON handling** (app/api/[...path]/route.ts):
       - Find the `.catch(() => ({}))` on `res.json()`.
       - Replace with a more informative fallback: `.catch(() => ({ error: 'Non-JSON response from backend', status: res.status }))`.
       - This ensures the frontend receives a parseable body with context about what happened.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx turbo run build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "try" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns a match near mutateAsync
    - grep "console.error" apps/web/src/hooks/use-portal-auth.ts returns matches (at least 2)
    - grep "Non-JSON" apps/web/src/app/api/\[...path\]/route.ts returns a match
  </acceptance_criteria>
  <done>createRental submit wrapped in try/catch preventing unhandled rejections. Portal auth errors logged to console. Proxy route returns descriptive error body for non-JSON backend responses.</done>
</task>

</tasks>

<verification>
- `npx turbo run build --filter=web` completes without errors
- All 5 detail pages have isError + ErrorState pattern
- New rental form submit has try/catch
- Portal auth has console.error in catch blocks
- Proxy route returns structured error for non-JSON responses
</verification>

<success_criteria>
- API failures on any detail page show an error message with a working retry button
- No unhandled promise rejections from createRental submit
- Portal auth errors are logged, not silently swallowed
- Proxy non-JSON responses produce parseable error bodies
</success_criteria>

<output>
After completion, create `.planning/phases/24-web-quality-accessibility/24-02-SUMMARY.md`
</output>
