---
phase: 24-web-quality-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(admin)/wynajmy/nowy/page.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
  - apps/web/src/app/(admin)/uzytkownicy/page.tsx
  - apps/web/src/app/(admin)/pojazdy/nowy/page.tsx
  - apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx
  - apps/web/src/hooks/queries/use-users.ts
autonomous: true
requirements:
  - WVAL-01
  - WVAL-02
  - WVAL-03
  - WVAL-04
  - WVAL-05
  - WUI-05

must_haves:
  truths:
    - "dailyRateNet is required (not optional) in new rental form schema"
    - "Extend rental dialog validates new end date is after current end date"
    - "Return mileage validates minimum against vehicle current mileage"
    - "Edit user dialog validates name is non-empty and role is selected"
    - "Numeric inputs (year, seats, mileage) show validation error for 0 values"
    - "User creation uses mutation hook with query invalidation"
  artifacts:
    - path: "apps/web/src/app/(admin)/wynajmy/nowy/page.tsx"
      provides: "Rental form with required dailyRateNet"
      contains: "dailyRateNet.*required\\|z\\.number.*min\\(1\\)"
    - path: "apps/web/src/app/(admin)/wynajmy/[id]/page.tsx"
      provides: "Extend and return dialogs with validation"
      contains: "endDate"
    - path: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      provides: "User creation via mutation hook"
      contains: "useCreateUser\\|useMutation"
  key_links:
    - from: "apps/web/src/app/(admin)/uzytkownicy/page.tsx"
      to: "apps/web/src/hooks/queries/use-users.ts"
      via: "useCreateUser mutation hook"
      pattern: "useCreateUser"
---

<objective>
Fix all form validation gaps in web admin: make dailyRateNet required, add date/mileage validations to extend/return dialogs, validate user edit form, fix numeric 0-value handling, and migrate user creation to mutation hook with cache invalidation.

Purpose: Prevent invalid data submission that causes server-side 400/500 errors with no helpful feedback to the user.
Output: 6 files updated with proper client-side validation and mutation patterns.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/WEB-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rental form and extend/return dialog validations</name>
  <files>
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx,
    apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
  </files>
  <read_first>
    apps/web/src/app/(admin)/wynajmy/nowy/page.tsx,
    apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
  </read_first>
  <action>
    1. **Make dailyRateNet required** (wynajmy/nowy/page.tsx):
       - Find the Zod schema where `dailyRateNet` is defined as `z.number().int().min(0).optional()`.
       - Change to `z.number({ required_error: 'Stawka dzienna jest wymagana' }).int().min(1, 'Stawka musi byc wieksza niz 0')`.
       - Remove `.optional()`.

    2. **Extend rental date validation** (wynajmy/[id]/page.tsx):
       - Find `handleExtend()` function.
       - After the existing `if (!newEndDate) return` check, add validation:
         ```
         const currentEnd = new Date(rental.endDate);
         const selectedEnd = new Date(newEndDate);
         if (selectedEnd <= currentEnd) {
           toast.error('Nowa data musi byc pozniejsza niz obecna data zakonczenia');
           return;
         }
         ```
       - Import `toast` from sonner if not already imported.

    3. **Return mileage minimum validation** (wynajmy/[id]/page.tsx):
       - Find `handleReturn()` function.
       - After the existing `mileage >= 0` check, add:
         ```
         const vehicleMileage = rental.vehicle?.mileage ?? 0;
         if (mileage < vehicleMileage) {
           toast.error(`Przebieg zwrotu nie moze byc mniejszy niz obecny przebieg pojazdu (${vehicleMileage} km)`);
           return;
         }
         ```
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx turbo run build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "optional" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx does NOT match dailyRateNet line (it's no longer optional)
    - grep "required_error\|Stawka dzienna" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns a match
    - grep "selectedEnd.*<=.*currentEnd\|pozniejsza" apps/web/src/app/\(admin\)/wynajmy/\[id\]/page.tsx returns a match
    - grep "vehicleMileage\|mniejszy niz obecny" apps/web/src/app/\(admin\)/wynajmy/\[id\]/page.tsx returns a match
  </acceptance_criteria>
  <done>dailyRateNet is required with min(1) validation. Extend dialog rejects dates not after current end date. Return dialog rejects mileage below vehicle current mileage. All show Polish error messages.</done>
</task>

<task type="auto">
  <name>Task 2: Fix user edit validation, numeric 0-values, and user creation mutation</name>
  <files>
    apps/web/src/app/(admin)/uzytkownicy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/nowy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx,
    apps/web/src/hooks/queries/use-users.ts
  </files>
  <read_first>
    apps/web/src/app/(admin)/uzytkownicy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/nowy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx,
    apps/web/src/hooks/queries/use-users.ts
  </read_first>
  <action>
    1. **Edit user validation** (uzytkownicy/page.tsx):
       - Find `handleEditSave` function.
       - Add validation before the mutation call:
         ```
         if (!editName?.trim()) {
           toast.error('Imie i nazwisko jest wymagane');
           return;
         }
         if (!editRole) {
           toast.error('Rola jest wymagana');
           return;
         }
         ```

    2. **User creation via mutation hook** (uzytkownicy/page.tsx + use-users.ts):
       - In `apps/web/src/hooks/queries/use-users.ts`, add a `useCreateUser` mutation hook if it doesn't exist:
         ```
         export function useCreateUser() {
           const queryClient = useQueryClient();
           return useMutation({
             mutationFn: (data: { name: string; email: string; password: string; role: string }) =>
               apiClient('/users', { method: 'POST', body: JSON.stringify(data) }),
             onSuccess: () => {
               queryClient.invalidateQueries({ queryKey: ['users'] });
               toast.success('Uzytkownik zostal utworzony');
             },
             onError: () => {
               toast.error('Nie udalo sie utworzyc uzytkownika');
             },
           });
         }
         ```
       - In uzytkownicy/page.tsx, replace the raw `fetch` call in `handleSubmit` with `createUser.mutate(...)` from the new hook.
       - Remove the manual fetch code and the success/error toast handling already in the raw fetch (since the hook handles it).

    3. **Numeric 0-value handling** (pojazdy/nowy/page.tsx and pojazdy/[id]/edytuj/page.tsx):
       - Find all `parseInt(e.target.value) || 0` patterns for year, seatCount, and mileage fields.
       - Replace with: `parseInt(e.target.value) || undefined` so that cleared fields become undefined (triggering Zod required validation) rather than silently submitting 0.
       - Alternatively if the Zod schema uses `.min(1)` for these fields, keep `|| 0` but ensure the schema has `z.number().min(1, 'Wartosc musi byc wieksza niz 0')` for year, seatCount. For mileage in new vehicle, `min(0)` is acceptable.
       - Ensure year has `.min(1900, 'Nieprawidlowy rok').max(new Date().getFullYear() + 1, 'Nieprawidlowy rok')`.
       - Ensure seatCount has `.min(1, 'Liczba miejsc musi byc wieksza niz 0')`.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx turbo run build --filter=web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "editName.*trim\|Imie i nazwisko" apps/web/src/app/\(admin\)/uzytkownicy/page.tsx returns a match
    - grep "editRole\|Rola jest wymagana" apps/web/src/app/\(admin\)/uzytkownicy/page.tsx returns a match
    - grep "useCreateUser" apps/web/src/hooks/queries/use-users.ts returns a match
    - grep "useCreateUser\|createUser" apps/web/src/app/\(admin\)/uzytkownicy/page.tsx returns a match
    - grep "invalidateQueries.*users" apps/web/src/hooks/queries/use-users.ts returns a match
    - grep "min(1900)\|Nieprawidlowy rok\|min.*1.*miejsc" apps/web/src/app/\(admin\)/pojazdy/nowy/page.tsx returns a match
  </acceptance_criteria>
  <done>Edit user validates name and role before save. User creation uses mutation hook with query invalidation (new users appear without page refresh). Year and seatCount fields reject 0 values with validation errors.</done>
</task>

</tasks>

<verification>
- `npx turbo run build --filter=web` completes without errors
- dailyRateNet schema no longer has .optional()
- Extend dialog has date comparison validation
- Return dialog has mileage minimum validation
- User edit validates name and role
- User creation uses mutation hook with cache invalidation
- Vehicle year/seatCount reject 0 values
</verification>

<success_criteria>
- Submitting rental form with empty daily rate shows inline validation error
- Extending rental to an earlier date shows toast error and prevents submission
- Return mileage below vehicle current mileage shows toast error
- Empty name or missing role in user edit shows toast error
- Creating a user refreshes the users table automatically
- Year=0 or seats=0 in vehicle form shows validation error
</success_criteria>

<output>
After completion, create `.planning/phases/24-web-quality-accessibility/24-03-SUMMARY.md`
</output>
