# Deferred Items - Phase 24

## Pre-existing Lint/Prettier Errors (out of scope)

1. `apps/web/src/lib/csv-export.ts:15` - Prettier formatting (pre-existing)
2. `apps/web/src/hooks/queries/use-users.ts:78` - Prettier formatting (pre-existing)
3. `apps/web/src/app/(admin)/uzytkownicy/page.tsx:52` - Unused `setSubmitting` variable (pre-existing)

These are pre-existing lint errors not caused by any Phase 24 changes.
