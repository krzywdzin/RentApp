---
phase: 26-code-quality-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/customers/customers.service.ts
  - apps/api/src/cepik/cepik.service.ts
  - apps/api/src/audit/audit.service.ts
  - apps/api/src/portal/strategies/portal-jwt.strategy.ts
  - apps/api/src/notifications/listeners/rental-activated.listener.ts
  - apps/api/src/notifications/listeners/rental-extended.listener.ts
  - apps/api/src/users/users.controller.ts
  - apps/api/src/rentals/rentals.service.ts
  - apps/api/src/storage/storage.service.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/health/health.module.ts
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-08]

must_haves:
  truths:
    - "No `: any` types in API service files (excluding catch clauses and test files)"
    - "Non-null assertions in rentals.service.ts have proper null guards"
    - "HealthModule has explicit imports for PrismaModule, StorageModule, ConfigModule"
    - "HealthController accesses storage availability via public getter, not `as any` cast"
  artifacts:
    - path: "apps/api/src/customers/customers.service.ts"
      provides: "Typed where clause and toDto method"
      contains: "Prisma."
    - path: "apps/api/src/cepik/cepik.service.ts"
      provides: "Typed toDto with Prisma payload type"
      contains: "CepikVerification"
    - path: "apps/api/src/health/health.module.ts"
      provides: "Explicit module imports"
      contains: "imports:"
    - path: "apps/api/src/storage/storage.service.ts"
      provides: "Public getter for s3Available"
      contains: "get isAvailable"
  key_links:
    - from: "apps/api/src/health/health.controller.ts"
      to: "apps/api/src/storage/storage.service.ts"
      via: "public getter instead of as any cast"
      pattern: "this\\.storage\\.isAvailable"
    - from: "apps/api/src/health/health.module.ts"
      to: "PrismaModule, StorageModule"
      via: "imports array"
      pattern: "imports:.*PrismaModule"
---

<objective>
Remove TypeScript `any` types from API service files, add null guards for non-null assertions, fix HealthModule imports, and expose StorageService availability via public getter.

Purpose: Eliminate type safety gaps in API services so TypeScript catches type mismatches at compile time. Fix HealthModule dependency fragility.
Output: All API service files use proper Prisma types; HealthModule has explicit imports.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-code-quality-cleanup/26-CONTEXT.md
@.planning/codebase/API-AUDIT.md (sections 3.1, 3.2, QUALITY-3, QUALITY-4, QUALITY-5)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace `any` types in API services with proper Prisma/explicit types</name>
  <files>apps/api/src/customers/customers.service.ts, apps/api/src/cepik/cepik.service.ts, apps/api/src/audit/audit.service.ts, apps/api/src/portal/strategies/portal-jwt.strategy.ts, apps/api/src/notifications/listeners/rental-activated.listener.ts, apps/api/src/notifications/listeners/rental-extended.listener.ts, apps/api/src/users/users.controller.ts, apps/api/src/rentals/rentals.service.ts</files>
  <read_first>
    - apps/api/src/customers/customers.service.ts (full file -- has `any` at lines ~188 and ~228)
    - apps/api/src/cepik/cepik.service.ts (full file -- `toDto(verification: any)` at line ~155)
    - apps/api/src/audit/audit.service.ts (line ~24 -- `changes as any`)
    - apps/api/src/portal/strategies/portal-jwt.strategy.ts (line ~16 -- `validate(payload: any)`)
    - apps/api/src/notifications/listeners/rental-activated.listener.ts (line ~16 -- `rental: any`)
    - apps/api/src/notifications/listeners/rental-extended.listener.ts (check for any types)
    - apps/api/src/users/users.controller.ts (line ~23 -- `user: any` in getMe)
    - apps/api/src/rentals/rentals.service.ts (lines ~262, ~430 -- non-null assertions)
  </read_first>
  <action>
Fix each file's `any` types:

1. **customers.service.ts** line ~188: Replace `const where: any = { isArchived: false }` with `const where: Prisma.CustomerWhereInput = { isArchived: false }`. Line ~228: Replace `toDto(customer: any)` with the proper Prisma Customer type -- use `Prisma.CustomerGetPayload<{}>` or define a local type alias matching the include pattern used in findAll/findOne.

2. **cepik.service.ts** line ~155: Replace `toDto(verification: any)` with `toDto(verification: Prisma.CepikVerificationGetPayload<{}>)` or the appropriate Prisma generated type. Also fix lines ~85, ~92 if they have `result as any` casts -- use the proper CepikVerification type.

3. **audit.service.ts** line ~24: Replace `entry.changes as any` with `entry.changes as Prisma.JsonValue` or `entry.changes as Record<string, unknown>` depending on the Prisma schema type for that field.

4. **portal-jwt.strategy.ts** line ~16: Replace `validate(payload: any)` with a proper JwtPayload interface: `validate(payload: { sub: string; type: string; customerId: string; iat?: number; exp?: number })`.

5. **rental-activated.listener.ts** line ~16: Replace `{ rental: any }` with a proper type -- use `Prisma.RentalGetPayload<{ include: { customer: true; vehicle: true } }>` or whatever the event emitter sends.

6. **rental-extended.listener.ts**: Check for `any` in catch clauses. `catch (error: any)` can stay as `catch (error: unknown)` with proper type narrowing: `if (error instanceof Error) { ... error.message ... }`.

7. **users.controller.ts** line ~23: Replace `@CurrentUser() user: any` with the proper User type from Prisma or a UserDto.

8. **rentals.service.ts** lines ~262 and ~430: The non-null assertions `updated!` already have null guards (`if (!updated)` checks at lines 262 and 430). Verify these guards exist and throw NotFoundException. If the `!` is still present AFTER the guard, it can be removed since the guard ensures non-null. If no guard exists, add: `if (!updated) throw new NotFoundException('Rental not found after update');`

For `catch (error: any)` patterns across services: Replace with `catch (error: unknown)` and use `error instanceof Error ? error.message : String(error)` for message access. This applies to contracts.service.ts, storage.service.ts, portal.service.ts, alert-scanner.service.ts, vehicles.service.ts catch blocks.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/api exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c ": any" apps/api/src/customers/customers.service.ts returns 0
    - grep -c "toDto(verification: any)" apps/api/src/cepik/cepik.service.ts returns 0
    - grep -c "user: any" apps/api/src/users/users.controller.ts returns 0
    - grep -c "payload: any" apps/api/src/portal/strategies/portal-jwt.strategy.ts returns 0
    - grep -c "rental: any" apps/api/src/notifications/listeners/rental-activated.listener.ts returns 0
  </acceptance_criteria>
  <done>No `: any` parameter/variable types remain in API service files (catch clauses use `unknown`); non-null assertions have proper guards</done>
</task>

<task type="auto">
  <name>Task 2: Fix HealthModule imports and StorageService public getter</name>
  <files>apps/api/src/storage/storage.service.ts, apps/api/src/health/health.controller.ts, apps/api/src/health/health.module.ts</files>
  <read_first>
    - apps/api/src/storage/storage.service.ts (line ~20 for private s3Available)
    - apps/api/src/health/health.controller.ts (full file -- line 35 has `as any` cast)
    - apps/api/src/health/health.module.ts (full file -- missing imports)
    - apps/api/src/prisma/prisma.module.ts (to get module name)
    - apps/api/src/storage/storage.module.ts (to get module name)
  </read_first>
  <action>
1. **StorageService** (`storage.service.ts`): Add a public getter for s3Available:
   ```typescript
   get isAvailable(): boolean {
     return this.s3Available;
   }
   ```
   Keep the existing `private s3Available = false` field unchanged.

2. **HealthController** (`health.controller.ts`): Replace line 35:
   - FROM: `Promise.resolve((this.storage as any).s3Available)`
   - TO: `Promise.resolve(this.storage.isAvailable)`

3. **HealthModule** (`health.module.ts`): Add explicit imports:
   ```typescript
   import { Module } from '@nestjs/common';
   import { ConfigModule } from '@nestjs/config';
   import { HealthController } from './health.controller';
   import { PrismaModule } from '../prisma/prisma.module';
   import { StorageModule } from '../storage/storage.module';

   @Module({
     imports: [PrismaModule, StorageModule, ConfigModule],
     controllers: [HealthController],
   })
   export class HealthModule {}
   ```
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/api exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "get isAvailable" apps/api/src/storage/storage.service.ts
    - grep -q "this.storage.isAvailable" apps/api/src/health/health.controller.ts
    - grep -v "as any" apps/api/src/health/health.controller.ts (no as any cast remains)
    - grep -q "imports:" apps/api/src/health/health.module.ts
    - grep -q "PrismaModule" apps/api/src/health/health.module.ts
    - grep -q "StorageModule" apps/api/src/health/health.module.ts
  </acceptance_criteria>
  <done>HealthController uses public getter (no `as any`), HealthModule has explicit PrismaModule/StorageModule/ConfigModule imports</done>
</task>

</tasks>

<verification>
- `pnpm --filter @rentapp/api exec tsc --noEmit` passes with no errors
- `pnpm --filter @rentapp/api test` passes (existing tests still work)
- `grep -rn ': any' apps/api/src/ --include='*.service.ts' --include='*.controller.ts' --include='*.strategy.ts' --include='*.listener.ts' --include='*.interceptor.ts' | grep -v '.spec.' | grep -v 'catch'` returns zero results
</verification>

<success_criteria>
- Zero `: any` types in API service/controller/strategy/listener files (excluding test files and catch clauses which use `unknown`)
- Non-null assertions have preceding null guards with NotFoundException
- HealthModule has imports: [PrismaModule, StorageModule, ConfigModule]
- StorageService exposes public isAvailable getter
- API builds and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-code-quality-cleanup/26-02-SUMMARY.md`
</output>
