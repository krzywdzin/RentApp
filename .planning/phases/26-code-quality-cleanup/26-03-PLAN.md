---
phase: 26-code-quality-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/common/env.validation.ts
  - apps/api/src/common/field-encryption.ts
autonomous: true
requirements: [QUAL-09, QUAL-10]

must_haves:
  truths:
    - "Database has indexes on Contract.createdById, CepikVerification.status, and Notification.createdAt"
    - "FIELD_ENCRYPTION_KEY is validated on startup in all environments"
    - "A dev fallback value triggers a logged warning"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Three new database indexes"
      contains: "@@index([createdById])"
    - path: "apps/api/src/common/env.validation.ts"
      provides: "FIELD_ENCRYPTION_KEY required for all environments"
      contains: "FIELD_ENCRYPTION_KEY"
  key_links:
    - from: "apps/api/src/common/field-encryption.ts"
      to: "env.validation.ts"
      via: "getKey() reads validated env var"
      pattern: "FIELD_ENCRYPTION_KEY"
---

<objective>
Add missing database indexes for query performance and enforce FIELD_ENCRYPTION_KEY in all environments with a dev fallback warning.

Purpose: Database queries filtering by Contract.createdById, CepikVerification.status, and ordering by Notification.createdAt will use indexes instead of full table scans. Encryption key misconfiguration will be caught at startup.
Output: Updated Prisma schema with indexes, env validation requiring encryption key.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-code-quality-cleanup/26-CONTEXT.md
@.planning/codebase/API-AUDIT.md (sections 13.1, 14.1, 14.2, 14.3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing database indexes to Prisma schema</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (search for "model Contract", "model CepikVerification", "model Notification" to find their @@index blocks)
  </read_first>
  <action>
Add three indexes to the Prisma schema:

1. **Contract model**: Add `@@index([createdById])` to the existing index list. This supports queries filtering by the employee who created the contract.

2. **CepikVerification model**: Add `@@index([status])` to support admin review queries filtering by FAILED/PENDING status.

3. **Notification model**: Add `@@index([createdAt])` to support the `getNotificationLog` query which orders by `createdAt desc`. The existing indexes cover type/relatedEntityId/scheduledFor and status but NOT createdAt.

After adding indexes, generate a Prisma migration:
```bash
cd apps/api && npx prisma migrate dev --name add-missing-indexes --create-only
```
Then verify the SQL contains the expected CREATE INDEX statements. Do NOT apply the migration -- just create it.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && grep -c "@@index(\[createdById\])" apps/api/prisma/schema.prisma && grep -A 20 "model CepikVerification" apps/api/prisma/schema.prisma | grep -c "@@index(\[status\])" && grep -A 30 "model Notification " apps/api/prisma/schema.prisma | grep -c "@@index(\[createdAt\])"</automated>
  </verify>
  <acceptance_criteria>
    - grep "@@index(\[createdById\])" apps/api/prisma/schema.prisma (appears in Contract model context)
    - grep -A 20 "model CepikVerification" apps/api/prisma/schema.prisma | grep -q "@@index(\[status\])"
    - grep -A 30 "model Notification " apps/api/prisma/schema.prisma | grep -q "@@index(\[createdAt\])"
    - ls apps/api/prisma/migrations/*add-missing-indexes/ (migration directory exists)
  </acceptance_criteria>
  <done>Three indexes added to Prisma schema (Contract.createdById, CepikVerification.status, Notification.createdAt) with migration file generated</done>
</task>

<task type="auto">
  <name>Task 2: Enforce FIELD_ENCRYPTION_KEY in all environments with dev fallback warning</name>
  <files>apps/api/src/common/env.validation.ts, apps/api/src/common/field-encryption.ts</files>
  <read_first>
    - apps/api/src/common/env.validation.ts (full file -- line ~44 has FIELD_ENCRYPTION_KEY in prodRequired)
    - apps/api/src/common/field-encryption.ts (full file -- getKey() function)
  </read_first>
  <action>
1. **env.validation.ts**: Move FIELD_ENCRYPTION_KEY from the `prodRequired` array to the `optionalDefaults` pattern with a deterministic dev fallback value. The approach:
   - Add FIELD_ENCRYPTION_KEY to `optionalDefaults` with value like `'0'.repeat(64)` (a known-weak key for dev only)
   - This ensures the env var always has a value so `getKey()` never throws at runtime
   - Remove it from `prodRequired` since validation now handles it differently

   Alternatively (preferred approach): Move FIELD_ENCRYPTION_KEY to the always-required validation list so it MUST be set in .env for every environment. The `.env.example` already has a placeholder value, so developers will always have it set.

2. **field-encryption.ts**: Update `getKey()` to:
   - Check if the key is the known dev fallback (all zeros or the .env.example placeholder)
   - If it IS a dev fallback, log a warning: `Logger.warn('FIELD_ENCRYPTION_KEY is using a development fallback value. Set a secure key for production.', 'FieldEncryption')`
   - Import Logger from '@nestjs/common'
   - Continue to validate that the key is a 64-character hex string

The net effect: App starts in all environments (dev gets fallback with warning, prod requires a real key).
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/api exec tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "FIELD_ENCRYPTION_KEY" apps/api/src/common/env.validation.ts
    - grep -q "warn" apps/api/src/common/field-encryption.ts
    - grep -q "Logger" apps/api/src/common/field-encryption.ts
    - grep -q "fallback\|development\|dev" apps/api/src/common/field-encryption.ts
  </acceptance_criteria>
  <done>FIELD_ENCRYPTION_KEY is validated on startup in all environments; dev fallback triggers a logged warning; production without a real key either fails validation or logs prominently</done>
</task>

</tasks>

<verification>
- `pnpm --filter @rentapp/api exec tsc --noEmit` passes
- Prisma schema has all three new indexes
- Migration file exists with CREATE INDEX SQL
- FIELD_ENCRYPTION_KEY validation works in env.validation.ts
- field-encryption.ts logs warning for dev fallback values
</verification>

<success_criteria>
- Contract.createdById, CepikVerification.status, Notification.createdAt have @@index in schema.prisma
- Migration file generated (not applied -- deployer runs `prisma migrate deploy`)
- FIELD_ENCRYPTION_KEY required at startup with dev fallback warning
- API compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/26-code-quality-cleanup/26-03-SUMMARY.md`
</output>
