---
phase: 26-code-quality-cleanup
plan: 04
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - apps/web/src/app/(admin)/wynajmy/columns.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/page.tsx
  - apps/web/src/app/(admin)/wynajmy/nowy/page.tsx
  - apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx
  - apps/web/src/app/(admin)/pojazdy/nowy/page.tsx
  - apps/web/src/app/(admin)/klienci/nowy/page.tsx
  - apps/web/src/app/(admin)/klienci/[id]/edytuj/page.tsx
  - apps/web/src/hooks/queries/use-audit.ts
  - apps/web/src/app/(admin)/uzytkownicy/columns.tsx
  - apps/web/src/app/(admin)/wynajmy/[id]/dokumentacja/page.tsx
  - apps/mobile/src/lib/constants.ts
autonomous: true
requirements: [QUAL-03, QUAL-07]

must_haves:
  truths:
    - "Web form pages use zodResolver output types directly instead of unsafe `as` casts"
    - "RentalWithRelations is imported from @rentapp/shared, not defined locally"
    - "Unused exports (userColumns alias, contractStatusLabel wrapper) are removed"
    - "CANCELLED status removed from mobile RENTAL_STATUS_COLORS"
  artifacts:
    - path: "apps/web/src/app/(admin)/wynajmy/columns.tsx"
      provides: "Import of RentalWithRelations from shared"
      contains: "from '@rentapp/shared'"
    - path: "apps/web/src/app/(admin)/wynajmy/[id]/page.tsx"
      provides: "Import of RentalWithRelations from shared, no local interface"
      contains: "from '@rentapp/shared'"
    - path: "apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx"
      provides: "No `as UpdateVehicleInput` cast"
    - path: "apps/web/src/app/(admin)/pojazdy/nowy/page.tsx"
      provides: "No `as CreateVehicleInput` cast"
  key_links:
    - from: "apps/web/src/app/(admin)/wynajmy/columns.tsx"
      to: "@rentapp/shared"
      via: "import { RentalWithRelations }"
      pattern: "import.*RentalWithRelations.*from.*@rentapp/shared"
    - from: "apps/web/src/app/(admin)/wynajmy/[id]/page.tsx"
      to: "@rentapp/shared"
      via: "import { RentalWithRelations }"
      pattern: "import.*RentalWithRelations.*from.*@rentapp/shared"
---

<objective>
Replace unsafe `as` type casts in web form pages with proper zodResolver typing, consolidate RentalWithRelations to use the shared package export, and remove dead code across web and mobile.

Purpose: Eliminate type safety bypasses in web forms so TypeScript catches DTO/schema drift. Remove dead code that confuses developers.
Output: Web form pages use proper types; RentalWithRelations imported from shared; dead code removed.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-code-quality-cleanup/26-CONTEXT.md
@.planning/codebase/WEB-AUDIT.md (sections 1.1-1.8, 15.1-15.4)
@.planning/codebase/INFRA-AUDIT.md (SHARED-4)

Depends on Plan 01 SUMMARY for RentalWithRelations type location:
@.planning/phases/26-code-quality-cleanup/26-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From packages/shared/src/types/rental.types.ts:
```typescript
export interface RentalWithRelations extends RentalDto {
  vehicle?: { registration: string; make: string; model: string; mileage: number };
  customer?: { firstName: string; lastName: string; companyName?: string };
}
```

From packages/shared/src/types/common.types.ts:
```typescript
export interface PaginatedResponse<T> {
  data: T[];
  total: number;
  page: number;
  limit: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace unsafe `as` casts in web form pages with proper zodResolver typing</name>
  <files>apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx, apps/web/src/app/(admin)/pojazdy/nowy/page.tsx, apps/web/src/app/(admin)/klienci/nowy/page.tsx, apps/web/src/app/(admin)/klienci/[id]/edytuj/page.tsx, apps/web/src/app/(admin)/wynajmy/nowy/page.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx (full file -- line ~76 has `data as UpdateVehicleInput`)
    - apps/web/src/app/(admin)/pojazdy/nowy/page.tsx (full file -- line ~67 has `data as CreateVehicleInput`)
    - apps/web/src/app/(admin)/klienci/nowy/page.tsx (full file -- line ~54 has `cleaned as CreateCustomerInput`)
    - apps/web/src/app/(admin)/klienci/[id]/edytuj/page.tsx (full file -- line ~62 has `cleaned as UpdateCustomerInput`)
    - apps/web/src/app/(admin)/wynajmy/nowy/page.tsx (full file -- lines ~66, ~70, ~153)
  </read_first>
  <action>
For each form page, replace the unsafe `as XxxInput` cast pattern with proper typing:

1. **pojazdy/[id]/edytuj/page.tsx** (line ~76):
   - Change form type from `z.input<typeof UpdateVehicleSchema>` to `z.output<typeof UpdateVehicleSchema>`
   - This makes the zodResolver output match the DTO type directly
   - Remove the `data as UpdateVehicleInput` cast -- use `data` directly since z.output matches the mutation input
   - If z.output doesn't exactly match UpdateVehicleInput, use `z.infer<typeof UpdateVehicleSchema>` as the form generic and let TypeScript verify compatibility

2. **pojazdy/nowy/page.tsx** (line ~67): Same pattern -- change FormValues to `z.output<typeof CreateVehicleSchema>`, remove `data as CreateVehicleInput` cast. Also move the `type FormValues` outside the component function body (audit item 1.2).

3. **klienci/nowy/page.tsx** (line ~54): Change to `z.output<typeof CreateCustomerSchema>`, remove `cleaned as CreateCustomerInput`.

4. **klienci/[id]/edytuj/page.tsx** (line ~62): Change to `z.output<typeof UpdateCustomerSchema>`, remove `cleaned as UpdateCustomerInput`.

5. **wynajmy/nowy/page.tsx** (lines ~66, ~70, ~153):
   - Line ~70: Replace `zodResolver(formSchema) as unknown as Resolver<FormValues>` -- fix by using `z.infer<typeof formSchema>` as the form generic type so zodResolver type aligns naturally
   - Line ~153: Replace `setValue('customerId', '' as unknown as string)` with `setValue('customerId', '')` -- empty string is a valid string, the `as unknown` is unnecessary
   - If the root cause is a Zod/RHF version mismatch, check if @hookform/resolvers is compatible with the installed zod version

For all files: If the `as` cast is needed because z.output has slightly different types (e.g., optional vs undefined), adjust the Zod schema's `.transform()` or the mutation's input type to align them. The goal is zero `as XxxInput` and zero `as unknown as` casts.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/web exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "as UpdateVehicleInput\|as CreateVehicleInput\|as CreateCustomerInput\|as UpdateCustomerInput" apps/web/src/app/\(admin\)/pojazdy/\[id\]/edytuj/page.tsx apps/web/src/app/\(admin\)/pojazdy/nowy/page.tsx apps/web/src/app/\(admin\)/klienci/nowy/page.tsx apps/web/src/app/\(admin\)/klienci/\[id\]/edytuj/page.tsx returns 0
    - grep -c "as unknown as Resolver" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns 0
    - grep -c "as unknown as string" apps/web/src/app/\(admin\)/wynajmy/nowy/page.tsx returns 0
  </acceptance_criteria>
  <done>All web form pages use proper zodResolver typing with zero unsafe `as` casts; TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate RentalWithRelations and remove dead code</name>
  <files>apps/web/src/app/(admin)/wynajmy/columns.tsx, apps/web/src/app/(admin)/wynajmy/[id]/page.tsx, apps/web/src/app/(admin)/uzytkownicy/columns.tsx, apps/mobile/src/lib/constants.ts</files>
  <read_first>
    - apps/web/src/app/(admin)/wynajmy/columns.tsx (lines 1-20 for local RentalWithRelations)
    - apps/web/src/app/(admin)/wynajmy/[id]/page.tsx (lines 1-15 for local RentalWithRelations, lines 50-60 for contractStatusLabel)
    - apps/web/src/app/(admin)/uzytkownicy/columns.tsx (line ~104 for unused userColumns export)
    - apps/mobile/src/lib/constants.ts (line ~13 for CANCELLED in RENTAL_STATUS_COLORS)
    - packages/shared/src/types/rental.types.ts (verify RentalWithRelations exists from Plan 01)
  </read_first>
  <action>
1. **wynajmy/columns.tsx** (lines ~12-15): Remove local `interface RentalWithRelations extends RentalDto { ... }`. Add import: `import { RentalWithRelations } from '@rentapp/shared'`. Update the existing RentalDto import if needed.

2. **wynajmy/[id]/page.tsx** (lines ~7-10): Remove local `interface RentalWithRelations extends RentalDto { ... }`. Add import: `import { RentalWithRelations } from '@rentapp/shared'`. Also remove the redundant `contractStatusLabel` wrapper function (lines ~57-58) -- replace usage on line ~330 with direct `contractStatusLabels[contract.status] || contract.status`.

3. **uzytkownicy/columns.tsx** (line ~104): Remove the unused `export const userColumns = getUserColumns` alias. Only `getUserColumns` is imported by consumers.

4. **mobile constants.ts** (line ~13): Remove `CANCELLED: '#DC2626'` from `RENTAL_STATUS_COLORS`. The CANCELLED status does not exist in the RentalStatus enum.

5. Check if `use-audit.ts` can use the new AuditLogDto type from shared. Read the file and if it defines local audit types, replace with imports from @rentapp/shared.
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/web exec tsc --noEmit 2>&1 | head -10 && pnpm --filter @rentapp/mobile exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "from '@rentapp/shared'" apps/web/src/app/\(admin\)/wynajmy/columns.tsx (imports RentalWithRelations)
    - grep -q "from '@rentapp/shared'" apps/web/src/app/\(admin\)/wynajmy/\[id\]/page.tsx (imports RentalWithRelations)
    - grep -c "interface RentalWithRelations" apps/web/src/app/\(admin\)/wynajmy/columns.tsx returns 0
    - grep -c "interface RentalWithRelations" apps/web/src/app/\(admin\)/wynajmy/\[id\]/page.tsx returns 0
    - grep -c "userColumns" apps/web/src/app/\(admin\)/uzytkownicy/columns.tsx returns 0
    - grep -c "CANCELLED" apps/mobile/src/lib/constants.ts returns 0
  </acceptance_criteria>
  <done>RentalWithRelations imported from @rentapp/shared (not defined locally); unused exports removed; CANCELLED phantom status removed from mobile</done>
</task>

</tasks>

<verification>
- `pnpm --filter @rentapp/web exec tsc --noEmit` passes
- `pnpm --filter @rentapp/mobile exec tsc --noEmit` passes
- Zero `as XxxInput` casts in form pages
- Zero local `RentalWithRelations` definitions in web
- No unused userColumns export
- No CANCELLED in mobile constants
</verification>

<success_criteria>
- All web form submit handlers use properly typed zodResolver output without unsafe casts
- RentalWithRelations imported from @rentapp/shared in both web files that use it
- contractStatusLabel wrapper function removed (direct lookup used)
- userColumns alias removed from uzytkownicy/columns.tsx
- CANCELLED removed from mobile RENTAL_STATUS_COLORS
- Both web and mobile typecheck cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/26-code-quality-cleanup/26-04-SUMMARY.md`
</output>
