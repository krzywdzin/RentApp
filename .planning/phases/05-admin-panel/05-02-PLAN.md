---
phase: 05-admin-panel
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/src/app/(admin)/pojazdy/page.tsx
  - apps/web/src/app/(admin)/pojazdy/nowy/page.tsx
  - apps/web/src/app/(admin)/pojazdy/[id]/page.tsx
  - apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx
  - apps/web/src/app/(admin)/pojazdy/columns.tsx
  - apps/web/src/app/(admin)/pojazdy/filter-bar.tsx
  - apps/web/src/app/(admin)/klienci/page.tsx
  - apps/web/src/app/(admin)/klienci/nowy/page.tsx
  - apps/web/src/app/(admin)/klienci/[id]/page.tsx
  - apps/web/src/app/(admin)/klienci/[id]/edytuj/page.tsx
  - apps/web/src/app/(admin)/klienci/columns.tsx
  - apps/web/src/app/(admin)/klienci/filter-bar.tsx
  - apps/web/src/hooks/queries/use-vehicles.ts
  - apps/web/src/hooks/queries/use-customers.ts
  - apps/web/src/components/ui/dialog.tsx
  - apps/web/src/components/ui/dropdown-menu.tsx
  - apps/web/src/components/ui/checkbox.tsx
  - apps/web/src/components/ui/label.tsx
  - apps/web/src/components/ui/textarea.tsx
  - apps/web/src/components/ui/form.tsx
  - apps/web/src/components/ui/tabs.tsx
  - apps/web/src/components/ui/popover.tsx
  - apps/web/src/components/ui/calendar.tsx
  - apps/web/src/components/ui/breadcrumb.tsx
  - apps/web/src/lib/csv-export.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "Admin can see a sortable, paginated list of vehicles with status badges"
    - "Admin can filter vehicles by registration/VIN and status"
    - "Admin can select vehicles with checkboxes and perform bulk status change or CSV export"
    - "Admin can create a new vehicle with all required fields validated by Zod"
    - "Admin can view vehicle detail page with tabs (Dane, Wynajmy, Dokumenty)"
    - "Admin can edit an existing vehicle"
    - "Admin can see a sortable, paginated list of customers"
    - "Admin can filter customers by name or phone"
    - "Admin can create, view detail, and edit customers"
  artifacts:
    - path: "apps/web/src/app/(admin)/pojazdy/page.tsx"
      provides: "Vehicle list page with DataTable, filters, bulk ops"
      contains: "useVehicles"
    - path: "apps/web/src/app/(admin)/pojazdy/nowy/page.tsx"
      provides: "Create vehicle form with Zod validation"
      contains: "CreateVehicleSchema"
    - path: "apps/web/src/app/(admin)/pojazdy/[id]/page.tsx"
      provides: "Vehicle detail page with tabs"
      contains: "Tabs"
    - path: "apps/web/src/app/(admin)/klienci/page.tsx"
      provides: "Customer list page with DataTable and filters"
      contains: "useCustomers"
    - path: "apps/web/src/lib/csv-export.ts"
      provides: "CSV export utility for bulk operations"
      contains: "exportToCsv"
  key_links:
    - from: "apps/web/src/app/(admin)/pojazdy/page.tsx"
      to: "/api/vehicles"
      via: "useVehicles query hook"
      pattern: "useVehicles"
    - from: "apps/web/src/app/(admin)/pojazdy/nowy/page.tsx"
      to: "@rentapp/shared"
      via: "Zod schema import for form validation"
      pattern: "CreateVehicleSchema"
    - from: "apps/web/src/app/(admin)/klienci/page.tsx"
      to: "/api/customers"
      via: "useCustomers query hook"
      pattern: "useCustomers"
---

<objective>
Build Vehicle and Customer CRUD pages with full data table experience (sort, filter, paginate), create/edit forms with Zod validation from @rentapp/shared, detail pages with tabs, and vehicle bulk operations (checkbox select + status change + CSV export).

Purpose: Delivers ADMIN-01 (full CRUD on vehicles and customers) and ADMIN-02 (search/filter + bulk operations) for these two entities.
Output: Functional vehicle and customer management pages.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-panel/05-CONTEXT.md
@.planning/phases/05-admin-panel/05-UI-SPEC.md
@.planning/phases/05-admin-panel/05-01-SUMMARY.md

<interfaces>
From packages/shared/src/schemas/vehicle.schemas.ts:
```typescript
export const CreateVehicleSchema = z.object({
  registration: z.string().min(2).max(15),
  vin: z.string().length(17).regex(/^[A-HJ-NPR-Z0-9]{17}$/i),
  make: z.string().min(1).max(100),
  model: z.string().min(1).max(100),
  year: z.number().int().min(1900).max(currentYear + 1),
  color: z.string().max(50).nullable().optional(),
  fuelType: z.enum(['PETROL', 'DIESEL', 'LPG', 'HYBRID', 'ELECTRIC']),
  transmission: z.enum(['MANUAL', 'AUTOMATIC']),
  seatCount: z.number().int().min(1).max(99).default(5),
  mileage: z.number().int().min(0).default(0),
  notes: z.string().max(1000).nullable().optional(),
});
export const UpdateVehicleSchema = CreateVehicleSchema.partial().extend({ status, isArchived });
export type CreateVehicleInput = z.infer<typeof CreateVehicleSchema>;
export type UpdateVehicleInput = z.infer<typeof UpdateVehicleSchema>;
```

From packages/shared/src/schemas/customer.schemas.ts (to be read by executor):
- CreateCustomerSchema, UpdateCustomerSchema, CustomerSearchSchema

API endpoints (vehicles):
- GET /vehicles — returns VehicleDto[]
- POST /vehicles — body: CreateVehicleInput → VehicleDto
- GET /vehicles/:id — returns VehicleDto
- PATCH /vehicles/:id — body: UpdateVehicleInput → VehicleDto
- PATCH /vehicles/:id/archive — soft delete

API endpoints (customers):
- GET /customers — returns CustomerDto[]
- GET /customers/search?q=... — returns CustomerSearchResultDto[]
- POST /customers — body: CreateCustomerInput → CustomerDto
- GET /customers/:id — returns CustomerDto
- PATCH /customers/:id — body: UpdateCustomerInput → CustomerDto
- PATCH /customers/:id/archive — soft delete
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vehicle list with filters and bulk ops, vehicle CRUD forms and detail page</name>
  <files>
    apps/web/src/app/(admin)/pojazdy/page.tsx, apps/web/src/app/(admin)/pojazdy/columns.tsx,
    apps/web/src/app/(admin)/pojazdy/filter-bar.tsx, apps/web/src/app/(admin)/pojazdy/nowy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/[id]/page.tsx, apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx,
    apps/web/src/hooks/queries/use-vehicles.ts, apps/web/src/lib/csv-export.ts,
    apps/web/src/components/ui/dialog.tsx, apps/web/src/components/ui/dropdown-menu.tsx,
    apps/web/src/components/ui/checkbox.tsx, apps/web/src/components/ui/label.tsx,
    apps/web/src/components/ui/textarea.tsx, apps/web/src/components/ui/form.tsx,
    apps/web/src/components/ui/tabs.tsx, apps/web/src/components/ui/popover.tsx,
    apps/web/src/components/ui/breadcrumb.tsx
  </files>
  <read_first>
    apps/web/src/components/data-table/data-table.tsx,
    apps/web/src/components/data-table/data-table-pagination.tsx,
    apps/web/src/components/data-table/data-table-column-header.tsx,
    apps/web/src/lib/api-client.ts,
    apps/web/src/lib/format.ts,
    apps/web/src/lib/utils.ts,
    apps/web/src/hooks/queries/use-vehicles.ts,
    packages/shared/src/schemas/vehicle.schemas.ts,
    packages/shared/src/types/vehicle.types.ts,
    apps/api/src/vehicles/vehicles.controller.ts,
    .planning/phases/05-admin-panel/05-UI-SPEC.md
  </read_first>
  <action>
1. Install additional shadcn/ui components: `npx shadcn@latest add dialog dropdown-menu checkbox label textarea form tabs popover breadcrumb`

2. Expand `src/hooks/queries/use-vehicles.ts` with full query/mutation hooks:
   - `vehicleKeys` factory: all, list(filters), detail(id)
   - `useVehicles()`: useQuery calling `apiClient<VehicleDto[]>('/vehicles')`
   - `useVehicle(id)`: useQuery calling `apiClient<VehicleDto>('/vehicles/' + id)`
   - `useCreateVehicle()`: useMutation POST /vehicles, invalidates vehicleKeys.all on success, shows toast "Pojazd dodany pomyslnie"
   - `useUpdateVehicle(id)`: useMutation PATCH /vehicles/:id, invalidates vehicleKeys.all + detail(id), toast "Zmiany zapisane"
   - `useArchiveVehicle(id)`: useMutation PATCH /vehicles/:id/archive, invalidates vehicleKeys.all, toast "Pojazd usuniety"
   - `useBulkUpdateVehicles()`: useMutation that takes { ids: string[], status: VehicleStatus } and calls PATCH /vehicles/:id for each, invalidates vehicleKeys.all

3. Create `src/lib/csv-export.ts`:
   - `exportToCsv<T>(data, columns: { key: keyof T; label: string }[], filename)` — generates CSV with BOM for Polish chars, triggers download
   - UTF-8 BOM prefix (\uFEFF) for Excel compatibility

4. Create `src/app/(admin)/pojazdy/columns.tsx` — column definitions for TanStack Table:
   - Columns: checkbox (selection), Rejestracja (sortable), Marka/Model (combined), Rok, Przebieg (formatted with km), Status (badge per UI-SPEC colors: AVAILABLE=green "Dostepny", RENTED=amber "Wynajety", SERVICE=zinc "Serwis", RETIRED=zinc "Wycofany"), Akcje (dropdown menu)
   - Actions dropdown: "Szczegoly" (navigate to detail), "Edytuj" (navigate to edit), "Usun" (opens delete confirmation dialog)
   - Row click navigates to detail page

5. Create `src/app/(admin)/pojazdy/filter-bar.tsx` — 'use client':
   - Text input for registration/VIN search (debounced 300ms)
   - Select dropdown for status filter (Wszystkie, Dostepny, Wynajety, Serwis, Wycofany)
   - Client-side filtering of the vehicle array (dataset is small)
   - Uses nuqs for URL state persistence of filters

6. Create `src/app/(admin)/pojazdy/page.tsx` — 'use client' vehicle list:
   - Page header: "Pojazdy" title + "Dodaj pojazd" primary button (links to /pojazdy/nowy)
   - FilterBar component
   - DataTable with vehicle columns, client-side pagination (useReactTable with data filtered by filter-bar)
   - Bulk operations bar (appears when rows selected): "Zmien status" dropdown + "Eksportuj CSV" button
   - Delete confirmation dialog using shadcn dialog per UI-SPEC: title "Usunac pojazd?", body with registration, destructive confirm button "Usun pojazd", cancel "Anuluj"
   - Bulk status change confirmation dialog per UI-SPEC: "Zmienic status {count} pojazdow?"

7. Create `src/app/(admin)/pojazdy/nowy/page.tsx` — 'use client' create vehicle form:
   - Breadcrumbs: Pojazdy > Nowy pojazd
   - React Hook Form with `zodResolver(CreateVehicleSchema)` imported from @rentapp/shared
   - Form fields matching CreateVehicleSchema: registration, vin, make, model, year, color, fuelType (select), transmission (select), seatCount, mileage, notes (textarea)
   - Polish labels for all fields: "Numer rejestracyjny", "Numer VIN", "Marka", "Model", "Rok produkcji", "Kolor", "Rodzaj paliwa", "Skrzynia biegow", "Liczba miejsc", "Przebieg (km)", "Notatki"
   - FuelType options in Polish: "Benzyna", "Diesel", "LPG", "Hybrydowy", "Elektryczny"
   - Transmission options: "Manualna", "Automatyczna"
   - Submit button "Dodaj pojazd" calls useCreateVehicle mutation
   - On success: redirect to /pojazdy with toast
   - Loading state: button shows "Dodawanie..." and is disabled

8. Create `src/app/(admin)/pojazdy/[id]/edytuj/page.tsx` — 'use client' edit vehicle:
   - Breadcrumbs: Pojazdy > {registration} > Edytuj
   - Same form as create but pre-filled with existing data from useVehicle(id)
   - Uses UpdateVehicleSchema for validation
   - Submit button "Zapisz zmiany" calls useUpdateVehicle mutation
   - Loading state: button "Zapisywanie..." disabled

9. Create `src/app/(admin)/pojazdy/[id]/page.tsx` — 'use client' vehicle detail:
   - Breadcrumbs: Pojazdy > {registration}
   - Header: registration as title, status badge, "Edytuj" button
   - Tabs component with 3 tabs:
     a. "Dane" tab: card grid showing all vehicle fields (make, model, year, VIN, fuel type, transmission, seats, mileage, color, notes, insurance info, inspection info)
     b. "Wynajmy" tab: small DataTable showing rentals for this vehicle (placeholder query for now — filter from useRentals where vehicleId matches)
     c. "Dokumenty" tab: list of uploaded documents/photos if any (read from vehicle.photoUrl, insurance.documentUrl, inspection.documentUrl)
   - Archive button with destructive confirmation dialog
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/(admin)/pojazdy/page.tsx contains "useVehicles" and "Dodaj pojazd" and "Eksportuj CSV"
    - apps/web/src/app/(admin)/pojazdy/columns.tsx contains "Rejestracja" and "Dostepny" and "Wynajety"
    - apps/web/src/app/(admin)/pojazdy/nowy/page.tsx contains "CreateVehicleSchema" and "zodResolver" and "Numer rejestracyjny"
    - apps/web/src/app/(admin)/pojazdy/[id]/page.tsx contains "Tabs" and "Dane" and "Wynajmy" and "Dokumenty"
    - apps/web/src/app/(admin)/pojazdy/[id]/edytuj/page.tsx contains "UpdateVehicleSchema" and "Zapisz zmiany"
    - apps/web/src/app/(admin)/pojazdy/filter-bar.tsx contains "nuqs" or "useQueryState"
    - apps/web/src/lib/csv-export.ts contains "exportToCsv" and "\uFEFF"
    - apps/web/src/hooks/queries/use-vehicles.ts contains "useCreateVehicle" and "useUpdateVehicle" and "useArchiveVehicle"
    - `pnpm --filter @rentapp/web build` succeeds
  </acceptance_criteria>
  <done>Vehicle list page with sortable columns, status badges, filter bar (registration/VIN + status), bulk operations (status change + CSV export), create form with Zod validation, edit form, and detail page with 3 tabs. All Polish labels.</done>
</task>

<task type="auto">
  <name>Task 2: Customer list with filters, customer CRUD forms and detail page</name>
  <files>
    apps/web/src/app/(admin)/klienci/page.tsx, apps/web/src/app/(admin)/klienci/columns.tsx,
    apps/web/src/app/(admin)/klienci/filter-bar.tsx, apps/web/src/app/(admin)/klienci/nowy/page.tsx,
    apps/web/src/app/(admin)/klienci/[id]/page.tsx, apps/web/src/app/(admin)/klienci/[id]/edytuj/page.tsx,
    apps/web/src/hooks/queries/use-customers.ts
  </files>
  <read_first>
    apps/web/src/components/data-table/data-table.tsx,
    apps/web/src/app/(admin)/pojazdy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/nowy/page.tsx,
    apps/web/src/app/(admin)/pojazdy/columns.tsx,
    apps/web/src/hooks/queries/use-vehicles.ts,
    apps/web/src/lib/api-client.ts,
    packages/shared/src/schemas/customer.schemas.ts,
    packages/shared/src/types/customer.types.ts,
    apps/api/src/customers/customers.controller.ts,
    .planning/phases/05-admin-panel/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `src/hooks/queries/use-customers.ts`:
   - `customerKeys` factory: all, list(filters), detail(id), search(query)
   - `useCustomers()`: useQuery GET /customers → CustomerDto[]
   - `useCustomer(id)`: useQuery GET /customers/:id → CustomerDto
   - `useSearchCustomers(q)`: useQuery GET /customers/search?q={q} → CustomerSearchResultDto[] (enabled only when q.length >= 2)
   - `useCreateCustomer()`: useMutation POST /customers, invalidates customerKeys.all, toast "Klient dodany pomyslnie"
   - `useUpdateCustomer(id)`: useMutation PATCH /customers/:id, invalidates customerKeys.all + detail(id), toast "Zmiany zapisane"
   - `useArchiveCustomer(id)`: useMutation PATCH /customers/:id/archive, invalidates customerKeys.all, toast "Klient usuniety"

2. Create `src/app/(admin)/klienci/columns.tsx`:
   - Columns: Nazwisko (lastName, sortable), Imie (firstName), Telefon (phone), Email, Data dodania (createdAt, formatted), Akcje (dropdown)
   - Actions: "Szczegoly", "Edytuj", "Usun"
   - Row click navigates to detail

3. Create `src/app/(admin)/klienci/filter-bar.tsx`:
   - Text input for name/phone search (debounced 300ms)
   - Client-side filtering of customer array
   - Uses nuqs for URL state

4. Create `src/app/(admin)/klienci/page.tsx` — 'use client':
   - Header: "Klienci" + "Dodaj klienta" primary button
   - FilterBar + DataTable with customer columns
   - Delete confirmation dialog: "Usunac klienta?", body "Dane klienta {firstName} {lastName} zostana trwale usuniete..."
   - Empty state: "Brak klientow" / "Klienci pojawia sie tutaj po dodaniu pierwszego rekordu."

5. Create `src/app/(admin)/klienci/nowy/page.tsx` — 'use client':
   - Breadcrumbs: Klienci > Nowy klient
   - React Hook Form with zodResolver(CreateCustomerSchema) from @rentapp/shared
   - Fields with Polish labels: "Imie" (firstName), "Nazwisko" (lastName), "Telefon" (phone), "Email" (email), "Adres" (address), "PESEL" (pesel), "Numer dowodu" (idNumber), "Organ wydajacy dowod" (idIssuedBy), "Data wydania dowodu" (idIssuedDate), "Numer prawa jazdy" (licenseNumber), "Kategoria prawa jazdy" (licenseCategory), "Organ wydajacy prawo jazdy" (licenseIssuedBy)
   - Submit button "Dodaj klienta"
   - On success: redirect to /klienci with toast

6. Create `src/app/(admin)/klienci/[id]/edytuj/page.tsx` — 'use client':
   - Breadcrumbs: Klienci > {lastName} {firstName} > Edytuj
   - Same form pre-filled, UpdateCustomerSchema validation
   - Submit "Zapisz zmiany", toast on success

7. Create `src/app/(admin)/klienci/[id]/page.tsx` — 'use client':
   - Breadcrumbs: Klienci > {lastName} {firstName}
   - Header: name as title, "Edytuj" button
   - Tabs with 2 tabs:
     a. "Dane" tab: card grid with all customer fields. Sensitive fields (PESEL, idNumber, licenseNumber) displayed as received from API (API returns decrypted values for authorized users)
     b. "Wynajmy" tab: small DataTable showing rentals for this customer (filter from useRentals where customerId matches)
   - Archive button with destructive confirmation
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/(admin)/klienci/page.tsx contains "useCustomers" and "Dodaj klienta" and "Brak klientow"
    - apps/web/src/app/(admin)/klienci/columns.tsx contains "Nazwisko" and "Telefon"
    - apps/web/src/app/(admin)/klienci/nowy/page.tsx contains "CreateCustomerSchema" and "zodResolver" and "PESEL"
    - apps/web/src/app/(admin)/klienci/[id]/page.tsx contains "Tabs" and "Dane" and "Wynajmy"
    - apps/web/src/hooks/queries/use-customers.ts contains "useCreateCustomer" and "useSearchCustomers"
    - `pnpm --filter @rentapp/web build` succeeds
  </acceptance_criteria>
  <done>Customer list page with filter bar (name/phone), CRUD forms with Zod validation, detail page with Dane and Wynajmy tabs, archive with confirmation. All Polish labels.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @rentapp/web build` passes
- Vehicle pages: list with filters + bulk ops, create/edit forms, detail with tabs
- Customer pages: list with filters, create/edit forms, detail with tabs
- All forms use Zod schemas from @rentapp/shared
- All UI text in Polish per UI-SPEC copywriting contract
- Status badges use correct colors per UI-SPEC
</verification>

<success_criteria>
- Admin can browse vehicles with sort/filter/pagination and perform bulk status change + CSV export
- Admin can create, view, edit, and archive vehicles through the web panel
- Admin can browse customers with search/filter
- Admin can create, view, edit, and archive customers through the web panel
- All forms validate input using shared Zod schemas
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-panel/05-02-SUMMARY.md`
</output>
