---
phase: 05-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/src/app/globals.css
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/login/page.tsx
  - apps/web/src/app/(admin)/layout.tsx
  - apps/web/src/app/(admin)/page.tsx
  - apps/web/src/components/layout/sidebar.tsx
  - apps/web/src/components/layout/top-bar.tsx
  - apps/web/src/components/layout/breadcrumbs.tsx
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/input.tsx
  - apps/web/src/components/ui/card.tsx
  - apps/web/src/components/ui/badge.tsx
  - apps/web/src/components/ui/skeleton.tsx
  - apps/web/src/components/ui/tooltip.tsx
  - apps/web/src/components/ui/scroll-area.tsx
  - apps/web/src/components/ui/separator.tsx
  - apps/web/src/components/ui/sonner.tsx
  - apps/web/src/components/ui/avatar.tsx
  - apps/web/src/components/data-table/data-table.tsx
  - apps/web/src/components/data-table/data-table-pagination.tsx
  - apps/web/src/components/data-table/data-table-column-header.tsx
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/utils.ts
  - apps/web/src/lib/format.ts
  - apps/web/src/lib/providers.tsx
  - apps/web/src/hooks/use-auth.ts
  - apps/web/src/middleware.ts
  - apps/web/src/app/api/auth/login/route.ts
  - apps/web/src/app/api/auth/refresh/route.ts
  - apps/web/src/app/api/auth/logout/route.ts
  - apps/web/src/app/api/[...path]/route.ts
  - apps/api/src/main.ts
autonomous: true
requirements:
  - ADMIN-01
must_haves:
  truths:
    - "Next.js app builds successfully within the Turbo monorepo"
    - "Unauthenticated user is redirected to /login"
    - "Admin can log in and see a collapsible sidebar with 6 nav items in Polish"
    - "Auth tokens are stored in httpOnly cookies via BFF proxy"
    - "DataTable component renders with server-side pagination controls"
    - "API requests from frontend reach the NestJS backend through the proxy"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js app with all required dependencies"
      contains: "@tanstack/react-query"
    - path: "apps/web/src/middleware.ts"
      provides: "Auth middleware redirecting to /login"
      contains: "access_token"
    - path: "apps/web/src/components/layout/sidebar.tsx"
      provides: "Collapsible sidebar with Polish nav labels"
      contains: "Pulpit"
    - path: "apps/web/src/lib/api-client.ts"
      provides: "Centralized fetch wrapper for API calls"
      contains: "apiClient"
    - path: "apps/web/src/components/data-table/data-table.tsx"
      provides: "Generic TanStack Table wrapper with pagination/sorting"
      contains: "useReactTable"
    - path: "apps/web/src/app/api/[...path]/route.ts"
      provides: "BFF proxy forwarding requests to NestJS API"
      contains: "Bearer"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "/login"
      via: "cookie check and redirect"
      pattern: "access_token"
    - from: "apps/web/src/app/api/[...path]/route.ts"
      to: "apps/api"
      via: "fetch proxy with Bearer token from cookie"
      pattern: "Authorization.*Bearer"
    - from: "apps/web/src/app/api/auth/login/route.ts"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "POST proxy that sets httpOnly cookies"
      pattern: "cookies\\.set"
---

<objective>
Scaffold the Next.js admin panel app within the Turbo monorepo, configure shadcn/ui with dark zinc theme, build the auth flow (BFF proxy + httpOnly cookies + middleware), create the admin layout (collapsible sidebar + top bar), set up shared infrastructure (API client, TanStack Query provider, reusable DataTable component, formatting utilities), and update API CORS for credential support.

Purpose: Every subsequent plan depends on this foundation -- the app scaffold, auth flow, layout shell, API client, and reusable DataTable are prerequisites for all entity pages.
Output: A running Next.js app with login, authenticated layout shell, and reusable components ready for entity pages.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-panel/05-CONTEXT.md
@.planning/phases/05-admin-panel/05-RESEARCH.md
@.planning/phases/05-admin-panel/05-UI-SPEC.md

<interfaces>
<!-- Shared types and schemas consumed by the frontend -->

From packages/shared/src/types/user.types.ts:
```typescript
export enum UserRole { ADMIN = 'ADMIN', EMPLOYEE = 'EMPLOYEE', CUSTOMER = 'CUSTOMER' }
export interface UserDto { id: string; email: string; name: string; role: UserRole; isActive: boolean; createdAt: string; updatedAt: string; }
export interface TokenPairDto { accessToken: string; refreshToken: string; deviceId: string; }
```

From packages/shared/src/types/vehicle.types.ts:
```typescript
export enum VehicleStatus { AVAILABLE = 'AVAILABLE', RESERVED = 'RESERVED', RENTED = 'RENTED', SERVICE = 'SERVICE', RETIRED = 'RETIRED' }
export interface VehicleDto { id: string; registration: string; vin: string; make: string; model: string; year: number; color: string | null; fuelType: FuelType; transmission: TransmissionType; seatCount: number; mileage: number; notes: string | null; status: VehicleStatus; photoUrl: string | null; isArchived: boolean; insurance: VehicleInsuranceDto | null; inspection: VehicleInspectionDto | null; createdAt: string; updatedAt: string; }
```

From packages/shared/src/types/rental.types.ts:
```typescript
export enum RentalStatus { DRAFT = 'DRAFT', ACTIVE = 'ACTIVE', EXTENDED = 'EXTENDED', RETURNED = 'RETURNED' }
export interface RentalDto { id: string; vehicleId: string; customerId: string; createdById: string; startDate: string; endDate: string; status: RentalStatus; dailyRateNet: number; totalPriceNet: number; totalPriceGross: number; vatRate: number; ... }
```

Auth API endpoints (from apps/api/src/auth/auth.controller.ts):
- POST /auth/login — body: { email, password, deviceId } → returns { accessToken, refreshToken, deviceId }
- POST /auth/refresh — body: { deviceId, refreshToken }, header: Authorization Bearer <expired-token> → returns new token pair
- POST /auth/logout — body: { deviceId }, requires auth

API main.ts currently uses `app.enableCors()` with NO options — must update to support credentials.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js app, shadcn/ui, dependencies, API CORS update, auth BFF proxy, and middleware</name>
  <files>
    apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/postcss.config.mjs,
    apps/web/components.json, apps/web/src/app/globals.css, apps/web/src/app/layout.tsx,
    apps/web/src/lib/utils.ts, apps/web/src/lib/api-client.ts, apps/web/src/lib/format.ts,
    apps/web/src/lib/providers.tsx, apps/web/src/hooks/use-auth.ts, apps/web/src/middleware.ts,
    apps/web/src/app/api/auth/login/route.ts, apps/web/src/app/api/auth/refresh/route.ts,
    apps/web/src/app/api/auth/logout/route.ts, apps/web/src/app/api/[...path]/route.ts,
    apps/web/src/app/login/page.tsx, apps/api/src/main.ts
  </files>
  <read_first>
    apps/api/src/main.ts,
    apps/api/src/auth/auth.controller.ts,
    packages/shared/src/types/user.types.ts,
    packages/shared/src/index.ts,
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    .planning/phases/05-admin-panel/05-RESEARCH.md,
    .planning/phases/05-admin-panel/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `apps/web/` directory. Initialize Next.js App Router project:
   - `package.json` with name `@rentapp/web`, scripts: `dev` (next dev -p 3001), `build` (next build), `start` (next start), `lint` (next lint)
   - Install dependencies: `next`, `react`, `react-dom`, `@tanstack/react-table`, `@tanstack/react-query`, `react-hook-form`, `@hookform/resolvers`, `zod`, `date-fns`, `lucide-react`, `sonner`, `nuqs`, `@rentapp/shared` (workspace:*)
   - Dev dependencies: `typescript`, `@types/react`, `@types/react-dom`, `@types/node`, `tailwindcss`, `@tailwindcss/postcss`, `postcss`
   - `next.config.ts`: set `transpilePackages: ['@rentapp/shared']`
   - `tsconfig.json`: extend Next.js tsconfig with path alias `@/*` -> `./src/*`
   - `postcss.config.mjs`: `@tailwindcss/postcss` plugin

2. Initialize shadcn/ui: Run `npx shadcn@latest init` in `apps/web/` OR manually create:
   - `components.json` with style: default, baseColor: zinc, cssVariables: true, aliases: `@/components`, `@/lib`, `@/hooks`
   - `src/app/globals.css` with Tailwind v4 import (`@import "tailwindcss"`) and shadcn dark theme CSS variables for zinc palette as specified in UI-SPEC (background: 240 10% 3.9%, card: 240 10% 7.8%, primary: 0 0% 98%, destructive: 0 84% 60%, muted: 240 3.7% 15.9%, border: 240 3.7% 15.9%, ring: 240 4.9% 83.9%)
   - `src/lib/utils.ts` with `cn()` helper using clsx + tailwind-merge

3. Install shadcn/ui components needed for scaffold: `npx shadcn@latest add button input card skeleton sonner avatar tooltip`

4. Create `src/lib/api-client.ts`:
   - `API_BASE` = `/api` (all requests go through Next.js BFF proxy)
   - Export `ApiError` class with status and data
   - Export async function `apiClient<T>(path, options)` that calls `fetch(API_BASE + path, { ...options, headers: { 'Content-Type': 'application/json', ...options.headers } })`, throws `ApiError` on non-OK response, returns `res.json()`

5. Create `src/lib/format.ts`:
   - `formatDate(date)` → `format(new Date(date), 'd MMM yyyy', { locale: pl })` (import pl from date-fns/locale/pl)
   - `formatDateTime(date)` → `format(new Date(date), 'd MMM yyyy, HH:mm', { locale: pl })`
   - `formatCurrency(grosze)` → `Intl.NumberFormat('pl-PL', { style: 'currency', currency: 'PLN' }).format(grosze / 100)`

6. Create `src/lib/providers.tsx` — 'use client' component wrapping children with:
   - `QueryClientProvider` from @tanstack/react-query with default QueryClient (staleTime: 30000)
   - `Toaster` from sonner positioned bottom-right
   - `NuqsAdapter` from nuqs/adapters/next/app

7. Create `src/app/layout.tsx` (Server Component):
   - HTML lang="pl", dark class on html element
   - Import globals.css, Inter font from next/font/google
   - Wrap children in `<Providers>`

8. Create auth BFF proxy Route Handlers:
   - `src/app/api/auth/login/route.ts`: POST handler that receives {email, password}, generates a deviceId (crypto.randomUUID()), forwards to `API_URL/auth/login` with {email, password, deviceId}, on success sets `access_token` (httpOnly, secure in prod, sameSite lax, path /, maxAge 900) and `refresh_token` (httpOnly, secure in prod, sameSite lax, path /api/auth, maxAge 86400) cookies, returns { user } or the response body minus tokens. API_URL from `process.env.API_URL ?? 'http://localhost:3000'`.
   - `src/app/api/auth/refresh/route.ts`: POST handler that reads refresh_token + access_token from cookies, forwards to API_URL/auth/refresh with { deviceId, refreshToken } and Authorization: Bearer <access_token>, sets new cookies on success.
   - `src/app/api/auth/logout/route.ts`: POST handler that reads cookies, forwards to API_URL/auth/logout, clears both cookies.

9. Create general API proxy `src/app/api/[...path]/route.ts`:
   - Strips `/api` prefix from pathname
   - Reads `access_token` from cookies
   - Forwards to `API_URL + path + search` with `Authorization: Bearer <token>` header
   - Passes through method, headers (Content-Type), and body
   - Export GET, POST, PATCH, DELETE handlers

10. Create `src/hooks/use-auth.ts` — 'use client' hook:
    - Stores user state (UserDto | null) in React state
    - `login(email, password)`: POST to `/api/auth/login`, stores returned user
    - `logout()`: POST to `/api/auth/logout`, clears user, redirects to /login via router.push
    - `checkAuth()`: GET to `/api/users/me` (through proxy), sets user if OK, otherwise null
    - Export `AuthProvider` context and `useAuth` hook

11. Create `src/middleware.ts`:
    - Check for `access_token` cookie
    - If no token and not on `/login` → redirect to `/login`
    - If token and on `/login` → redirect to `/`
    - Matcher: `['/((?!_next/static|_next/image|favicon.ico|api).*)']` (exclude API routes)

12. Create `src/app/login/page.tsx` — 'use client' login page:
    - Form with email + password fields using shadcn input/button
    - Uses `useAuth().login()` on submit
    - Redirects to `/` on success via router.push
    - Shows error toast on failure
    - Polish labels: "Email", "Haslo", button "Zaloguj sie"
    - Dark theme, centered card layout

13. Update `apps/api/src/main.ts` CORS configuration:
    - Change `app.enableCors()` to `app.enableCors({ origin: process.env.WEB_URL ?? 'http://localhost:3001', credentials: true })`
    - This allows the BFF proxy to communicate with the API (though the proxy runs server-side, the credentials config ensures proper handling)
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm install && pnpm --filter @rentapp/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json exists with "@tanstack/react-query", "@tanstack/react-table", "react-hook-form", "@rentapp/shared"
    - apps/web/src/middleware.ts contains "access_token" cookie check
    - apps/web/src/app/api/auth/login/route.ts contains "cookies.set" for httpOnly token storage
    - apps/web/src/app/api/[...path]/route.ts contains "Bearer" for auth forwarding
    - apps/web/src/lib/api-client.ts exports "apiClient" function
    - apps/web/src/lib/format.ts imports "pl" from date-fns locale
    - apps/web/src/app/login/page.tsx contains "Zaloguj"
    - apps/api/src/main.ts contains "credentials: true" in CORS config
    - `pnpm --filter @rentapp/web build` succeeds
  </acceptance_criteria>
  <done>Next.js app scaffolded in apps/web/ with shadcn/ui dark zinc theme, auth BFF proxy with httpOnly cookies, middleware redirect, login page, API client, format utils, and TanStack Query provider. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Admin layout shell (sidebar + top bar), reusable DataTable, and dashboard page</name>
  <files>
    apps/web/src/app/(admin)/layout.tsx, apps/web/src/components/layout/sidebar.tsx,
    apps/web/src/components/layout/top-bar.tsx, apps/web/src/components/layout/breadcrumbs.tsx,
    apps/web/src/components/data-table/data-table.tsx,
    apps/web/src/components/data-table/data-table-pagination.tsx,
    apps/web/src/components/data-table/data-table-column-header.tsx,
    apps/web/src/components/ui/table.tsx, apps/web/src/components/ui/select.tsx,
    apps/web/src/components/ui/scroll-area.tsx, apps/web/src/components/ui/separator.tsx,
    apps/web/src/components/ui/badge.tsx, apps/web/src/components/ui/collapsible.tsx,
    apps/web/src/app/(admin)/page.tsx, apps/web/src/components/dashboard/stat-card.tsx,
    apps/web/src/hooks/queries/use-vehicles.ts, apps/web/src/hooks/queries/use-rentals.ts
  </files>
  <read_first>
    apps/web/src/app/layout.tsx,
    apps/web/src/lib/api-client.ts,
    apps/web/src/lib/utils.ts,
    apps/web/src/lib/format.ts,
    apps/web/src/hooks/use-auth.ts,
    .planning/phases/05-admin-panel/05-UI-SPEC.md
  </read_first>
  <action>
1. Install additional shadcn/ui components: `npx shadcn@latest add table select scroll-area separator badge collapsible`

2. Create `src/components/layout/sidebar.tsx` — 'use client':
   - Collapsible sidebar: expanded 256px, collapsed 64px (icon only with tooltip on hover)
   - Navigation items as flat list with lucide icons per UI-SPEC:
     - { href: '/', label: 'Pulpit', icon: LayoutDashboard }
     - { href: '/pojazdy', label: 'Pojazdy', icon: Car }
     - { href: '/klienci', label: 'Klienci', icon: Users }
     - { href: '/wynajmy', label: 'Wynajmy', icon: CalendarClock }
     - { href: '/umowy', label: 'Umowy', icon: FileText }
     - { href: '/audyt', label: 'Audyt', icon: Shield }
   - Active item: left 3px border accent color, background `--muted`
   - Use `usePathname()` from next/navigation to detect active item
   - Chevron toggle button at bottom to collapse/expand
   - Store collapsed state in localStorage for persistence
   - Dark background using `--card` color (zinc-900)

3. Create `src/components/layout/top-bar.tsx` — 'use client':
   - Height ~48px, spans full width above content
   - Left: breadcrumbs area (passed as children or via context)
   - Right: user avatar (initials from user name) + dropdown with "Wyloguj" option
   - Uses `useAuth()` for user info and logout

4. Create `src/components/layout/breadcrumbs.tsx`:
   - Accept `items: { label: string; href?: string }[]` prop
   - Render with shadcn breadcrumb pattern, last item non-clickable
   - Polish labels

5. Create `src/app/(admin)/layout.tsx`:
   - Route group layout for authenticated pages
   - Renders Sidebar on left + main content area on right
   - TopBar above content
   - AuthProvider wrapping the layout (calls checkAuth on mount)
   - Flex layout: sidebar fixed width, content area flex-1 with overflow-auto

6. Create reusable DataTable components:
   a. `src/components/data-table/data-table.tsx` — 'use client':
      - Generic component `DataTable<TData, TValue>` using @tanstack/react-table's `useReactTable`
      - Props: `columns`, `data`, `pageCount`, `pagination`, `onPaginationChange`, `sorting`, `onSortingChange`, `rowSelection?`, `onRowSelectionChange?`, `onRowClick?`, `isLoading?`
      - Set `manualPagination: true`, `manualSorting: true` for server-side control
      - Render shadcn `<Table>` with `<TableHeader>`, `<TableBody>`, `<TableRow>`, `<TableCell>`
      - Loading state: render 5 skeleton rows
      - Empty state: centered message "Brak danych" in table body

   b. `src/components/data-table/data-table-pagination.tsx`:
      - Shows "Strona {n} z {total}" with prev/next buttons
      - Per-page selector with options: 10, 25, 50
      - Uses shadcn button and select

   c. `src/components/data-table/data-table-column-header.tsx`:
      - Sortable column header with arrow indicators (asc/desc/none)
      - Click toggles sort direction

7. Create dashboard stat card: `src/components/dashboard/stat-card.tsx`:
   - Props: `title: string`, `value: string | number`, `subtitle: string`, `icon: LucideIcon`, `variant?: 'default' | 'destructive'`
   - shadcn Card with icon, display-size value (28px font), title, subtitle
   - Destructive variant: count in `--destructive` color + 3px left border in destructive

8. Create initial query hooks for dashboard data:
   - `src/hooks/queries/use-vehicles.ts`: vehicleKeys factory, `useVehicles()` hook calling `apiClient<VehicleDto[]>('/vehicles')`
   - `src/hooks/queries/use-rentals.ts`: rentalKeys factory, `useRentals()` hook calling `apiClient<RentalDto[]>('/rentals')`

9. Create `src/app/(admin)/page.tsx` — 'use client' dashboard:
   - Heading "Pulpit" (Display size 28px)
   - 4 stat cards in responsive grid (4 cols on xl, 2 on md, 1 on sm):
     - "Aktywne wynajmy" — count of rentals with status ACTIVE or EXTENDED, icon CalendarClock, subtitle "{count} pojazdow w uzyciu"
     - "Dostepne pojazdy" — count of vehicles with status AVAILABLE, icon Car, subtitle "{count} z {total} w flocie"
     - "Dzisiejsze zwroty" — count of ACTIVE/EXTENDED rentals with endDate = today, icon Clock, subtitle "{count} zaplanowanych"
     - "Przeterminowane" — count of ACTIVE/EXTENDED rentals past endDate, icon AlertTriangle, variant destructive if count > 0, subtitle "{count} wymaga uwagi"
   - Compute counts client-side from vehicle and rental list queries (dataset is small for local fleet)
   - Loading state: 4 skeleton cards
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && pnpm --filter @rentapp/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/layout/sidebar.tsx contains "Pulpit", "Pojazdy", "Klienci", "Wynajmy", "Umowy", "Audyt"
    - apps/web/src/components/layout/sidebar.tsx contains "LayoutDashboard" and "Car" and "Shield" lucide imports
    - apps/web/src/components/data-table/data-table.tsx contains "useReactTable" and "manualPagination: true"
    - apps/web/src/components/data-table/data-table-pagination.tsx contains "Strona"
    - apps/web/src/app/(admin)/page.tsx contains "Aktywne wynajmy" and "Dostepne pojazdy" and "Przeterminowane"
    - apps/web/src/components/dashboard/stat-card.tsx contains "destructive"
    - apps/web/src/app/(admin)/layout.tsx contains "Sidebar"
    - `pnpm --filter @rentapp/web build` succeeds
  </acceptance_criteria>
  <done>Admin layout shell with collapsible sidebar (6 Polish nav items), top bar with user menu, reusable DataTable with pagination/sorting, dashboard page with 4 stat cards computed from API data. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @rentapp/web build` passes (TypeScript + Next.js compilation)
- Login page renders at /login with Polish labels
- Auth middleware redirects unauthenticated users to /login
- Admin layout renders sidebar with 6 navigation items
- Dashboard page renders 4 stat cards
- DataTable component is generic and reusable for all entity lists
</verification>

<success_criteria>
- Next.js app builds in Turbo monorepo with @rentapp/shared dependency resolved
- Auth flow works: login page -> BFF proxy -> API -> httpOnly cookies -> middleware allows access
- Sidebar shows all 6 navigation items with correct Polish labels and lucide icons
- Dashboard displays stat cards with data from vehicles and rentals API
- DataTable component ready for entity list pages with pagination and sorting
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-panel/05-01-SUMMARY.md`
</output>
