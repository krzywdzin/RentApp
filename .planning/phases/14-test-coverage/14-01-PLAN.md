---
phase: 14-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/vitest.config.ts
  - apps/web/src/test/setup.ts
  - apps/web/src/test/test-utils.tsx
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/src/app/(admin)/__tests__/dashboard.test.tsx
  - apps/web/src/app/(admin)/wynajmy/__tests__/rentals-page.test.tsx
  - apps/web/src/app/(admin)/pojazdy/__tests__/vehicles-page.test.tsx
  - apps/web/src/app/(admin)/klienci/__tests__/customers-page.test.tsx
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Web admin component tests exist and pass for dashboard, rental list, vehicle list, and customer list"
    - "Tests verify rendering, loading states, error states, and key UI elements"
    - "Test infrastructure (Vitest + RTL + jsdom) is configured and working"
  artifacts:
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest configuration with jsdom, path aliases, setup file"
    - path: "apps/web/src/test/setup.ts"
      provides: "Global test setup (cleanup, mocks)"
    - path: "apps/web/src/test/test-utils.tsx"
      provides: "Reusable render wrapper with QueryClientProvider"
    - path: "apps/web/src/app/(admin)/__tests__/dashboard.test.tsx"
      provides: "Dashboard page component tests"
    - path: "apps/web/src/app/(admin)/wynajmy/__tests__/rentals-page.test.tsx"
      provides: "Rental list page component tests"
    - path: "apps/web/src/app/(admin)/pojazdy/__tests__/vehicles-page.test.tsx"
      provides: "Vehicle list page component tests"
    - path: "apps/web/src/app/(admin)/klienci/__tests__/customers-page.test.tsx"
      provides: "Customer list page component tests"
  key_links:
    - from: "apps/web/vitest.config.ts"
      to: "apps/web/src/test/setup.ts"
      via: "setupFiles config"
      pattern: "setupFiles.*setup"
    - from: "test files"
      to: "apps/web/src/test/test-utils.tsx"
      via: "import { render }"
      pattern: "import.*test-utils"
---

<objective>
Set up Vitest + React Testing Library for the web admin panel and create component tests for the 4 critical pages: dashboard, rental list, vehicle list, and customer list.

Purpose: Catch regressions in core admin screens before merge -- rendering, loading states, error states, and key interactions.
Output: Working test infrastructure and 4 passing test suites.
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/package.json
@apps/web/tsconfig.json
@apps/web/next.config.ts
@apps/web/src/app/(admin)/page.tsx
@apps/web/src/app/(admin)/wynajmy/page.tsx
@apps/web/src/app/(admin)/pojazdy/vehicles-page.tsx
@apps/web/src/app/(admin)/klienci/customers-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vitest + React Testing Library infrastructure for apps/web</name>
  <files>apps/web/vitest.config.ts, apps/web/src/test/setup.ts, apps/web/src/test/test-utils.tsx, apps/web/package.json, apps/web/tsconfig.json</files>
  <read_first>
    - apps/web/package.json (current deps)
    - apps/web/tsconfig.json (path aliases)
    - apps/web/next.config.ts (transpilePackages)
  </read_first>
  <action>
1. Install dev dependencies in apps/web:
   ```
   cd apps/web && pnpm add -D vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
   ```

2. Create apps/web/vitest.config.ts:
   - Import defineConfig from vitest/config and react plugin from @vitejs/plugin-react
   - Set test.environment to "jsdom"
   - Set test.globals to true
   - Set test.setupFiles to ["./src/test/setup.ts"]
   - Set test.include to ["src/**/*.test.{ts,tsx}"]
   - Set resolve.alias: "@" maps to "./src" (matching tsconfig paths)
   - Set test.css to false (skip CSS processing)

3. Create apps/web/src/test/setup.ts:
   - Import "@testing-library/jest-dom/vitest" for DOM matchers (toBeInTheDocument, etc.)
   - Import { cleanup } from @testing-library/react and call afterEach(cleanup)
   - Mock next/navigation: vi.mock("next/navigation", () => ({ useRouter: () => ({ push: vi.fn(), back: vi.fn(), refresh: vi.fn() }), usePathname: () => "/", useSearchParams: () => new URLSearchParams() }))

4. Create apps/web/src/test/test-utils.tsx:
   - Import { render, type RenderOptions } from @testing-library/react
   - Import { QueryClient, QueryClientProvider } from @tanstack/react-query
   - Create a createTestQueryClient() that returns a new QueryClient with defaultOptions: { queries: { retry: false, gcTime: 0 }, mutations: { retry: false } }
   - Export a custom render function that wraps component in QueryClientProvider with a fresh test QueryClient
   - Re-export everything from @testing-library/react
   - Export userEvent from @testing-library/user-event

5. Add "test" script to apps/web/package.json scripts: "test": "vitest run"
   Add "test:watch" script: "test:watch": "vitest"

6. Add "src/test" to tsconfig.json include array if not present. Ensure the tsconfig types array includes "vitest/globals" for global test functions. If tsconfig has no types field, add compilerOptions.types: ["vitest/globals"].
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/web && npx vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "vitest" apps/web/package.json
    - grep -q "testing-library" apps/web/package.json
    - test -f apps/web/vitest.config.ts
    - test -f apps/web/src/test/setup.ts
    - test -f apps/web/src/test/test-utils.tsx
  </acceptance_criteria>
  <done>Vitest + RTL infrastructure is installed and configured. Running `pnpm test` in apps/web exits 0 (no tests yet, passWithNoTests).</done>
</task>

<task type="auto">
  <name>Task 2: Create component tests for dashboard, rental list, vehicle list, and customer list pages</name>
  <files>apps/web/src/app/(admin)/__tests__/dashboard.test.tsx, apps/web/src/app/(admin)/wynajmy/__tests__/rentals-page.test.tsx, apps/web/src/app/(admin)/pojazdy/__tests__/vehicles-page.test.tsx, apps/web/src/app/(admin)/klienci/__tests__/customers-page.test.tsx</files>
  <read_first>
    - apps/web/src/app/(admin)/page.tsx (dashboard - uses useVehicles, useRentals)
    - apps/web/src/app/(admin)/wynajmy/page.tsx (rentals - uses useRentals)
    - apps/web/src/app/(admin)/pojazdy/vehicles-page.tsx (vehicles - uses useVehicles, useArchiveVehicle, useBulkUpdateVehicles)
    - apps/web/src/app/(admin)/klienci/customers-page.tsx (customers - uses useCustomers, useArchiveCustomer)
    - apps/web/src/hooks/queries/use-rentals.ts (hook signatures)
    - apps/web/src/hooks/queries/use-vehicles.ts (hook signatures)
    - apps/web/src/hooks/queries/use-customers.ts (hook signatures)
    - apps/web/src/test/test-utils.tsx (custom render)
  </read_first>
  <action>
IMPORTANT: All tests must mock hooks at the module level so components render without real API calls. Use vi.mock() for each hook module. Each test file follows the same pattern:

**For each test file:**
1. Import render, screen, waitFor from the custom test-utils (../../test/test-utils or appropriate relative path)
2. Import the page component
3. vi.mock() the query hooks used by that page
4. Mock @rentapp/shared to provide any types/enums needed (e.g., RentalStatus)

**apps/web/src/app/(admin)/__tests__/dashboard.test.tsx:**
- vi.mock("@/hooks/queries/use-vehicles") and vi.mock("@/hooks/queries/use-rentals")
- Test "renders loading skeletons when data is loading":
  - Mock useVehicles and useRentals to return { data: undefined, isLoading: true, isError: false, refetch: vi.fn() }
  - Render DashboardPage
  - Expect document to contain skeleton elements or loading indicators
- Test "renders stat cards with data":
  - Mock useVehicles returning { data: [{ id: "v1", status: "AVAILABLE", registrationNumber: "ABC123" }], isLoading: false, isError: false, refetch: vi.fn() }
  - Mock useRentals returning { data: [{ id: "r1", status: "ACTIVE", startDate: "2026-01-01", endDate: "2026-12-31" }], isLoading: false, isError: false, refetch: vi.fn() }
  - Expect screen.getByText matching vehicle/rental count or stat labels
- Test "renders error state with retry button":
  - Mock hooks returning { data: undefined, isLoading: false, isError: true, refetch: vi.fn() }
  - Expect screen.getByRole("button") with retry text

**apps/web/src/app/(admin)/wynajmy/__tests__/rentals-page.test.tsx:**
- vi.mock("@/hooks/queries/use-rentals")
- Test "renders loading state": mock isLoading: true, check for loading skeleton or spinner
- Test "renders rental list with data": mock data with 2 rental objects containing { id, status: "ACTIVE", startDate, endDate, vehicle: { registrationNumber: "ABC" }, customer: { firstName: "Jan", lastName: "Kowalski" } }, verify "Wynajmy" heading and rental data visible
- Test "renders empty state": mock data: [], verify empty or "0" indication

**apps/web/src/app/(admin)/pojazdy/__tests__/vehicles-page.test.tsx:**
- vi.mock("@/hooks/queries/use-vehicles")
- Test "renders loading state": mock isLoading: true
- Test "renders vehicle table with data": mock data with vehicle objects { id, registrationNumber: "WA12345", make: "Toyota", model: "Corolla", status: "AVAILABLE" }
- Test "shows vehicle data in table": verify registrationNumber, make text visible
- Mock useArchiveVehicle and useBulkUpdateVehicles as { mutate: vi.fn(), isPending: false }

**apps/web/src/app/(admin)/klienci/__tests__/customers-page.test.tsx:**
- vi.mock("@/hooks/queries/use-customers")
- Test "renders loading state": mock isLoading: true
- Test "renders customer table with data": mock data with customer objects { id, firstName: "Anna", lastName: "Nowak", email: "anna@test.pl", phone: "123456789" }
- Mock useArchiveCustomer as { mutate: vi.fn(), isPending: false }

IMPORTANT: For any shared types imports (RentalStatus, VehicleStatus, etc.), check if they need mocking. If @rentapp/shared is a workspace package and vitest resolve.alias doesn't handle it, add it to vitest.config.ts resolve.alias or use vi.mock. Check if the transpilePackages in next.config.ts needs a vitest equivalent -- likely add @rentapp/shared to deps.inline in vitest config.

Also mock any lucide-react icons if they cause issues: vi.mock("lucide-react", () => new Proxy({}, { get: (_, name) => () => null }))

After writing all 4 test files, run: cd apps/web && npx vitest run
If tests fail, debug and fix. Common issues:
- Missing mock for a sub-component or hook
- Path alias not resolving -- check vitest.config.ts resolve.alias
- Module not found for @rentapp/shared -- add to deps.inline in vitest config
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp/apps/web && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f apps/web/src/app/\(admin\)/__tests__/dashboard.test.tsx
    - test -f apps/web/src/app/\(admin\)/wynajmy/__tests__/rentals-page.test.tsx
    - test -f apps/web/src/app/\(admin\)/pojazdy/__tests__/vehicles-page.test.tsx
    - test -f apps/web/src/app/\(admin\)/klienci/__tests__/customers-page.test.tsx
    - cd apps/web && npx vitest run 2>&1 | grep -q "passed"
  </acceptance_criteria>
  <done>4 component test suites pass: dashboard (loading, data, error), rental list (loading, data, empty), vehicle list (loading, data), customer list (loading, data). All tests run via `pnpm test` in apps/web with exit code 0.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx vitest run` exits 0 with all tests passing
- At least 8 test cases across 4 test files
- No test uses real API calls (all hooks mocked)
</verification>

<success_criteria>
- Vitest + React Testing Library configured in apps/web
- 4 critical admin pages have passing component tests
- Tests cover: render without crash, loading states, data display, error states
- `pnpm test` in apps/web exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-test-coverage/14-01-SUMMARY.md`
</output>
