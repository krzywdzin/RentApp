---
phase: 33
slug: foundation-schema-simple-fields
status: draft
shadcn_initialized: true
preset: default (zinc base, css variables, lucide icons)
created: 2026-04-12
---

# Phase 33 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (web), custom components (mobile) |
| Preset | default — zinc baseColor, cssVariables: true |
| Component library | Radix UI (web via shadcn), React Native core (mobile) |
| Icon library | lucide-react (web), inline SVG (mobile) |
| Font (web) | Fraunces (display), Satoshi (body), IBM Plex Mono (data) |
| Font (mobile) | Fraunces-Variable (display), Satoshi-Variable (body), IBMPlexMono (data) |

Source: `apps/web/components.json`, `apps/mobile/src/lib/theme.ts`

---

## Spacing Scale

Standard scale (multiples of 4) used across both web and mobile:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, label-to-input margin, error text margin-top |
| sm | 8px | Inline icon padding (iconWrap marginRight), compact element spacing |
| base | 16px | Default element spacing, input horizontal padding, section icon margin |
| xl | 24px | Button horizontal padding, action wrap margin-top, major section gaps |
| xxl | 32px | Empty state horizontal padding, page-level horizontal padding |

### Legacy tokens (locked — inherited from `apps/mobile/src/lib/theme.ts`)

These values exist in the shipped mobile theme and are used across existing screens. Changing them would require a full mobile UI regression pass, which is out of scope for Phase 33.

| Token | Value | Justification |
|-------|-------|---------------|
| md | 12px | Inner form group gaps (between label groups in a section). Used in AppInput, AppCard, and all wizard steps. Changing to 8px or 16px would alter every existing form. Locked legacy token. |
| lg | 20px | Between form sections. Provides visual separation between toggle groups and field clusters. Changing to 16px or 24px would require re-testing all wizard step layouts. Locked legacy token. |
| section | 40px | Top/bottom page section breaks. Used in scroll containers for breathing room between major sections. Nearest standard values (32px, 48px) would alter page rhythm across all existing screens. Locked legacy token. |

Exceptions: Mobile input height is 48px (touch target). This is not a spacing token but a fixed component dimension. Web inputs follow shadcn default height (40px).

Source: `apps/mobile/src/lib/theme.ts` (spacing object), `AppInput.tsx` (48px height), `AppButton.tsx` (48px minHeight)

---

## Typography

### Web (Tailwind + shadcn tokens)

2 weights: 400 (body) and 600 (heading/button).

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Body | 14px (text-sm) | 400 | 1.43 (20px) | Satoshi (--font-body) |
| Label | 14px (text-sm) | 600 | 1.43 (20px) | Satoshi (--font-body) |
| Heading (page) | 20px (text-xl) | 600 | 1.2 | Fraunces (--font-display) |
| Table header | 14px (text-sm) | 600 | 1.43 | Satoshi (--font-body) |

### Mobile (StyleSheet)

2 weights: 400 (body/label/error) and 600 (heading/button).

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Body / Input | 16px | 400 | 1.5 (24px) | Satoshi-Variable |
| Label | 13px | 400 | 1.38 (18px) | system default (matches AppInput) |
| Error | 13px | 400 | 1.38 (18px) | system default |
| Section heading | 20px | 600 | 1.2 (24px) | Fraunces-Variable |
| Button | 16px | 600 | 1.5 | Satoshi-Variable |

Source: `AppInput.tsx` (13px label, 16px input), `AppButton.tsx` (16px/600), `EmptyState.tsx` (heading), `globals.css`

---

## Color

All colors are established in the project. No new colors introduced in Phase 33.

| Role | Value (hex) | OKLCH (web) | Usage |
|------|-------------|-------------|-------|
| Dominant (60%) | #FDFAF6 | oklch(0.984 0.008 80) | Page background (cream) |
| Secondary (30%) | #F3EDE7 | oklch(0.946 0.012 75) | Cards, sidebar, nav, table rows (warm-stone) |
| Accent (10%) | #1B4332 | oklch(0.33 0.06 160) | See reserved list below (forest-green) |
| Destructive | #C75D3A | oklch(0.56 0.15 40) | Delete vehicle class confirmation, error states (terracotta) |

**Accent (forest-green) reserved for:**
- Primary CTA buttons ("Dodaj klase" on /klasy page, "Zapisz nazwe" in edit dialog)
- Switch/toggle active state (isCompanyRental, isInsuranceRental)
- Focus ring on inputs (border: #3B82F6 on mobile -- existing blue kept for mobile focus; forest-green on web)
- Sidebar active link indicator

**Note on mobile focus color:** Mobile `AppInput` uses `#3B82F6` (blue) for focus border. This is an existing convention from v1.0. Do NOT change it -- maintain consistency with all existing mobile forms.

Source: `apps/mobile/src/lib/theme.ts`, `apps/web/src/app/globals.css`

---

## Component Inventory (Phase 33)

### Web — Existing shadcn Components (no new installs)

| Component | Usage in Phase 33 |
|-----------|-------------------|
| `Dialog` | Create/edit vehicle class modal |
| `Input` | Class name field, search filters |
| `Select` | Vehicle class dropdown in vehicle form |
| `Table` | Vehicle class list, vehicle list (add class column) |
| `Button` | CRUD actions, dialog confirm/cancel |
| `Label` | Form field labels |
| `Badge` | Vehicle class display in vehicle list |
| `Checkbox` | Insurance case number filter (has/doesn't have) |
| `Form` | Vehicle class create/edit form validation |

### Web — Existing Custom Components

| Component | Usage in Phase 33 |
|-----------|-------------------|
| `EmptyState` | Empty vehicle class list |
| `ErrorState` | API error on vehicle class fetch |

### Mobile — Existing Custom Components

| Component | Usage in Phase 33 |
|-----------|-------------------|
| `AppInput` | Address fields (street, houseNumber, apartmentNumber, postalCode, city), NIP field, insurance case number field |
| `AppButton` | Form submit buttons (existing wizard pattern) |
| `AppCard` | Rental detail cards showing company/insurance info |
| `WizardStepper` | Existing step indicator (no changes needed) |
| `ConfirmationDialog` | N/A for Phase 33 mobile (no destructive mobile actions) |

### Mobile — New Components Needed

| Component | Description |
|-----------|-------------|
| `AppSwitch` | Styled Switch for "Wynajem na firme" and "Najem ubezpieczeniowy?" toggles. If no existing Switch wrapper exists, create one matching AppInput styling conventions (13px label, forest-green active track, 48px touch area row). |
| `AppPicker` / `AppSelect` | Dropdown for VAT payer status selection (3 options). Use React Native `Picker` or a bottom sheet select matching project style. |

Source: `apps/mobile/src/components/`, `apps/web/src/components/ui/`

---

## Copywriting Contract

All copy in Polish (project language -- see REQUIREMENTS.md "Wielojezycznosc" out of scope, interface only in Polish).

### Mobile — Customer Address Fields (customer step)

| Element | Copy |
|---------|------|
| Section heading | Adres klienta |
| Field: street | Ulica |
| Field: houseNumber | Numer domu |
| Field: apartmentNumber | Numer mieszkania |
| Field: postalCode | Kod pocztowy |
| Field: postalCode placeholder | 00-000 |
| Field: city | Miasto |
| Validation: postalCode format | Format: XX-XXX |

### Mobile — Company/NIP/VAT Fields (dates step)

| Element | Copy |
|---------|------|
| Toggle label | Wynajem na firme |
| Field: NIP | NIP |
| Field: NIP placeholder | 0000000000 |
| Validation: NIP required | NIP jest wymagany dla wynajmu firmowego |
| Validation: NIP invalid | Nieprawidlowy NIP |
| Dropdown label | Platnik VAT |
| Option: FULL_100 | 100% |
| Option: HALF_50 | 50% |
| Option: NONE | Nie |

### Mobile — Insurance Case Number (vehicle step)

| Element | Copy |
|---------|------|
| Toggle label | Najem ubezpieczeniowy? |
| Field: case number | Nr sprawy ubezpieczeniowej |
| Field: case number placeholder | np. ABC/123/2026 |

### Web — Vehicle Class CRUD (`/klasy` page)

| Element | Copy |
|---------|------|
| Page heading | Klasy pojazdow |
| Primary CTA | Dodaj klase |
| Dialog heading (create) | Nowa klasa pojazdu |
| Dialog heading (edit) | Edytuj klase |
| Field: name | Nazwa klasy |
| Field: name placeholder | np. Ekonomiczna |
| Confirm button (create) | Dodaj klase |
| Confirm button (edit) | Zapisz nazwe |
| Cancel button (create) | Nie, wroc |
| Cancel button (edit) | Nie, wroc |
| Empty state heading | Brak klas pojazdow |
| Empty state body | Dodaj pierwsza klase, aby organizowac flote. |
| Error state | Nie udalo sie zaladowac klas. Sprobuj ponownie. |
| Delete confirmation | Usun klase "{name}"? Tej operacji nie mozna cofnac. |
| Delete blocked (vehicles assigned) | Nie mozna usunac klasy przypisanej do pojazdow. Najpierw zmien klase tych pojazdow. |
| Delete button | Usun klase |

### Web — Vehicle Form (class dropdown)

| Element | Copy |
|---------|------|
| Dropdown label | Klasa pojazdu |
| Dropdown placeholder | Wybierz klase |

### Web — Vehicle List (class column)

| Element | Copy |
|---------|------|
| Column header | Klasa |

### Web — Rental Details (new fields display)

| Element | Copy |
|---------|------|
| Section: company info | Dane firmy |
| Label: NIP | NIP |
| Label: VAT status | Platnik VAT |
| Display: FULL_100 | 100% |
| Display: HALF_50 | 50% |
| Display: NONE | Nie |
| Label: insurance case | Nr sprawy ubezpieczeniowej |
| Label: vehicle class | Klasa pojazdu |

### Web — Rental List Filters

| Element | Copy |
|---------|------|
| Filter label | Ubezpieczeniowy |
| Filter option: yes | Tak |
| Filter option: no | Nie |
| Search placeholder | Szukaj nr sprawy... |

---

## Visual Hierarchy

### `/klasy` page (web)

1. **Page heading** "Klasy pojazdow" — 20px/600 Fraunces, top-left
2. **Primary CTA** "Dodaj klase" — accent button, top-right, aligned with heading
3. **Table** — secondary background rows, 14px/400 Satoshi body cells, 14px/600 Satoshi header cells
4. **Empty state** — centered in table area, 20px/600 heading + 14px/400 body below

### Vehicle class dialog (web)

1. **Dialog heading** "Nowa klasa pojazdu" / "Edytuj klase" — 20px/600 Fraunces
2. **Input field** — single `Nazwa klasy` input, auto-focused, 14px/400 value text
3. **Button row** — right-aligned: "Nie, wroc" (ghost/outline) + "Dodaj klase" or "Zapisz nazwe" (accent filled)

### Customer address section (mobile)

1. **Section heading** "Adres klienta" — 20px/600 Fraunces-Variable, left-aligned, 20px margin-top from previous section
2. **Field stack** — AppInput fields at 12px vertical gaps between label groups
3. **Side-by-side rows** — houseNumber+apartmentNumber (50/50), postalCode+city (40/60), 8px horizontal gap

### Company/NIP toggle section (mobile, dates step)

1. **Toggle row** "Wynajem na firme" — 16px/400 label, AppSwitch right-aligned, 48px row height
2. **Revealed fields** (when ON) — NIP input + VAT picker animate in below toggle, 12px gap between fields
3. **Section boundary** — 20px gap before next section

### Insurance toggle section (mobile, vehicle step)

1. **Toggle row** "Najem ubezpieczeniowy?" — 16px/400 label, AppSwitch right-aligned, 48px row height
2. **Revealed field** (when ON) — case number input animates in below toggle, 12px gap

---

## Interaction Contracts

### Toggle Progressive Disclosure (Mobile)

**"Wynajem na firme" toggle (dates step):**
1. Default state: toggle OFF, NIP and VAT fields hidden
2. Toggle ON: animate-in NIP input + VAT picker below the toggle (use `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut)` or simple conditional render)
3. Toggle OFF: clear `companyNip` and `vatPayerStatus` values, hide fields
4. NIP validation runs on blur (not on every keystroke)

**"Najem ubezpieczeniowy?" toggle (vehicle step):**
1. Default state: toggle OFF, case number field hidden
2. Toggle ON: show insurance case number input below toggle
3. Toggle OFF: clear `insuranceCaseNumber`, hide field
4. No special validation -- free text, optional

### Address Fields Layout (Mobile)

Address fields appear as a group below existing customer personal data fields in the "Nowy klient" modal:
1. `Ulica` — full width
2. `Numer domu` + `Numer mieszkania` — side by side (50/50 split)
3. `Kod pocztowy` + `Miasto` — side by side (40/60 split)

Postal code input: `keyboardType="numeric"`, `maxLength={6}` (including dash). Auto-insert dash after 2 digits.

### Vehicle Class CRUD (Web)

1. `/klasy` page loads with table of existing classes
2. "Dodaj klase" button top-right opens Dialog with single `Nazwa klasy` input
3. Input auto-focuses on dialog open
4. Submit on Enter key or "Dodaj klase" button click
5. Inline edit: click row edit icon opens same Dialog pre-filled
6. Delete: click row delete icon shows confirmation dialog
7. If class has assigned vehicles, delete returns 409 and shows "Nie mozna usunac..." toast/inline message
8. Table is NOT paginated (expected < 20 classes)

### Vehicle Form Class Dropdown (Web)

1. shadcn `Select` component at the top of the vehicle form (after brand/model, before VIN)
2. Options fetched via `useVehicleClasses()` query hook
3. Required field -- form won't submit without selection
4. Default: no selection (placeholder "Wybierz klase")

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Dialog, Input, Select, Table, Button, Label, Badge, Checkbox, Form | not required |
| Third-party | none | not applicable |

No third-party registries. All components from shadcn official registry or existing project custom components.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
