---
phase: 32-interactive-damage-map
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/mobile/app/return/damage-map.tsx
  - apps/mobile/app/return/_layout.tsx
  - apps/mobile/app/return/confirm.tsx
  - apps/mobile/app/return/checklist.tsx
autonomous: false

requirements: [DMAP-01, DMAP-02, DMAP-03, DMAP-04]

must_haves:
  truths:
    - "Return wizard Step 3 shows SVG car diagram instead of text checklist"
    - "Worker taps car zone and modal opens for damage details"
    - "Saved damage pins appear as visual indicators on the car outline"
    - "Worker can proceed with no damage via 'Brak uszkodzen' button"
    - "Confirm screen shows damage pins summary instead of checklist items"
  artifacts:
    - path: "apps/mobile/app/return/damage-map.tsx"
      provides: "Return wizard Step 3 damage map screen"
      min_lines: 80
    - path: "apps/mobile/app/return/_layout.tsx"
      provides: "Updated layout with damage-map route replacing checklist"
      contains: "damage-map"
    - path: "apps/mobile/app/return/confirm.tsx"
      provides: "Updated confirm screen showing damage pins"
      contains: "damagePins"
  key_links:
    - from: "apps/mobile/app/return/damage-map.tsx"
      to: "apps/mobile/src/components/CarDamageMap.tsx"
      via: "component import"
      pattern: "import.*CarDamageMap"
    - from: "apps/mobile/app/return/damage-map.tsx"
      to: "apps/mobile/src/api/damage.api.ts"
      via: "API calls for walkthrough + damage report"
      pattern: "createWalkthrough|createDamageReport|confirmNoDamage"
    - from: "apps/mobile/app/return/damage-map.tsx"
      to: "apps/mobile/src/stores/return-draft.store.ts"
      via: "Zustand store for damagePins and walkthroughId"
      pattern: "useReturnDraftStore"
---

<objective>
Wire the CarDamageMap and DamageDetailModal into the return wizard, replacing the old text checklist (Step 3) with the interactive SVG damage map screen. Update confirm screen to show damage pins.

Purpose: Complete the feature by integrating all building blocks into the return wizard flow.
Output: damage-map.tsx screen, updated _layout.tsx, updated confirm.tsx
</objective>

<execution_context>
@/Users/antonio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-interactive-damage-map/32-CONTEXT.md
@.planning/phases/32-interactive-damage-map/32-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

CarDamageMap component props:
```typescript
interface CarDamageMapProps {
  pins: DamagePin[];
  onZoneTap: (zoneName: string, x: number, y: number) => void;
}
```

DamageDetailModal component props:
```typescript
interface DamageDetailModalProps {
  visible: boolean;
  zoneName: string;
  onSave: (damageType: DamageType, note: string) => void;
  onCancel: () => void;
}
```

damage.api.ts exports:
```typescript
export function createWalkthrough(rentalId: string): Promise<{ id: string }>;
export function createDamageReport(walkthroughId: string, pins: DamagePin[]): Promise<any>;
export function confirmNoDamage(walkthroughId: string): Promise<any>;
```

return-draft.store.ts updated state:
```typescript
interface ReturnDraft {
  step: number;
  rentalId: string | null;
  returnMileage: number | null;
  checklist: Record<string, ChecklistItem>; // kept for compat
  walkthroughId: string | null;  // NEW
  damagePins: DamagePin[];       // NEW
  notes: string;
}
```

Wizard flow: [rentalId] -> mileage -> damage-map (NEW, replaces checklist) -> notes -> confirm
Step numbers: 1=rental, 2=mileage, 3=damage-map, 4=notes, 5=confirm

Zone name -> Polish label map (from CarDamageMap):
hood="Maska", roof="Dach", trunk="Bagaznik", etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create damage-map screen and update wizard routing</name>
  <files>apps/mobile/app/return/damage-map.tsx, apps/mobile/app/return/_layout.tsx, apps/mobile/app/return/checklist.tsx</files>
  <read_first>
    - apps/mobile/app/return/checklist.tsx (screen being replaced — study structure)
    - apps/mobile/app/return/_layout.tsx (layout to update routes)
    - apps/mobile/app/return/mileage.tsx (previous screen — check navigation to next step)
    - apps/mobile/src/stores/return-draft.store.ts (store with new fields)
    - apps/mobile/src/api/damage.api.ts (API functions from Plan 01)
    - apps/mobile/src/components/CarDamageMap.tsx (component from Plan 01)
    - apps/mobile/src/components/DamageDetailModal.tsx (modal from Plan 01)
  </read_first>
  <action>
    1. Create `apps/mobile/app/return/damage-map.tsx`:
       - Follow same pattern as checklist.tsx (SafeAreaView, WizardStepper at step 3/5, bottom bar with Next button)
       - On mount: if no walkthroughId in store, call `createWalkthrough(rentalId)` and store the ID via updateDraft({ walkthroughId })
       - Render CarDamageMap component with current damagePins from store
       - State: `modalVisible`, `selectedZone` (zoneName, x, y)
       - onZoneTap callback: set selectedZone and show modal
       - DamageDetailModal onSave: create new DamagePin with { pinNumber: pins.length + 1, svgView: 'top', x, y, damageType, severity: 'minor', note }, add to store damagePins array
       - Show list of current damage pins below the SVG map (in ScrollView) with pin number, zone name, damage type label, and a delete button (X) that removes from store array
       - "Brak uszkodzen" (No damage) button: outline/secondary style, above Next button or as alternative action. When tapped, call confirmNoDamage(walkthroughId), clear damagePins, then navigate to notes
       - handleNext: if damagePins.length > 0, call createDamageReport(walkthroughId, damagePins). Then updateDraft({ step: 3 }) and router.push('/return/notes')
       - Handle loading state while creating walkthrough (show ActivityIndicator)
       - Use KeyboardAvoidingView like other wizard screens
       - Hydration guard same as checklist.tsx (redirect if no rentalId)

    2. Update `apps/mobile/app/return/_layout.tsx`:
       - Add Stack.Screen for "damage-map" with same listeners as checklist
       - Keep "checklist" Stack.Screen entry (Expo Router file-based routing, the file still exists but won't be navigated to)

    3. Update `apps/mobile/app/return/mileage.tsx`:
       - Change navigation from `router.push('/return/checklist')` to `router.push('/return/damage-map')` in handleNext

    4. Delete the content of `checklist.tsx` and replace with a redirect to damage-map (in case any deep links point to it):
       ```typescript
       import { Redirect } from 'expo-router';
       export default function ChecklistRedirect() {
         return <Redirect href="/return/damage-map" />;
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - damage-map.tsx renders CarDamageMap + DamageDetailModal
    - Tapping a zone opens modal, saving adds pin to store and shows on map
    - "Brak uszkodzen" button calls confirmNoDamage API
    - Next button calls createDamageReport with pins, then navigates to notes
    - Mileage screen navigates to damage-map instead of checklist
    - Layout includes damage-map route
  </acceptance_criteria>
  <done>Worker sees SVG car diagram at Step 3 of return wizard, can tap zones to mark damage, pins display on map, can proceed with or without damage</done>
</task>

<task type="auto">
  <name>Task 2: Update confirm screen to show damage pins instead of checklist</name>
  <files>apps/mobile/app/return/confirm.tsx</files>
  <read_first>
    - apps/mobile/app/return/confirm.tsx (current confirm screen)
    - packages/shared/src/types/photo.types.ts (DAMAGE_TYPE_LABELS for Polish labels)
    - apps/mobile/src/stores/return-draft.store.ts (damagePins field)
  </read_first>
  <action>
    1. Update `apps/mobile/app/return/confirm.tsx`:
       - Import damagePins from useReturnDraftStore (add `const damagePins = useReturnDraftStore((s) => s.damagePins)`)
       - Import DAMAGE_TYPE_LABELS from '@rentapp/shared'
       - Replace the checklist summary AppCard section (the one with "Step 3" label showing damaged/ok items):
         - If damagePins.length > 0: show each pin as a row with red dot, pin number, damage type label (Polish from DAMAGE_TYPE_LABELS), and note if present
         - If damagePins.length === 0: show "Brak uszkodzen" (no damage) with green dot
       - Update handleSubmit: replace checklistSummary generation with damage pins summary:
         ```
         const damageSummary = damagePins.map(p =>
           `#${p.pinNumber} ${DAMAGE_TYPE_LABELS[p.damageType]}${p.note ? ': ' + p.note : ''}`
         ).join('; ');
         ```
         Use damageSummary instead of checklistSummary in generalNotes
       - Remove CHECKLIST_ITEMS import (no longer needed)
       - Remove checklist from useReturnDraftStore selectors (no longer needed)
       - Remove damagedItems/okItems calculations
       - Keep existing handleSubmit API call structure (returnMutation.mutate) unchanged
  </action>
  <verify>
    <automated>cd /Users/antonio/Documents/RentApp && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Confirm screen shows damage pins list with Polish damage type labels
    - No damage case shows "Brak uszkodzen" with green indicator
    - Submit includes damage summary in generalNotes
    - No references to CHECKLIST_ITEMS remain in confirm.tsx
  </acceptance_criteria>
  <done>Confirm screen displays damage pins summary instead of old checklist, submit includes damage info in notes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Interactive damage map in return wizard</name>
  <files>apps/mobile/app/return/damage-map.tsx</files>
  <action>
    Human verification of the complete interactive damage map feature in the return wizard.

    How to verify:
    1. Open mobile app, navigate to an active rental
    2. Start return process (tap "Zwrot")
    3. Step 1: Confirm rental info, tap Next
    4. Step 2: Enter mileage, tap Next
    5. Step 3 (DAMAGE MAP): Verify SVG car diagram appears instead of old checklist
    6. Tap on a car zone (e.g., hood/maska) — verify modal opens
    7. Select a damage type (e.g., "Rysa"), optionally add a note, tap "Zapisz"
    8. Verify red pin appears on the car diagram at the tapped zone
    9. Tap another zone, add another damage — verify second pin appears
    10. Verify pin list below the map shows both pins with details
    11. Tap Next — should proceed to Notes screen (Step 4)
    12. Step 4: Add notes or leave empty, tap Next
    13. Step 5 (CONFIRM): Verify damage pins are shown (not old checklist items)
    14. Also test "Brak uszkodzen" flow: go back to Step 3, verify no-damage button works
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms the interactive damage map works correctly in the return wizard</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit --project apps/mobile/tsconfig.json`
- Return wizard flow: [rentalId] -> mileage -> damage-map -> notes -> confirm
- Damage map screen shows SVG car with tappable zones
- Modal opens on zone tap with damage type + notes
- Pins display on car outline after saving
- Confirm screen shows damage pins summary
- "Brak uszkodzen" flow works end-to-end
</verification>

<success_criteria>
The return wizard Step 3 is fully replaced: workers see an interactive SVG car diagram instead of the old text checklist. They can tap zones, mark damage with type and notes, see visual pins, and the confirm screen shows the damage summary. The "no damage" flow also works.
</success_criteria>

<output>
After completion, create `.planning/phases/32-interactive-damage-map/32-02-SUMMARY.md`
</output>
